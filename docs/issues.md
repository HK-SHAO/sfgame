# 用户体验问题清单

老大（即用户）会定期把问题新增在此文档中。

针对每一个问题，研发人员（或 AI Agent 等）在阅读后需要着手核实、尝试复现、深度研究和思考。

注意老大的问题或建议不一定完全属实、合适或是最佳决策，要以第一性原理、产品体验和工程可靠且优雅为要义，批判性的研究、执行和回复。

每一个问题的状态要及时更新，例如是否已阅读，是否在执行中，执行结果或简要报告要体现。报告一定要挑重点，省略低信息量（即业界高频出现的模式）的细节，不能省略 bug 的修复或缺陷的改进。

老大负责最终给每个问题结束工单（用【ok】表示，只有老大才能结束工单，研发人员/Agent 没有权限），在此前研发绝不能忽视。

此文件仅供老大更改。如需汇报状态编辑 docs/issues 目录，文件名为 #N.md，参考业界的 issue 相关经验进行编辑。

## 2026/08/05

- #20 通过 AssemblyScript、WASM、SIMD、Worker、共享内存等加速、高性能手段，把涉及到数值计算、图形渲染等 CPU 密集工作完整的，逐一迁移过去。要求研发时考虑好一致性、性能加速、鲁棒性。进行完善完整的迁移，包括“无头”测试、模拟、run-level、寻找解等可以被良好的加速。对应的 js 代码不要移除，方便通过切换“后端”的方式进行切换，进行性能验证。参考资料有：https://www.assemblyscript.org/getting-started.html 、https://www.assemblyscript.org/stdlib/globals.html、https://www.assemblyscript.org/concepts.html 。（installed assemblyscript@0.28.20 with binaries）。遇到特别难题卡点可以网上查资料。

- #19【ok】大胆删掉几乎所有注释，进行排序后，只保留20%真正必要的注释。

- #18【ok】目前的 bun check 测试耗时实在太长，请找到高耗时测试（例如「零操作挂机不能通关：进入抵达圆即算抵达（含贴地滑行），关卡设计须保证挂机轨迹不穿圆」）直接进行大幅简化（推荐大胆删除掉）。回顾所有测试，哪些测试实在太细、太严格，有冗余就大胆删除。

- #17【ok】现在有些逻辑不属于 ui 如 sfgame-web/src/ui/devtools.ts，应该放到合适的目录中去。与渲染有关的，不要放到ui目录。以此类推，把文件和文件应处目录全面整理干净。

- #16【ok】开发面板中的关卡编辑，确认生效后，面板又会折叠起来，不方便连续更改。请优化，尽量保持app、游戏状态一致性。把关卡左上角暂停按钮移动到右上的速率按钮旁边。

- #15【ok】将perf面板升级为dev面板。面板增加一个默认折叠的功能，展开后是一个输入框，允许编辑当前关卡的yaml，确认后生效。要做好鲁棒性、处理好性能问题。功能要高内聚，低耦合，模块化。编辑的关卡不会永久生效，只是临时有效，所以不会真的改变游戏设计。

- #14【ok】为每个地图增加云的视觉效果，云会受到气流的影响，云会维持一定高度比较难下降，若下降会淡出消失。云可预测的（“伪随机”）生成在空中，或生成在地图外。云超出地图较远距离会自动消失。目前云也是具有物理效果的，但是要从简设计，遵循第一性原理。如果可以也可也向前兼容，但是不要因此增加复杂度。这次目标仅是增加视觉效果，但未来可能会增加物理相关玩法。本次要克制化。

- #13【ok】反馈：移除掉 bench.html 所有相关代码。「解法参考」页面的返回按钮应该跟关卡一样有一致性，能正常返回，而不是只能回主页。「存储管理」标题下的「共 2 项 · 976 B」移动到底部，不要放到副标题（这也是为了一致性）。存储项目的具体状态或值应该可以展开看清楚，而不是只有简略。只有dev=1时才能看到主页的开发者页面按钮。开发者页面里可以切换是否dev=1（也就是开关dev模式）。

- #13【ok】增加一个开发者页面按钮在主菜单底部。解法参考页面入口也放到开发者页面里。开发者页面还可以进入 bench 页面（要有一致性的代码架构、UI/UX）。增加一个存储管理页面，此页面可以看到持久化的数据内容和占用的空间，不需要编辑功能，需要删除或清理功能。开发者页面里的关于部分显示此 app 作者是 烧风 (HK-SHAO)。可以显示当前版本号，版本号跟随 package.json 。

- #12【ok】反馈：HUD title 显示关卡和关卡标题。但是由于横向空间确实吃紧，现在建议这个标题放置到游戏画面最底部，类似一个footer。注意样式各方面与header的一致性。

- #12【ok】HUD title 和旁边按钮应该紧凑，不要因隐藏了title而出现不一致的空白间距。即使是宅屏幕例如一些手机竖屏，也应该能显示title。横向空间实在不够用时，出现横向滚动条。2. 抵达旗杆，绿色旗子消失也要带动旗杆绿色部分消失，也就是说，绿色部分相当于旗面的一部分，区别是稳定不动。3. 粒子轨迹有时候感觉有链条、折线感，请让它更丝滑，末尾圆润。4. 过关弹窗的下一关按钮应该居中。

- #11【ok】渲染层面进行优化，减少锯齿感（例如纸飞机和其轨迹线条的锯齿、旗子的锯齿、太阳）。让旗面部分的旗杆与旗面同色。让旗杆被地面遮挡。（注意这些更改应不影响到游戏关卡、物理效果等，仅属于画面上的变化）2. 让关卡设计的目标（如旗帜）范围可以被调整（即那个虚线圆的半径）。虚线圆应该准确符合抵达目标的空间范围。3. 就算是纸飞机在地面上滑过去，也应该就如飞过去一样进入虚线圆范围内就算抵达旗杆（同一套逻辑或简化逻辑）。4. 让冷、热、常温气体颜色上区分更明显（现在太多是灰色的常温气体，区分度不够明显。当然常温气体属于多数）你可以通过统计的方法看看温度分布再做调整。5. 以上所有实现，可以大胆改。本着第一性原理和降低复杂度，增加性能和鲁棒性，符合玩家体验去。

- #10【ok】的验收（先自行CR一遍整个上一个commit，找到缺陷，找到仓库中冗余代码或注释或文档内容，这些应该修复或优化掉）：1. 关于寻找参考答案，目前是否有经验，是否有稳定高性能的搜索算法或方法（要用通用方法，如启发式、遗传式，还可以利用好并行计算），可以总结成 skill。2. 我希望参考答案解法优先考虑物体（如纸飞机）走较短的路程（例如“回头路”会增加路程，目的是让纸飞机更平稳移动），其次避免飞机接触地面（或“山坡等”），再考虑减少耗时。3. dev模式下关卡允许玩家随时通过空格键暂停或恢复整个游戏的物理时间。而一个关卡完全过关后（弹出弹窗的同时）暂停物理模拟（免得背景还在持续运行，纸飞机有概率被吹走）。4. 视觉效果和音效需要优化。例如触发过关的逻辑是所有旗子被纸飞机“抵达过”（目的是简化逻辑，不限制玩家必须有先后关系抵达旗子）。经过一个旗子应该有区别于过关的合适音效（“奖励”）提示音。第一个旗子有半透明渐变的矩形，然而第二个旗子却没有（需要有一致性，我建议从简，只保留旗子本身和表达抵达范围的虚线圆，底部运动的圆环和旗杆的半透明绿色矩形光效都去掉）。当一个旗子被抵达后只留下旗杆，周围的提醒效果要消失掉（例如底部圆环和虚线圆形等）。5. 第四关在风速涨落的某一瞬间我看到气流消失不见了（随后立马出现），可能是因为风速为0时没有处理好可视化？需要修复。6. 以上所有意见，应该从底层或根源出发去解决，目标让每个关卡各方面更舒服。

## 2026/08/04

- #10【ok】了解 docs 中关于设计、玩法、创意。要有浪漫感，例如信息论、熵、庞加莱回归、宇宙演化相关的“文学”浪漫感。已有文档仅作为参考，可以跳出其中，也可以做好取舍。目前已经实现的 2 个关卡可以视作简单教学关卡，再设计三个教学关卡（完成度要十分的高，并充分验证），为之后的更多需要人工设计的关卡打好基础。例如，设计好一些基本元素、原子能力，其他研发可以像搭积木一样微调或组合出全新的关卡。因此每个关卡是可以完美 json（或yaml） 序列化的，这样也方便玩家 DIY 关卡进行传播。在设计一套足够强大的“乘法式”或“涌现式”原子能力后，也需要一个 skill 能够真实的指导新手用基于序列化的某种“协议”搭建关卡。需要特别注意不要堆砌元素或玩法，而是提高少量真正足够精选的原子能力。已有的关卡也完美移植到这一套系统。其中一个特点是，可序列化的关卡与状态能够在一个“无头”的引擎内部运行模拟，这也方便调试或接入算法或ai。对于关卡设计的要义、行业优秀的经验等，强烈建议搜索足够多高质量资料学习。最后强调，此需求的开发不设限，但克制化意味着不要让项目大规模膨胀和对各种想象做好取舍。技术部分参考 docs/development.md

- #9【ok】为鼓励玩家用更快的时间完成关卡，在关卡合适位置添加一个不会太显眼的计时 UI，过关需时展示玩家耗时（要区分实际耗时和额外耗时）。物体（如热源冷源）数量越多会给予一个合适的“惩罚性“耗时。目标鼓励玩家追求使用更少的道具和更快的通关。当然重新开始是要重新计时的（如有惩罚性计时要保留）。

- #8【ok】在 #7 的基础之上，实现第二关。注意第二关不是已经设定好的关卡，而是由你做取舍，有想表达的东西（idea 可以参考 docs），做一个不杂乱、较简洁的关卡。不做任何限制，但是建议利用好现有架构基础。做关卡设计时不受任何沉没成本干扰（如之前的文案或者文档）。

- #7【ok】本次首要目标是大幅度提升游戏运行性能、流畅度。当前发现 ios Safari 可能由于 js 引擎或 jit 限制有更差的性能（目前发现可能是物理模拟数值计算性能较差，是否考虑大胆采用 webgl 进行加速）。桌面端、Android Webview 性能好很多。考虑不引入更多复杂度，整洁的优化整体性能。例如通过 js 代码生成、wasm（可以用 moonbit 语言）、兼容性更好的 webgl （重点看看）做渲染和物理数值运算的加速。如需做性能分析，推荐采用 chrome dev tools 或者 Safari 的 WRDP 或其他性能分析方案。做好取舍，再大胆下狠手。

- #6【ok】运动的物体，在空气中运动，会产生摩擦，会发出真实感的声音。用物理的方法和数学算法去产生游戏音效

- #5【ok】第一关，纸飞机初始状态是在画布外且距离地面有一定高度，有一个合适的初速度表达出其速度感，如果不进行任何操作，飞机会很快落地而无法抵达目标。我的目标是这样能给玩家在一开始就构建出物理感、空气与纸飞机运动的真实感。

- #4【ok】风的粒子、纸飞机等移动物体增加拖尾特效，拖尾效果不要显眼，而是“淡淡的”，路程更长的拖尾轨迹会渐渐消失（非单纯随时间消失，而是随着路程的增加旧的轨迹淡出）。对于空气或风，我希望营造出真实的风的线条感（例如场的可视化）。对于可移动物体，我希望拖尾轨迹能够让用户直观感受到运动的历史（由于按照路程淡出轨迹，所以如果物体停止运动，对应可视的轨迹不会消失）。

- #3【ok】UI、画布和游戏内容更好的适配竖屏，例如手机。保持横屏的体验

## 2026/08/03

- #2【ok】完整参考 docs 中材料，将 docs/idea2.md 第一个关卡完整实现。在此之前一定要搭建一个好的项目地基。先深度规划，做好取舍，再进行执行。

- #1【ok】老大已经创建项目基础资料文档 docs/、目录 sfgame-web/ 为 vite + lit 初始结构。
