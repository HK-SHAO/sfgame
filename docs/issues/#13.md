# #13 开发者页面（解法/bench 入口迁移）+ 存储管理页 + 关于

- **状态**：已实现、自测通过（`bun run check` 全绿 119 项）
- **日期**：2026-08-05

## 1. 开发者页面（主菜单底部入口）

- 主菜单底部新增「开发者页面」按钮（滑块图标胶囊，风格与旧解法入口一致）；**解法参考入口从主菜单移入开发者页面**
- 开发者页面页（`ui/dev-menu.ts`，`sf-dev-menu` 组件，与解法页同款页面骨架/卡片/返回键架构）：
  - **性能诊断**：进入 `bench.html` 基准页（原地跳转）；bench 页顶部补「← 返回游戏」链接（改版前无任何返回途径）
  - **解法参考**：原解法页（?v=solutions）
  - **存储管理**：见下
  - **关于**：作者 烧风 (HK-SHAO)、版本号 v{version}——**版本号 import 自 `package.json`**（`resolveJsonModule` 已开），发版改版本号即跟随
- 页面视图统一走 URL（`?v=dev`、`?v=storage`，与 `?v=solutions` 同协议，C6 刷新不丢）：`game/state.ts` 的 `AppView` 枚举与 codec 白名单扩充两个取值；返回箭头 = `history.back()`（URL 驱动回退，直达链接无历史时兜底回标题页）

## 2. 存储管理页（`ui/storage-view.ts`，`sf-storage`）

- 展示本 app 在 localStorage 的全部持久化项（前缀 `sfgame.`/`zaofeng.`，键字符串单源：`progress.ts` 导出 `STORAGE_KEY`、`sfx.ts` 导出 `MUTED_KEY`）：键名、字节数（B/KB）、内容摘要（进度 = 逐关成绩条数，静音 = 开/关）
- **只读展示、不提供编辑**；每项「删除」+ 底部「清空全部数据」，**两击确认**（首次点击武装为"确认删除"，3 秒未确认自动解除）
- 删除后整页重载（URL 带 v=storage 重载后仍回本页）——进度/音效等模块单例持有内存副本，重载是最简单可靠的一致性保证；空态显示「暂无本地持久化数据」
- 实测全流程：造数据 → 列表/摘要/大小正确 → 两击删除 → 重载后清空、空态显示；console 无错误

## 备注

- bench 页面本身未重构（独立诊断工具页，调色板与 app 一致；本轮仅补返回入口），如需并入 app 壳内可再议
- 已同步 `AGENTS.md`？否——本工单无玩法/架构不变量变化；新增页面组件均落在 `tsconfig.app.json` 的 `src/` include 内

## 修订（老大反馈 2026-08-05）

1. **移除 bench 全部相关代码**：`bench.html`、`src/dev/bench.ts`/`bench-core.ts`、`scripts/bench.ts`/`bench-browser.ts` 整体删除；开发者页面里的「性能诊断」行移除；pitfalls I1/I4/I7 与 #7.md 的 bench 引用同步标注移除（历史基准数字保留）
2. **解法参考页返回一致性**：返回按钮从"回主页"改为 `history.back()`（与关卡页箭头同语义，URL 驱动回退；直达链接无历史时兜底回主页）
3. **存储管理合计移到底部**：「共 N 项 · X」从标题副文案移到页面底部（清空按钮下方），与页面 footer 位置一致
4. **存储条目可展开**：每项新增「展开/收起」显示完整原始值（等宽小字、可滚动），摘要保留在折叠态
5. **开发者入口仅 dev 模式可见**：主菜单「开发者页面」按钮只在 `?dev=1` 时显示
6. **开发者页面内 dev 开关**：菜单首行「开发者模式」开关（胶囊拨杆，与整体药丸风格一致），切换即改 `?dev=` 状态——开启后 perf 叠加层/高速档/空格暂停/主菜单入口全部生效
