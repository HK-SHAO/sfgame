# #17 目录整理：非 UI 逻辑移出 src/ui/

- **状态**：已完成，`bun run check` 全绿
- **日期**：2026-08-06

## 调整（`git mv` 保留历史）

- **`src/dev/`（新）— ?dev=1 开发者工具，与玩家界面分离**：`devtools.ts`（组装）、`dev-panel.ts`（面板外壳）、`perf.ts`（性能块）、`level-editor.ts`（YAML 编辑器）、`dev-menu.ts`（开发者页面）
- **`src/render/`（新）— 渲染层不进 ui/**：`render.ts`（场景→顶点批）、`gl.ts`（WebGL 薄层）；`batch.ts` 自 `core/` 移入（渲染专用顶点批，纯计算仍可无头测试）
- **`src/ui/` 收敛为玩家界面**：`app.ts`、`sf-game.ts`、`controller.ts`、`input.ts`、`icons.ts`、`status-bar.ts`、`solutions-view.ts`、`storage-view.ts`
- 全部导入路径同步修正；tsconfig `include: ["src"]` 无需改动

## 文档同步

- AGENTS.md 架构边界重写：DOM 边界改为 `ui/` + `dev/`；`core/game/sim` 保持无头；tests 可 import `render/batch`
- `skills/pitfalls/SKILL.md` 两处旧路径（D7、I6 调试条目）更新
