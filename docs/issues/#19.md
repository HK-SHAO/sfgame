# #19 删几乎所有注释 + 只保留 ~20% 必要测试

- **状态**：已完成，`bun run check` 全绿（36 项测试 5.9s）
- **日期**：2026-08-06

## 注释（src + scripts 全量）

- 注释行 **~868 → 137**（保留率 ≈16%，低于 20% 目标）；代码总行 8003 → 7274
- 删除：全部复述型 JSDoc/行注释、CSS 分节横幅与样式说明、自明的调参注释
- 保留（一律压成一行）：物理不变量（悬停阈值 gravity/dragK、贴地边界层耦合、MacCormack 平流/红黑 GS/warm-start 压强）、平台坑（iOS MSAA、canvas resize 重置 WebGL 缓冲、shadow DOM box-sizing、composedPath 重定向、bfcache pageshow）、协议细节（URL 1 位小数零百分号、lv=0 dev 槽、进度载荷版本容错）、调参取舍（粒子/dpr 降级顺序、60fps 预算留余量）、音频物理映射（1/f 粉红噪声、响度 ∝ v²）
- 顺手修正两处过时文案（空格键暂停已随 #15 八修移除）：perf 行「已暂停（空格恢复）」→「已暂停」；开发者页 dev 开关说明 → 「开发面板/高速档/不限量道具」

## 测试（与 #18 合并执行）

139 项 → **36 项（26%）**，每个保留项的取舍见 #18。执行时间 44.2s → 5.9s。

## 验证

- `bun run check` 全绿；产物 234.09 kB（gzip 72.18 kB，较前 −2.1 kB gzip）
- diff 逐行核对：除 #16/#17 的功能改动外，src/scripts 仅注释变化（含 5 处行尾注释剥离，代码部分逐字不变）
