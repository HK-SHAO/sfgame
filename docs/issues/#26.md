# #26 dev 面板：perf 去状态格 + author/version

状态：已执行完毕（2026-08-07），待老大验收。

## 改动

1. `app/dev/perf.ts`：删除「状态（运行中/已暂停）」单元格与 `paused` state；在 dpr 右侧追加两格 `author` / `version`（读 package.json，随版本变更）
2. 清理死链：`DevTools.syncPause()` 及 controller 中两处调用一并删除（perf 不再需要暂停态）
3. #29 跟进：author 改为 package.json 对象后，perf 显示 `author.name`

`bun run check` 全绿。
