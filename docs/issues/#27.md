# #27 新原子能力 + 第二关组（5 关）+ 主页选项卡

状态：已执行完毕（2026-08-07），待老大验收。按指示：**解法与玩法验证交给玩家实测**，本次只做设计 + 功能，参考解为手摆最佳尝试（winTime 不再由测试守护）。

## 新原子（协议 v1 增量，向后兼容；schema 不变）

```yaml
group: 2                          # 关卡组（主页选项卡），缺省 1
fixed:                            # 关卡自带、不可移除/撤销、不占预算
  - { x: 30, y: 40, kind: hot }
fans:                             # 风扇：定向气流；swing/period 可选（摇头）
  - { x: 24, y: 26, dir: 0, power: 5, swing: 0.4, period: 8 }
```

- **内核**（`assembly/core.ts` 新增 `addForce`，动量圆域注入 + 压强投影塑成射流；引擎/fluid 门面逐层补导出）
- 解耦：摇头数学在 game 层纯函数 `fanDirection`（模拟与渲染共用）；求解器零改动（fixed/fans 是关卡静态上下文）
- 校验：`level-format.ts` 覆盖新字段（范围/枚举/摆动范围/数量 ≤8）
- 渲染：固定源**形象化**（篝火/空调，见 #29）；风扇（机身 + 旋转叶片 + 朝向箭头）；示踪粒子自动勾勒射流

## 第二关组「焚风」（L6-10，教学弧线：引入→发展→转折→收束）

| 关 | 名称 | 教什么 | 结构 |
|---|---|---|---|
| 6 | 营火 | 固定热源 | 双崖接力：篝火在高原1，玩家热源崖底托升 + 接力翻第二道崖 |
| 7 | 冰窖 | 固定冷源 | 冷源压顶：热托 + 冷压上目标丘（滑行入圈，联动 #25） |
| 8 | 鼓风 | 风扇（定向） | 热源把飞机托进射流，借风渡沟壑落右岸 |
| 9 | 钟摆 | 摇头风扇 | 摆幅 0.4 rad 永不上扬，带速滑入射流随摆风渡壑 |
| 10 | 焚风 | 综合 | 火 × 风扇 = 热风走廊翻崖（涌现），冷源压顶进终点，两站旅程 |

- 主页选项卡（组 1「长风」/组 2「焚风」，常显、组内线性解锁）；`playNext` 限同组；解法参考页按组分节
- 设计红线（挂机不穿圈）已按新物理适配：环境风弱 + 目标放墙/崖后；L6-8 挂机 150s 实测通过（L9/L10 结构同理，未跑验证——解法验证交给玩家）

## 参考资料沉淀

- `skills/level-design/SKILL.md`：原子表 + 组合语法 + 风扇玩法实测经验（射流射程/回流混沌/固定源关卡红线）+ 设计工作流更新
- 行业调研（涌现式机制/关卡教学/关卡组惯例/风扇机制），要点已并入 SKILL

## 验证

`bun run check` 全绿（58 测试）；solutions.test.ts 按指示只查「有解 + 不超预算」，不再守护 winTime。
