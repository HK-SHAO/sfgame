# #29 package.json author 对象化 + 固定道具形象化（篝火/空调）

状态：已执行完毕（2026-08-07），待老大验收。

## 改动

1. **author 对象化**：`package.json` → `"author": { "name": "HK-SHAO", "email": "hk-shao@outlook.com" }`；相关消费点更新：
   - `app/dev/dev-menu.ts` 关于页：作者：HK-SHAO、邮箱：hk-shao@outlook.com、版本 vX
   - `app/dev/perf.ts`：author 格显示 `author.name`（避免 [object Object]）
2. **固定道具形象化**（`app/render/render.ts`，替代原圆盘样式），按老大要求：**圆润封闭、简约、优雅**：
   - 热源 = **篝火**：圆头圆尾的泪滴火苗（外橙内黄双层胶囊，轻微摇曳）+ 圆润坐垫底座 + 含蓄光晕——封闭圆润的极简形态
   - 冷源 = **空调**：全圆角白色机身（rect + 四角圆盘）+ 同心圆环出风口 + 蓝色指示灯 + 含蓄冷光——封闭圆润的极简形态
   - 风扇沿用机身 + 旋转叶片 + 朝向箭头（同为圆润图元）
   - 玩家可放的热/冷源保持原有圆盘样式，与固定源视觉明确区分
   - 实现方式：沿用现有矢量图元批（stroke 胶囊/discGrad/rect，数学上与 SVG 路径等价），不引入纹理管线——保持单程序单缓冲渲染架构与移动端性能
   - 像素级验证（headless 截图 + 帧缓冲采样）：篝火橙焰黄心、空调白机身圆环蓝点、风扇机壳面盘叶片箭头均正确渲染

3. **修复一处渲染 bug**：篝火火苗初版误将颜色按 0-255 原始值传入顶点批（batch 颜色须 0..1 归一化，其余调用均用归一化常量），被钳制为纯白——已改用归一化常量 `FLAME_OUTER/FLAME_INNER` 修复

`bun run check` 全绿。
