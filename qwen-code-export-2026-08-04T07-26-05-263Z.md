# Chat Session Export

- **Session ID**: `d04187b2-4aa6-4c41-bf3e-12d3f39633fa`
- **Start Time**: 2026-08-04T07:04:40.208Z
- **Exported**: 2026-08-04T07:26:05.304Z

- **Working Directory**: `/Users/sf/dev/scnucj2026`
- **Git Repository**: HK-SHAO/sfgame
- **Git Branch**: `main`

- **Model**: qwen3.8-max
- **Prompt Count**: 3

- **Total Tokens**: 1521974
- **Context Window Size**: 1000000
- **Context Usage**: 10.9%

- **Files Written**: 5
- **Lines Added**: 536
- **Lines Removed**: 130

<details>
<summary><strong>Unique Files Referenced (5)</strong></summary>

- `/Users/sf/dev/scnucj2026/sfgame-web/scripts/bench.ts`
- `/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.node.json`
- `/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench-core.ts`
- `/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench.ts`
- `/Users/sf/dev/scnucj2026/sfgame-web/bench.html`
</details>

---

## User

@docs/issues.md 重点仅关注 #7 深思熟虑优化方案，大胆执行。第一性原理是iPhone用户也有好的体验，不降低原有任何好的体验。


> **Referenced Files:**

```

Content from docs/issues.md:

# 用户体验问题清单

老大（即用户）会定期把问题新增在此文档中。

针对每一个问题，研发人员（或 AI Agent 等）在阅读后需要着手核实、尝试复现、深度研究和思考。

注意老大的问题或建议不一定完全属实、合适或是最佳决策，要以第一性原理、产品体验和工程可靠且优雅为要义，批判性的研究、执行和回复。

每一个问题的状态要及时更新，例如是否已阅读，是否在执行中，执行结果或简要报告要体现。报告一定要挑重点，省略低信息量（即业界高频出现的模式）的细节，不能省略 bug 的修复或缺陷的改进。

老大负责最终给每个问题结束工单（用【ok】表示，只有老大才能结束工单，研发人员/Agent 没有权限），在此前研发绝不能忽视。

此文件仅供老大更改。如需汇报状态编辑 docs/issues 目录，文件名为 #N.md，参考业界的 issue 相关经验进行编辑。

## 2026/08/04

- #8【ok】在 #7 的基础之上，实现第二关。注意第二关不是已经设定好的关卡，而是由你做取舍，有想表达的东西（idea 可以参考 docs），做一个不杂乱、较简洁的关卡。不做任何限制，但是建议利用好现有架构基础。做关卡设计时不受任何沉没成本干扰（如之前的文案或者文档）。

- #7【部分ok，需要再深思、查资料和实践】本次首要目标是大幅度提升游戏运行性能、流畅度。当前发现 ios Safari 可能由于 js 引擎或 jit 限制有更差的性能（目前发现可能是物理模拟数值计算性能较差，是否考虑大胆采用 webgl 进行加速）。桌面端、Android Webview 性能好很多。考虑不引入更多复杂度，整洁的优化整体性能。例如通过 js 代码生成、wasm（可以用 moonbit 语言）、兼容性更好的 webgl （重点看看）做渲染和物理数值运算的加速。如需做性能分析，推荐采用 chrome dev tools 或者 Safari 的 WRDP 或其他性能分析方案。做好取舍，再大胆下狠手。

- #6【ok】运动的物体，在空气中运动，会产生摩擦，会发出真实感的声音。用物理的方法和数学算法去产生游戏音效

- #5【ok】第一关，纸飞机初始状态是在画布外且距离地面有一定高度，有一个合适的初速度表达出其速度感，如果不进行任何操作，飞机会很快落地而无法抵达目标。我的目标是这样能给玩家在一开始就构建出物理感、空气与纸飞机运动的真实感。

- #4【ok】风的粒子、纸飞机等移动物体增加拖尾特效，拖尾效果不要显眼，而是“淡淡的”，路程更长的拖尾轨迹会渐渐消失（非单纯随时间消失，而是随着路程的增加旧的轨迹淡出）。对于空气或风，我希望营造出真实的风的线条感（例如场的可视化）。对于可移动物体，我希望拖尾轨迹能够让用户直观感受到运动的历史（由于按照路程淡出轨迹，所以如果物体停止运动，对应可视的轨迹不会消失）。

- #3【ok】UI、画布和游戏内容更好的适配竖屏，例如手机。保持横屏的体验

## 2026/08/03

- #2【ok】完整参考 docs 中材料，将 docs/idea2.md 第一个关卡完整实现。在此之前一定要搭建一个好的项目地基。先深度规划，做好取舍，再进行执行。

- #1【ok】老大已经创建项目基础资料文档 docs/、目录 sfgame-web/ 为 vite + lit 初始结构。


```



## Assistant

First, let's start by understanding the current state. The user wants me to focus on issue #7: performance optimization, especially for iOS Safari. According to the issue, #7 is in a "partially OK" state, and further deep thinking, investigation, and practice are required. The previous work (according to memory and git history, commit 5661333 "Fix WebGL context restore...") has already introduced the WebGL renderer (#7 mentions the WebGL refactor).

First, let me read the memory file, docs/issues/#7.md, and check the current state of the codebase.

Things to check:
1. Memory file: project/sfgame-review-fixes.md
2. docs/issues/#7.md (the issue report file)
3. Current state of the code

Let's start exploring.


### Tool: ReadFile: /Users/.../-Users-sf-dev-scnucj2026/memory/project/sfgame-review-fixes.md
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/.qwen/projects/-Users-sf-dev-scnucj2026/memory/project/sfgame-review-fixes.md"
}
```

**Affected Files:**
- `/Users/sf/.qwen/projects/-Users-sf-dev-scnucj2026/memory/project/sfgame-review-fixes.md`

**Output:**
```markdown
---
name: sfgame-web 工单进度与遗留（2026-08-04）
description: 造风游戏 docs/issues 工单实现状态、#7 WebGL 重构与 #8 第二关要点、跨会话遗留项
type: project
---

截至 2026-08-04：#2–#6 已实现待老大验收；**#7（性能）与 #8（第二关）已由 Qwen 实现**，`bun run check` 全绿（typecheck + 67 单测 + build），报告在 docs/issues/#7.md、#8.md。

#7 要点（渲染 GPU 化）：
- 诊断：iOS Canvas 2D 为 CPU 栅格化，旧渲染每帧 ~9600 段 Path2D 描边是瓶颈；流体求解非瓶颈
- 取舍：WebGL1（iOS 8+/全 WebView GPU 加速）；**不做 WASM/MoonBit**（7.5k 格 JS 已够快，工具链复杂度不抵收益）、不做 WebGPU（覆盖不足）
- 新模块：`core/batch.ts` 纯计算顶点批（可无头测试）+ `ui/gl.ts` WebGL 薄层（上下文丢失恢复）；render.ts 重写，公共 API 不变，控制器/UI 零改动
- 逐顶点颜色取代分桶（视觉更细腻）；线宽几何化；径向渐变=扇形插值；删除 bg 离屏缓存
- **修复隐藏 bug**：旧 drawPlane 高空时连飞机本体一起 return（凭空消失）
- 降级档位重调：移动端粒子 128→240、轨迹 12→24、dpr 上限 2（旧限制来自 CPU 描边）

#8 要点（第二关「降落」· 下沉气流）：
- 与 L1「上升气流」对偶：教冷源/下沉。极简谷地，飞机高空入场，不操作掠过目标撞右壁
- 曾实现「翻山」概念后废弃：轻飞机在强热柱中轨迹混沌、摆放敏感、84s 混乱通关——违背简洁，沉没成本不留
- 定稿：spawn(-5,8,vx16)、ambient 2.6、goal(62,r7.5)、预算热2冷3；双解注册（19.6s/21.7s），±2 扰动 23/25 通关
- 解耦：`LevelDef.win {title,text}` 取代 app.ts 硬编码文案

**Why:** 老大在 issues.md 以工单驱动迭代，报告写在 docs/issues/#N.md（issues.md 本身只有老大能改）。

**How to apply:**
- 测试策略（见 project/testing-policy.md）：只做小模块单元/白盒测试，不做整局 bot/E2E，体验由玩家反馈
- 渲染层改动以纯计算单测（batch）覆盖；视觉/实机帧率由老大验收
- 遗留：AGENTS.md「Lit 3 + Canvas 2D」描述已过时（渲染已 WebGL），该文件受保护，已提示老大确认后更新；游戏名「造风」暂定；3–5 关占位

```


### Tool: ReadFile: docs/issues/#7.md
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/docs/issues/#7.md"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/docs/issues/#7.md`

**Output:**
```markdown
# #7 性能与流畅度大幅提升（渲染 GPU 化）

- **状态**：已实现、自测通过（`bun run check` 67 项全绿）；iOS 实机体验待老大验收
- **日期**：2026-08-04

## 诊断：瓶颈在渲染栅格化，不在物理

- iOS Safari 的 Canvas 2D 为 **CPU 栅格化**（WebKit）。旧渲染每帧 ~400 粒子 × 24 轨迹点 ≈ **9600 段描边**，即使分桶也要 20+ 次 Path2D stroke + 渐变对象/精灵烘焙，CPU 成本随 段数×dpr 非线性增长——正是 iOS 明显差于桌面/Android 的原因。
- 流体求解器（~7.5k 格 × ~39 趟/步）在桌面 <2ms，非瓶颈；粒子/刚体步进同为次要项。

## 取舍：WebGL1，不做 WASM/WebGPU

- **WebGL 1.0**：iOS 8+ / 全部 Android WebView 可用且 GPU（Metal）加速——兼容性最优解，正中"iOS 最差"的病灶。
- **WASM（MoonBit）物理加速：不做**。网格仅 7.5k 格，JS typed array 已足够快；wasm 引入工具链、异步装载、数据搬运的复杂度，收益不抵——违背"不引入更多复杂度"。若未来物理规模上一个量级再议。
- **WebGPU：不做**。iOS 支持太新、WebView 覆盖不足。

## 改动

**架构（公共 API 零变化，控制器/UI 无感）**

- `src/core/batch.ts`（新）：纯计算顶点批构建器——线段加宽、椭圆扇、径向渐变盘、虚线圆等全部展开为彩色三角形。无 DOM，可无头测试（tests 分层不变量保持：只 import core/game/sim）。
- `src/ui/gl.ts`（新）：WebGL1 薄层——单程序（位置+逐顶点色）、单动态 VBO、每帧一次 `drawArrays`；含上下文丢失/恢复处理（iOS 内存压力回收）。
- `src/ui/render.ts`（重写）：场景绘制全部输出到顶点批。删除 bg 离屏缓存、Path2D 分桶、渐变精灵 Map 等 Canvas 时代补丁。

**随重构获得的改进（非纯搬运）**

- **逐顶点颜色取代分桶近似**：旧实现受 strokeStyle 状态机所迫，透明度/温度离散分桶（4×5 档）；现在每段带精确 RGBA，一次提交——视觉更细腻且代码更简。
- **修复隐藏 bug**：旧 `drawPlane` 在高空（影子淡出高度以上）**连飞机本体一起 return，飞机凭空消失**；重写后影子淡出与本体绘制分离（详见 pitfalls D8）。
- 线宽几何化（GL lineWidth 多平台恒 1）；径向渐变用扇形逐顶点插值，免每帧 createRadialGradient。

**物理与调参（数值位级不变）**

- 热路径 `Math.hypot` → 内联 sqrt、投影迭代数提升为局部量——`tests/solutions.test.ts` 的通关时刻守护（±2s 实测全中）证明数值未漂移。
- 渲染 GPU 化后重调降级档位：移动端初始粒子档 128→240、轨迹点 12→24（旧限制来自 CPU 描边，现已移除），dpr 上限放宽至 2；自适应降级阶梯机制（EMA+慢帧计数）原样保留。

## 验证

- `bun run check`：typecheck + 67 单测 + build 全绿；新增 14 项顶点批白盒测试。
- 按项目约定不做视觉 E2E；渲染层变更已尽量以纯计算单测覆盖，余下观感与 iOS 实机帧率请老大验收。

```


## Assistant

Alright, let's check the current state of the repository. In `git status`, `docs/issues.md` has been modified (the boss updated the status of #7 to "partially OK, need to think more deeply, look up materials, and practice"). In other words, the boss is asking for further deep optimization of #7.

The user says: "Focus only on #7, think deeply and come up with an optimization plan, then execute boldly. The first principle is that iPhone users should also be able to have a good experience, and none of the existing good experiences should be degraded."

So what's needed is:
1. Deeply understand the current implementation (post-WebGL-renderer refactor)
2. Find where the remaining bottlenecks on iOS are
3. Think carefully and boldly execute optimizations

Main areas to consider regarding iOS performance:
- The physics simulation (fluid solver) is in JS — could this be the bottleneck on iOS? The previous report stated that the fluid solver is under 2ms on desktop, but iOS's JS engine (JavaScriptCore/Safari's JIT) might be slow. In fact, in Safari JIT is enabled, but inside WebViews (WKWebView) the JIT is disabled, so JS performance can drop dramatically (up to 10x). The issue says "ios Safari is likely to have worse performance due to limitations of the js engine or jit restrictions" — wait, it says iOS Safari. Safari on iOS now also uses V8 (on iOS 17+? No, actually Safari has been using JSC's JIT for a long time; did Safari switch to V8 in iOS 17.4 or later? No, that's wrong. Actually on iOS, Safari used to be stuck with the JavaScriptCore interpreter (no JIT) up until iOS 17.4, and from iOS 17.4 onwards JIT is enabled for Safari. Other browsers on iOS are also now permitted to use their own engines). WKWebView still has no JIT unless entitlements are granted.

So on iOS, physics in JS can be slow, especially in WKWebView (Android WebView was mentioned as being fast — the Android WebView uses V8 with JIT).

Options for #7 round 2:
1. **Move the fluid solver into a WebGL compute pass?** WebGL1 has no compute shaders. You can do GPGPU with textures and floating-point textures (OES_texture_float / half-float). This is complex.
2. **WASM for the fluid solver** — WASM runs near-natively in iOS Safari, and importantly, WASM is permitted even in WKWebView without JIT. MoonBit was suggested by the boss. The previous decision was "don't do WASM," but the boss is now saying "partially OK, need to think more deeply, look up materials, and practice," and the issue explicitly mentions "e.g. acceleration of rendering and physics numerical computation via js code generation, wasm (in the moonbit language), webgl with better compatibility (focus on looking into this)." The boss is asking us to think more deeply, look up materials, and practice. The fact that #7 is only partially OK means the WebGL render refactor was accepted but further work is needed.

3. **Optimize the JS solver itself** — maybe there's room: reducing iterations, typed array optimizations, avoiding allocations, etc.

4. **OffscreenCanvas + Worker** — move physics to a worker so the main thread doesn't get blocked. iOS Safari has supported OffscreenCanvas since 16.4; do workers work in WKWebView? Workers work inside WKWebView, yes.

Let's think about first principles:
- iPhone users should have a good experience too
- Don't degrade any of the existing good experiences
- Don't add more complexity, optimize cleanly

Plan:
1. First, thoroughly audit the current state: read the sim code (fluid solver), render, controller, core/loop, and check current performance-related code paths.
2. Profile: I can run benchmarks on this Mac (desktop). I can't directly test on an iPhone, but I can use headless benchmarks, and I can also emulate by running the solver in a JS engine without JIT? It's hard to simulate iOS precisely. But I can measure and reason.

Important: AGENTS.md says "game experience verification depends on user/player feedback; unless necessary or requested by the user, R&D must not run Computer Use or E2E script verification experiments. This is to reduce token waste." So no E2E. However, micro-benchmarks (unit-level performance tests) are fine and encouraged ("performance analysis, Chrome DevTools or Safari WRDP recommended").

Let's explore the codebase first. Read the main files:
- sfgame-web/src/sim/ (fluid solver)
- sfgame-web/src/ui/render.ts, gl.ts
- sfgame-web/src/core/batch.ts, loop
- docs/development.md
- skills/pitfalls/SKILL.md (sections D, G, H)

Then decide the optimization plan.

Possible optimizations:
1. **Fluid solver in WASM via MoonBit** — the boss explicitly mentioned it. But there's toolchain complexity. MoonBit compiles to wasm, and the moonc toolchain exists via npm (`moon` CLI). Can it be installed with bun? Building MoonBit requires the `moon` build tool (npm package `moonbit`). This adds a build-time dependency. Alternative: hand-written WAT? Too painful for the full solver. Or: TS → WASM via AssemblyScript? Also a toolchain.

   Actually, is the fluid solver really the bottleneck on iOS? The grid is 7.5k cells × about 39 passes per step. Per pass: about 7.5k × a handful of flops. 39 passes ≈ 300k cell updates ≈ 3–6M flops per frame (2 steps?). JS with JIT can do 100M+ simple ops per second; on desktop that's under 2ms. Without JIT (WKWebView), 10–50x slower → 20–100ms — game over. But Safari with JIT on iOS (17.4+) should be fine. The boss said "iOS Safari may have worse performance due to js engine or jit restrictions (currently we've found that the performance of physics simulation numerical computation may be poor…)." So they observed that physics is poor on iOS Safari. Hmm, but that was the original diagnosis, and it may have been speculation. After the WebGL refactor, the boss tested and marked #7 as "partially OK, needs more deep thought," meaning improvements were seen but not enough — further deep optimization is needed.

2. **Reduce the solver cost algorithmically**:
   - Fewer pressure iterations (adaptive)
   - Half-resolution grid for pressure?
   - Red-black Gauss-Seidel doesn't converge faster per iteration than Jacobi… they currently do about 39 passes.
   - Maybe use typed arrays in a cache-friendly order, avoid `Math.max/min` chains, etc.

3. **Substep / fixed timestep**: check the loop.

4. **Particle advection cost**: 240 particles × RK2 sampling — sampling the velocity field with bilinear interpolation — no problem.

5. **Render cost**: even with WebGL, if dpr 2 on iPhone makes the canvas huge — fill rate is the issue. GPU fill rate on iPhone is good. The batch builds many vertices on the CPU per frame — trail vertices: 240 particles × 24 points × 2 triangles… actually trails are: per particle, a polyline of up to 24 points → quads. So 240×23×6 vertices ≈ 33k vertices + particles, and the plane trail, etc. Building these per frame on the CPU in JS typed arrays is allocation-heavy — potential garbage pressure. Maybe pre-allocate Float32Arrays and write in-place to optimize. Need to check the current implementation.

Let's look at the code.

Also check the git log since the #7 implementation to see what the other AI tool (opencode+deepseek) has done — the code may have changed! The memory says "the code can change between conversations without the user mentioning it — always re-verify the current state of the repo." Recent commits: ec08d46 text color fix, 2f97cf0 fix for heat sources disappearing after restart, 36f9e16 polyline stroke with miter joins for MeshBatch, e865eb4 game speed control, 5661333 WebGL context restoration fix. So work has continued after the #7/#8 session.

Let's thoroughly explore the current source tree.


## Assistant

收到。#7 被老大标记为「部分 ok，需再深思、查资料和实践」——说明 WebGL 渲染方向对了，但 iPhone 上的性能/体验还有硬骨头。我先重新核实代码现状（其他工具可能已改过代码），再做深度诊断。


### Tool: Shell: cd /Users/sf/dev/scnucj2026 &amp;&amp; git log --oneline -15 &amp;&amp; echo --- &amp;&amp; git status --short &amp;&amp; echo --- &amp;&amp; ls sfgame-web/src sfgame-web/src/sim sfgame-web/src/core sfgame-web/src/ui sfgame-web/src/game sfgame-web/tests (核实 git 现状与源码布局)
**Status**: completed

**Input:**
```json
{
  "command": "cd /Users/sf/dev/scnucj2026 && git log --oneline -15 && echo --- && git status --short && echo --- && ls sfgame-web/src sfgame-web/src/sim sfgame-web/src/core sfgame-web/src/ui sfgame-web/src/game sfgame-web/tests",
  "description": "核实 git 现状与源码布局"
}
```

**Output:**
````
Command: cd /Users/sf/dev/scnucj2026 && git log --oneline -15 && echo --- && git status --short && echo --- && ls sfgame-web/src sfgame-web/src/sim sfgame-web/src/core sfgame-web/src/ui sfgame-web/src/game sfgame-web/tests
Directory: (root)
Output: ec08d46 (HEAD -> main, origin/main) Fix text color and alignment consistency across platforms
2f97da0 Fix source invisibility after restart by resetting born times
36f9e16 Add miter-joined polyline stroke to MeshBatch
e865eb4 Add game speed control with shortened URL state keys
5661333 Fix WebGL context restore, keep sources on UI restart
8d0f690 Render scene via WebGL vertex batches
2b33500 整理 issues 文档并更新仓库布局说明
fd523e5 Improve plane shadow with parallel light projection
534ba23 Enhance sound design with stereo panning and UI feedback
986425c Move AGENTS.md to repo root and broaden audio unlock
12f7f43 ``` Add docs and skills for game design and animation
7f69795 Refactor constants and simplify comments
ce92c69 Color tracer lines by temperature and tune performance
90326c0 解法参考页：删 dev 模式与 agent，改为 URL 解链接；新增 view URL 状态持久化页面
b4d473b 修复：开新局清理 URL sources、切后台回前台恢复音频、HUD/提示改用 activeLevel
---
 M docs/issues.md
---
sfgame-web/src:
core            game            index.css       main.ts         sim             styles.css      ui

sfgame-web/src/core:
batch.ts        loop.ts         sfx.ts          url-state.ts

sfgame-web/src/game:
levels.ts       simulation.ts   solutions.ts    state.ts        types.ts

sfgame-web/src/sim:
bodies.ts       fluid.ts        particles.ts    trail.ts        types.ts

sfgame-web/src/ui:
app.ts                  gl.ts                   input.ts                sf-game.ts
controller.ts           icons.ts                render.ts               solutions-view.ts

sfgame-web/tests:
batch.test.ts           fluid.test.ts           level2.test.ts          solutions.test.ts       trail.test.ts
bodies.test.ts          level1.test.ts          loop.test.ts            state.test.ts           url-state.test.ts
Error: (none)
Exit Code: 0
Signal: 0
Process Group PGID: 95414
````


### Tool: ReadFile: docs/development.md
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/docs/development.md"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/docs/development.md`

**Output:**
```markdown
## 实现要点

美术：极简矢量风格（做好兼容性、可迁移性，目标是未来可以随时把素材替换掉），物体用纯色几何图形，善用粒子系统（小点）表示和可视化气流。

引擎：利用好Web、浏览器特性，能够很好的兼容Chrome、IOS Safari，物理用简单的2D刚体+预设力场），善用流体模拟（基础力学、拉格朗日视角、欧拉视角之间要耦合好，善用先进且成熟的算法）。

为什么要做好拉格朗日视角、欧拉视角之间要耦合好：这个拉格朗日视角，相当于模拟很多小球在碰撞，所以呢，会把那个流体说把什么空气呀水呀，把它想象成有一堆小小的小球很纯粹的小球，他们在疯狂的互相碰撞。欧拉是欧拉视角就是不关注这些玩意儿，原来是由什么分子原子这些小球组成，而是有一个很神奇的一块一块的空间组成的，这些空间有很多概念，比如说它有温度的概念，它有压强的概念，然后还有整体的那种力的概念等等。拉格朗日视角模拟这些小球之间的运动，其实呢，它运用在钢铁模拟上就很方便，比如说你可以把手机看作一个盒子，然后它可以跟桌子啊什么的地板碰撞，然后纸飞机也是也可以几乎看作一个钢体，很多时候这些简单的一些模型，一些物体，它可以看作是拉格朗日视角上的些运动的模拟而欧拉视角就很擅长去看待流体，比如说空气的运动，比如说这个液体水的运动。

 ## 实践建议
- 如需使用 html 圆角（`border-radius`）均需搭配 css `corner-shape: squircle;` 以获得丝滑曲率的 iOS 风格圆角
- https://koboyo.com/icons 可以使用 koboyo mcp 获取很多矢量图形作为参考素材，也可进行二创
- 音效可以采用 sfxr / jsfx 例如 https://github.com/chr15m/jsfxr

## 开发守则

- 对于不确定的、疑似陈旧的、遇到困难的要及时联网搜索最新的、可靠的资料作为参考
- 有疑惑、不懂或缺少信息时，优先通过网络（如搜索引擎、Github、官方 docs 等）结合可靠性、时效性获取文档
- 做好平台兼容性
- 优先采用 bun 作为包管理器和运行时，而非 npm/node。优先采用 TS/JS
- 优先采用 vite + lit 作为 web 开发的基础。常用功能可以自己做成模块或库（可以放到 los-app/src/libs）
- 优先采用 web 自带的能力和规范，减少引入依赖项。可以联网参考某些轮子的实现，但对于小型功能避免直接用包管理器引入更多依赖。（大型库可以采取成熟的依赖项，比如 PixiJS、Matter.js、three.js）
- 做好取舍，经常考虑鲁棒性、一致性、全局观。少即是多，简单易懂大于复杂大于杂乱无章
- 做好对生命周期、内存、事件、流等状态的管理。可能耗时的任务做好异步、进度反馈，避免阻塞或卡顿
- 优先遵循最佳规范，优先级最大是 apple 规范，其次是 google 规范、meta 规范。应用在无论是 UI/UX 还是架构、工程还是小到每一行代码
- 优先考虑移动端 UI/UX 设计（当然也能适应桌面端更好），不为不同屏幕尺寸做太多冗余逻辑
- 优先为汉语母语者设计 UI/UX、文案，简约自然，可以参考 Apple 的习惯
- 项目主体是一个单页面应用程序 SPA，减少对服务器的依赖
- 可以利用好 ./skills 中的技能


## 重要提醒

- 优先考虑游戏交互和设计、而非通常的 app 页面布局UI/UX
- 操作、交互设计要简洁，避免复杂的按钮、按键或触控等操作，适配好手机、平板等屏幕尺寸的触摸操作
- 本着第一性原理，简化项目复杂度，少即是多
- 非必要不注释，让代码就如注释一般清晰、好懂、可维护。实在需要补充高信息量的上下文时，可以注释
- 对待项目、代码，始终要克制化

```


## Assistant

Alright, let me read the core simulation code and rendering pipeline. Main files:
- sfgame-web/src/sim/fluid.ts (fluid solver — the most likely CPU bottleneck)
- sfgame-web/src/sim/particles.ts, bodies.ts, trail.ts
- sfgame-web/src/game/simulation.ts (LevelSimulation orchestration)
- sfgame-web/src/core/loop.ts
- sfgame-web/src/core/batch.ts
- sfgame-web/src/ui/gl.ts, render.ts, controller.ts

Okay, let me read these in parallel.


### Tool: ReadFile: sfgame-web/src/sim/fluid.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/sim/fluid.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/sim/fluid.ts`

**Output:**
```typescript
import type { Vec2 } from './types'

/**
 * 均匀网格上的稳定流体求解器（Jos Stam, "Real-Time Fluid Dynamics for Games"）——欧拉视角。
 *
 * 场：速度 (u, v) 与温度扰动 t（相对环境温度的偏差，热为正、冷为负）。
 * 每步：浮力 → 涡度约束 → MacCormack 二阶平流（含衰减）→ 压强投影（保持无散度）。
 *
 * MacCormack（Selle et al. 2006）用"前推-回溯误差补偿"把一阶半拉格朗日升为二阶：
 * 耗散大幅下降、细节保留更好，代价仅两趟平流 + 邻域钳制（保证无条件稳定）。
 *
 * 压强投影用红黑 Gauss-Seidel，并沿用上一帧压强场做 warm-start 初值
 * （流场逐帧缓变，初值极好，同迭代数收敛显著更好）。
 *
 * 游戏物理叙事在此成立：热源加热空气 → 浮力上升 → 投影使周围冷空气补充流入，
 * 压强差自然涌现出水平风；冷源相反。世界坐标 y 向下。
 */
export interface FluidConfig {
  /** 网格列数 / 行数 */
  nx: number
  ny: number
  /** 单元格边长（世界单位） */
  cell: number
  /** 每单位温度产生的浮力加速度（世界单位/s²） */
  buoyancy: number
  /** 温度绝对值上限 */
  tMax: number
  /** 源每秒注入的温度量（中心处） */
  heatRate: number
  /** 源的注入半径（世界单位） */
  sourceRadius: number
  /** 速度每步保留比例（数值阻尼） */
  velDamping: number
  /** 温度每步保留比例（向环境回归） */
  tDamping: number
  /** 压强投影的 Gauss-Seidel 迭代次数 */
  iterations: number
  /** 涡度约束强度（0 关闭） */
  vorticity: number
}

export class Fluid {
  readonly nx: number
  readonly ny: number
  readonly cell: number
  /** 温度绝对值上限（与 cfg.tMax 同步，供渲染等只读使用） */
  readonly tMax: number
  private cfg: FluidConfig

  u: Float32Array
  v: Float32Array
  t: Float32Array
  solid: Uint8Array

  private u0: Float32Array
  private v0: Float32Array
  private t0: Float32Array
  /** MacCormack 误差补偿的临时场（q1/q2 跨场复用，场间串行处理） */
  private q1: Float32Array
  private q2: Float32Array
  private p: Float32Array
  private div: Float32Array
  private curl: Float32Array

  constructor(cfg: FluidConfig) {
    this.cfg = cfg
    this.nx = cfg.nx
    this.ny = cfg.ny
    this.cell = cfg.cell
    this.tMax = cfg.tMax
    const n = cfg.nx * cfg.ny
    this.u = new Float32Array(n)
    this.v = new Float32Array(n)
    this.t = new Float32Array(n)
    this.solid = new Uint8Array(n)
    this.u0 = new Float32Array(n)
    this.v0 = new Float32Array(n)
    this.t0 = new Float32Array(n)
    this.q1 = new Float32Array(n)
    this.q2 = new Float32Array(n)
    this.p = new Float32Array(n)
    this.div = new Float32Array(n)
    this.curl = new Float32Array(n)
  }

  clear() {
    this.u.fill(0)
    this.v.fill(0)
    this.t.fill(0)
    this.p.fill(0)
  }

  private ambientX = 0
  private ambientY = 0

  /** 环境背景风（谷风等）：叠加在采样结果上，平流场本身不受扰动。 */
  setAmbient(x: number, y: number) {
    this.ambientX = x
    this.ambientY = y
  }

  /** 地形高度函数以下（含）的单元格视为固体壁。同时封闭网格四边。 */
  setGroundMask(groundY: (x: number) => number) {
    const { nx, ny, cell } = this
    this.solid.fill(0)
    for (let j = 0; j < ny; j++) {
      for (let i = 0; i < nx; i++) {
        const cx = (i + 0.5) * cell
        const cy = (j + 0.5) * cell
        const edge = i === 0 || j === 0 || i === nx - 1 || j === ny - 1
        this.solid[i + j * nx] = edge || cy >= groundY(cx) ? 1 : 0
      }
    }
  }

  /** 在世界坐标 (wx, wy) 附近注入温度（热为正、冷为负），按半径线性衰减。 */
  addHeat(wx: number, wy: number, amount: number) {
    const { cell, nx, ny, t } = this
    const gr = this.cfg.sourceRadius / cell
    const gx = wx / cell - 0.5
    const gy = wy / cell - 0.5
    const x0 = Math.max(1, Math.floor(gx - gr))
    const x1 = Math.min(nx - 2, Math.ceil(gx + gr))
    const y0 = Math.max(1, Math.floor(gy - gr))
    const y1 = Math.min(ny - 2, Math.ceil(gy + gr))
    const tMax = this.cfg.tMax
    for (let j = y0; j <= y1; j++) {
      for (let i = x0; i <= x1; i++) {
        const idx = i + j * nx
        if (this.solid[idx]) continue
        const dx = i - gx
        const dy = j - gy
        const d = Math.sqrt(dx * dx + dy * dy)
        if (d >= gr) continue
        const falloff = 1 - d / gr
        let val = t[idx] + amount * falloff
        if (val > tMax) val = tMax
        else if (val < -tMax) val = -tMax
        t[idx] = val
      }
    }
  }

  /** 双线性采样速度场（世界单位/s），结果写入 out。 */
  sampleVelocity(wx: number, wy: number, out: Vec2) {
    const { nx, ny, cell, u, v } = this
    let gx = wx / cell - 0.5
    let gy = wy / cell - 0.5
    if (gx < 0) gx = 0
    else if (gx > nx - 1.001) gx = nx - 1.001
    if (gy < 0) gy = 0
    else if (gy > ny - 1.001) gy = ny - 1.001
    const i0 = Math.floor(gx)
    const j0 = Math.floor(gy)
    const fx = gx - i0
    const fy = gy - j0
    const i1 = i0 + 1
    const j1 = j0 + 1
    const a = i0 + j0 * nx
    const b = i1 + j0 * nx
    const c = i0 + j1 * nx
    const d = i1 + j1 * nx
    out.x =
      u[a] * (1 - fx) * (1 - fy) +
      u[b] * fx * (1 - fy) +
      u[c] * (1 - fx) * fy +
      u[d] * fx * fy
    out.y =
      v[a] * (1 - fx) * (1 - fy) +
      v[b] * fx * (1 - fy) +
      v[c] * (1 - fx) * fy +
      v[d] * fx * fy
    out.x += this.ambientX
    out.y += this.ambientY
  }

  sampleTemp(wx: number, wy: number): number {
    const { nx, ny, cell, t } = this
    let gx = wx / cell - 0.5
    let gy = wy / cell - 0.5
    if (gx < 0) gx = 0
    else if (gx > nx - 1.001) gx = nx - 1.001
    if (gy < 0) gy = 0
    else if (gy > ny - 1.001) gy = ny - 1.001
    const i0 = Math.floor(gx)
    const j0 = Math.floor(gy)
    const fx = gx - i0
    const fy = gy - j0
    const a = i0 + j0 * nx
    const b = i0 + 1 + j0 * nx
    const c = i0 + (j0 + 1) * nx
    const d = i0 + 1 + (j0 + 1) * nx
    return (
      t[a] * (1 - fx) * (1 - fy) +
      t[b] * fx * (1 - fy) +
      t[c] * (1 - fx) * fy +
      t[d] * fx * fy
    )
  }

  step(dt: number) {
    this.applyBuoyancy(dt)
    if (this.cfg.vorticity > 0) this.applyVorticity(dt)

    this.u0.set(this.u)
    this.v0.set(this.v)
    this.t0.set(this.t)
    this.advectMacCormack(this.u, this.u0, dt, this.cfg.velDamping)
    this.advectMacCormack(this.v, this.v0, dt, this.cfg.velDamping)
    this.advectMacCormack(this.t, this.t0, dt, this.cfg.tDamping)

    this.project()
    this.enforceBoundary()
  }

  private applyBuoyancy(dt: number) {
    const { nx, ny, v, t, solid } = this
    const k = this.cfg.buoyancy * dt
    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) continue
        v[idx] -= k * t[idx]
      }
    }
  }

  private applyVorticity(dt: number) {
    const { nx, ny, u, v, solid, curl, cell } = this
    const h2 = 2 * cell
    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) {
          curl[idx] = 0
          continue
        }
        curl[idx] = (v[idx + 1] - v[idx - 1]) / h2 - (u[idx + nx] - u[idx - nx]) / h2
      }
    }
    const f = this.cfg.vorticity * cell * dt
    for (let j = 2; j < ny - 2; j++) {
      for (let i = 2; i < nx - 2; i++) {
        const idx = i + j * nx
        if (solid[idx]) continue
        const dwdx = (Math.abs(curl[idx + 1]) - Math.abs(curl[idx - 1])) / h2
        const dwdy = (Math.abs(curl[idx + nx]) - Math.abs(curl[idx - nx])) / h2
        const len = Math.sqrt(dwdx * dwdx + dwdy * dwdy) + 1e-5
        const nxN = dwdx / len
        const nyN = dwdy / len
        const w = curl[idx]
        u[idx] += f * nyN * w
        v[idx] -= f * nxN * w
      }
    }
  }

  /**
   * MacCormack 二阶平流（Selle et al. 2006）。
   * q1 = 前向半拉格朗日平流，q2 = 反向平流 q1，误差补偿
   * q_new = q1 + (src - q2)/2，再钳制到 src 的 3×3 邻域极值内——
   * 钳制保证不产生新极值、无条件稳定（代价仅两趟平流 + 一趟邻域扫描）。
   * 所有场都用本步开始前的速度场 (u0, v0) 回溯，互不干扰。
   */
  private advectMacCormack(dst: Float32Array, src: Float32Array, dt: number, damping: number) {
    const { nx, ny, solid, q1, q2 } = this
    this.advectPass(q1, src, dt, 1)
    this.advectPass(q2, q1, dt, -1)
    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) {
          dst[idx] = 0
          continue
        }
        // 3×3 邻域极值（含固体格，其值为 0，钳制方向安全）
        let lo = src[idx]
        let hi = lo
        for (let jj = j - 1; jj <= j + 1; jj++) {
          for (let ii = i - 1; ii <= i + 1; ii++) {
            const v = src[ii + jj * nx]
            if (v < lo) lo = v
            else if (v > hi) hi = v
          }
        }
        let val = q1[idx] + (src[idx] - q2[idx]) * 0.5
        if (val < lo) val = lo
        else if (val > hi) val = hi
        dst[idx] = val * damping
      }
    }
  }

  /**
   * 单趟半拉格朗日平流：沿速度场回溯（sign=1）或前推（sign=-1）一个 dt，
   * 双线性插值采样源场。无条件稳定，一阶精度。
   */
  private advectPass(dst: Float32Array, src: Float32Array, dt: number, sign: number) {
    const { nx, ny, u0, v0, solid, cell } = this
    const dt0 = (dt / cell) * sign
    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) {
          dst[idx] = 0
          continue
        }
        let x = i - dt0 * u0[idx]
        let y = j - dt0 * v0[idx]
        if (x < 0.5) x = 0.5
        else if (x > nx - 1.5) x = nx - 1.5
        if (y < 0.5) y = 0.5
        else if (y > ny - 1.5) y = ny - 1.5
        const i0 = Math.floor(x)
        const j0 = Math.floor(y)
        const fx = x - i0
        const fy = y - j0
        const a = i0 + j0 * nx
        const b = a + 1
        const c = a + nx
        const d = c + 1
        dst[idx] =
          src[a] * (1 - fx) * (1 - fy) +
          src[b] * fx * (1 - fy) +
          src[c] * (1 - fx) * fy +
          src[d] * fx * fy
      }
    }
  }

  private project() {
    const { nx, ny, u, v, p, div, solid, cell } = this
    const h = cell
    const inv2h = 1 / (2 * h)
    const h2 = h * h

    // 散度场。p 不清零：warm-start，沿用上一帧压强做初值（收敛更快）。
    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) {
          div[idx] = 0
          p[idx] = 0
          continue
        }
        const uR = solid[idx + 1] ? 0 : u[idx + 1]
        const uL = solid[idx - 1] ? 0 : u[idx - 1]
        const vD = solid[idx + nx] ? 0 : v[idx + nx]
        const vU = solid[idx - nx] ? 0 : v[idx - nx]
        div[idx] = (uR - uL + vD - vU) * inv2h
      }
    }

    // 红黑 Gauss-Seidel：两色交替扫描，收敛约两倍于顺序 GS，
    // 且天然可并行。网格四边恒为固体，内圈邻域索引必在界内。
    const iterations = this.cfg.iterations
    for (let it = 0; it < iterations; it++) {
      for (let parity = 0; parity < 2; parity++) {
        for (let j = 1; j < ny - 1; j++) {
          const i0 = ((parity ^ (j & 1)) & 1) ? 1 : 2
          for (let i = i0; i < nx - 1; i += 2) {
            const idx = i + j * nx
            if (solid[idx]) continue
            const pL = solid[idx - 1] ? p[idx] : p[idx - 1]
            const pR = solid[idx + 1] ? p[idx] : p[idx + 1]
            const pU = solid[idx - nx] ? p[idx] : p[idx - nx]
            const pD = solid[idx + nx] ? p[idx] : p[idx + nx]
            p[idx] = (pL + pR + pU + pD - h2 * div[idx]) * 0.25
          }
        }
      }
    }

    for (let j = 1; j < ny - 1; j++) {
      for (let i = 1; i < nx - 1; i++) {
        const idx = i + j * nx
        if (solid[idx]) continue
        const pL = solid[idx - 1] ? p[idx] : p[idx - 1]
        const pR = solid[idx + 1] ? p[idx] : p[idx + 1]
        const pU = solid[idx - nx] ? p[idx] : p[idx - nx]
        const pD = solid[idx + nx] ? p[idx] : p[idx + nx]
        u[idx] -= (pR - pL) * inv2h
        v[idx] -= (pD - pU) * inv2h
      }
    }
  }

  private enforceBoundary() {
    const { u, v, t, solid } = this
    for (let idx = 0; idx < u.length; idx++) {
      if (solid[idx]) {
        u[idx] = 0
        v[idx] = 0
        t[idx] = 0
      }
    }
  }
}

```


### Tool: ReadFile: sfgame-web/src/game/simulation.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/game/simulation.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/game/simulation.ts`

**Output:**
```typescript
import { Fluid, type FluidConfig } from '../sim/fluid'
import { createBody, stepBody, type Body } from '../sim/bodies'
import type { SourceKind } from '../sim/types'
import type { HudState, LevelDef, Source, SourcePlacement } from './types'

/** 流体物理调参：所有关卡共享同一套"空气性格"。 */
const FLUID_TUNING: Omit<FluidConfig, 'nx' | 'ny' | 'cell'> = {
  buoyancy: 2.0,
  tMax: 9,
  heatRate: 10,
  sourceRadius: 3.4,
  velDamping: 0.997,
  tDamping: 0.99,
  iterations: 12,
  vorticity: 0.5,
}

/** 纸飞机：很轻、风阻大、重力弱，飘然而起。
 * 悬停阈值 = gravity/dragK ≈ 1.0（上升气流需超过它才能抬升）。 */
const PLANE_PHYSICS = { radius: 1.0, dragK: 3.0, gravity: 3.0 }

/** 源之间的最小间距，避免叠放。 */
const MIN_SOURCE_GAP = 3.2
const SOURCE_HIT_RADIUS = 3.0
/** 放置下限：地面之上该高度内均可放置；贴地吸附高度 */
const GROUND_PLACE_MARGIN = 0.6
const GROUND_SNAP_LIFT = 0.7
/** 目标区圆心在地面上的抬升高度 / 判胜所需最小飞行高度 */
const GOAL_LIFT = 2
const FLYING_ALT = 1
/** URL 位置匹配容差：对齐 URL 的 1 位小数精度（舍入误差 ≤0.05） */
const URL_PRECISION_TOLERANCE = 0.06

/**
 * 无头关卡模拟：流体 + 刚体 + 源管理 + 胜负判定。
 * 不依赖 DOM/Canvas，可在 bun 中直接测试（含无头通关验证）。
 */
export class LevelSimulation {
  readonly level: LevelDef
  readonly fluid: Fluid
  readonly plane: Body
  sources: Source[] = []
  phase: 'playing' | 'won' = 'playing'
  time = 0

  private nextId = 1
  private usedHot = 0
  private usedCold = 0
  private placed = 0
  private spawnY: number
  private spawnVx: number
  private spawnVy: number

  constructor(level: LevelDef) {
    this.level = level
    const { w, h, cell } = level.world
    this.fluid = new Fluid({
      nx: Math.round(w / cell),
      ny: Math.round(h / cell),
      cell,
      ...FLUID_TUNING,
    })
    this.fluid.setGroundMask(level.ground)
    this.fluid.setAmbient(level.ambient?.x ?? 0, level.ambient?.y ?? 0)
    this.spawnY = level.spawn.y ?? level.ground(level.spawn.x) - 1.4
    this.spawnVx = level.spawn.vx ?? 0
    this.spawnVy = level.spawn.vy ?? 0
    this.plane = createBody(level.spawn.x, this.spawnY, PLANE_PHYSICS)
    this.plane.vx = this.spawnVx
    this.plane.vy = this.spawnVy
  }

  get hotLeft() {
    return this.level.budget.hot - this.usedHot
  }

  get coldLeft() {
    return this.level.budget.cold - this.usedCold
  }

  hudState(): HudState {
    return {
      phase: this.phase,
      hotLeft: this.hotLeft,
      coldLeft: this.coldLeft,
      placed: this.placed,
    }
  }

  /** 完整重置：清场、清空源与预算占用、飞机回出生点、时间与胜负复位。 */
  reset() {
    this.fluid.clear()
    this.sources = []
    this.usedHot = 0
    this.usedCold = 0
    this.placed = 0
    this.phase = 'playing'
    this.time = 0
    this.plane.x = this.level.spawn.x
    this.plane.y = this.spawnY
    this.plane.vx = this.spawnVx
    this.plane.vy = this.spawnVy
    this.plane.angle = 0
  }

  /** 只清场与复位飞机，保留玩家已放置的源与预算（UI「再玩一次」语义）。 */
  restart() {
    this.fluid.clear()
    this.phase = 'playing'
    this.time = 0
    // 源在新的一局重放生长动画：born 归零（否则 time < born，渲染 pop 为负 → 源隐形）
    for (const s of this.sources) s.born = 0
    this.plane.x = this.level.spawn.x
    this.plane.y = this.spawnY
    this.plane.vx = this.spawnVx
    this.plane.vy = this.spawnVy
    this.plane.angle = 0
  }

  /** 位置是否允许放置：世界内、地面之上（可贴地）、离其他源足够远。 */
  canPlaceAt(x: number, y: number): boolean {
    const { w } = this.level.world
    if (x < 2 || x > w - 2 || y < 3) return false
    if (y > this.level.ground(x) - GROUND_PLACE_MARGIN) return false
    for (const s of this.sources) {
      if (Math.hypot(s.x - x, s.y - y) < MIN_SOURCE_GAP) return false
    }
    return true
  }

  hitSource(x: number, y: number): Source | null {
    let best: Source | null = null
    let bestDist = SOURCE_HIT_RADIUS
    for (const s of this.sources) {
      const d = Math.hypot(s.x - x, s.y - y)
      if (d < bestDist) {
        bestDist = d
        best = s
      }
    }
    return best
  }

  /**
   * 放置源。force 仅绕过"must be playing"（URL 状态恢复用，含获胜后重做）；
   * 预算与位置校验仍生效。
   */
  placeSource(x: number, y: number, kind: SourceKind, force = false): Source | null {
    if (!force && this.phase !== 'playing') return null
    if (kind === 'hot' ? this.hotLeft <= 0 : this.coldLeft <= 0) return null
    // 点在地面/物体上时，吸附到贴地高度——"在脚下放源"是核心交互，不应拒绝
    const cy = Math.min(y, this.level.ground(x) - GROUND_SNAP_LIFT)
    if (!this.canPlaceAt(x, cy)) return null
    const source: Source = { id: this.nextId++, kind, x, y: cy, born: this.time }
    this.sources.push(source)
    if (kind === 'hot') this.usedHot++
    else this.usedCold++
    this.placed++
    return source
  }

  removeSource(id: number): boolean {
    const i = this.sources.findIndex((s) => s.id === id)
    if (i < 0) return false
    const s = this.sources[i]
    this.sources.splice(i, 1)
    if (s.kind === 'hot') this.usedHot--
    else this.usedCold--
    return true
  }

  /**
   * 把源集合收敛到目标列表（URL 状态应用）。最小差异：移除不在目标中的、
   * 补放目标中缺失的（force），存活源保留原 id/born（不重播生长动画）。
   * 注意移除必须与"目标列表"比对（比对场上会永不删除）。幂等，可安全重复调用。
   */
  applySources(target: SourcePlacement[]): void {
    const match = (a: Source, b: SourcePlacement) =>
      a.kind === b.kind &&
      Math.abs(a.x - b.x) < URL_PRECISION_TOLERANCE &&
      Math.abs(a.y - b.y) < URL_PRECISION_TOLERANCE
    for (const s of [...this.sources]) {
      if (!target.some((t) => match(s, t))) this.removeSource(s.id)
    }
    for (const t of target) {
      if (!this.sources.some((s) => match(s, t))) this.placeSource(t.x, t.y, t.kind, true)
    }
  }

  step(dt: number) {
    this.time += dt
    const rate = FLUID_TUNING.heatRate * dt
    for (const s of this.sources) {
      this.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)
    }
    this.fluid.step(dt)
    stepBody(this.plane, this.fluid, dt, this.level.ground, this.level.world)
    if (this.phase === 'playing' && this.inGoal()) {
      this.phase = 'won'
    }
  }

  private inGoal(): boolean {
    const g = this.level.goal
    const gy = this.level.ground(g.x) - GOAL_LIFT
    if (Math.hypot(this.plane.x - g.x, this.plane.y - gy) >= g.r) return false
    // 必须飞行抵达：贴地滑进目标区不算过关（杜绝"放着不动被风吹进圈"的挂机通关）
    return this.plane.y < this.level.ground(this.plane.x) - FLYING_ALT
  }
}

```


## Assistant

Alright, let's take a look at the level to understand the grid size, and also check loop.ts, batch.ts, gl.ts, render.ts, particles.ts, and trail.ts.


### Tool: ReadFile: sfgame-web/src/core/loop.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/core/loop.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/core/loop.ts`

**Output:**
```typescript
export interface LoopHandlers {
  /** 固定步长模拟 tick，dt 恒为 SIM_DT（秒） */
  tick: (dt: number) => void
  /** 每个动画帧调用一次 */
  render: () => void
}

export const SIM_DT = 1 / 60
const SIM_DT_MS = SIM_DT * 1000
const MAX_FRAME = 0.25
/** 单帧最多执行的模拟步数（= 1 秒模拟时间）：暂停回归的追赶尖峰在任意倍速下都封顶 */
const MAX_TICKS_PER_FRAME = 60

/**
 * 固定步长游戏循环：模拟以 60Hz 稳定推进，渲染跟随显示器刷新率。
 * 页面切后台时 rAF 自动暂停，恢复后由 MAX_FRAME 截断避免时间突进。
 *
 * 倍速（setRate）只改变每真实秒的 tick 次数，每 tick 的 dt 恒为 SIM_DT：
 * 物理轨迹逐位不变，只是墙上时间快慢不同——这是"速率不影响轨迹"的根源。
 *
 * 渲染只在"模拟步进过的帧"执行：120Hz ProMotion 屏上 rAF 以 120Hz 触发，
 * 但模拟只步进 60Hz，若每帧都渲染，Canvas 2D 工作量为 60fps 的两倍
 * （持续高负载 → 发热降频 → 帧率渐进恶化）。倍速下每帧都步进，
 * 渲染因此再封顶 60Hz，高速率不放大渲染负载。
 */
export class GameLoop {
  private handlers: LoopHandlers
  private rafId = 0
  private last = 0
  private acc = 0
  private running = false
  private rate = 1
  private lastRender = -Infinity

  constructor(handlers: LoopHandlers) {
    this.handlers = handlers
  }

  /** 游戏速率倍数（每真实秒的模拟 tick 数 = 60 × rate）。 */
  setRate(rate: number) {
    this.rate = rate
  }

  start() {
    if (this.running) return
    this.running = true
    this.last = performance.now()
    this.acc = 0
    this.lastRender = -Infinity
    this.rafId = requestAnimationFrame(this.frame)
  }

  stop() {
    if (!this.running) return
    this.running = false
    cancelAnimationFrame(this.rafId)
  }

  private frame = (now: number) => {
    if (!this.running) return
    let frameDt = (now - this.last) / 1000
    this.last = now
    if (frameDt > MAX_FRAME) frameDt = MAX_FRAME
    if (frameDt < 0) frameDt = 0
    this.acc += frameDt * this.rate
    let stepped = false
    let ticks = 0
    while (this.acc >= SIM_DT && ticks < MAX_TICKS_PER_FRAME) {
      this.handlers.tick(SIM_DT)
      this.acc -= SIM_DT
      stepped = true
      ticks++
    }
    if (stepped && now - this.lastRender >= SIM_DT_MS) {
      this.handlers.render()
      this.lastRender = now
    }
    this.rafId = requestAnimationFrame(this.frame)
  }
}

```


### Tool: ReadFile: sfgame-web/src/game/levels.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/game/levels.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/game/levels.ts`

**Output:**
```typescript
import type { LevelDef } from './types'

function clamp01(t: number) {
  return t < 0 ? 0 : t > 1 ? 1 : t
}

function smoothstep(t: number) {
  return t * t * (3 - 2 * t)
}

/**
 * 第 1 关 · 纸飞机起飞 —— 核心物理概念：上升气流。
 *
 * 布局：纸飞机从画布左外侧高速飞入，掠过峡谷；右侧是一座高崖，崖顶有目标区。
 * 开场即物理：不操作时飞机受重力缓缓坠落、约 5 秒落地谷底，无法抵达目标；
 * 玩家需在它落地前于其下方放热源，用上升热气流托住并送上山崖。
 * 谷中有一缕持续的向右谷风（ambient），托住后自然护送飞机右行，
 * 玩家可用热源接力、冷源按压微调落点（初学关，预算宽松）。
 */
function ground1(x: number): number {
  // 左段谷底 y=48，x∈[36,48] 平滑爬升，右段高原 y=22
  return 48 - 26 * smoothstep(clamp01((x - 36) / 12))
}

export const LEVEL_1: LevelDef = {
  id: 1,
  name: '纸飞机起飞',
  tagline: '上升气流',
  hint: '在纸飞机下方轻点，放一团热源——热空气上升，会把飞机送上山崖。',
  win: {
    title: '飞起来了！',
    text: '纸飞机乘着热气流抵达了目标。',
  },
  world: { w: 76, h: 56, cell: 0.75 },
  ground: ground1,
  budget: { hot: 4, cold: 2 },
  spawn: { x: -6, y: 33, vx: 40 },
  goal: { x: 58, r: 7.5 },
  ambient: { x: 1.8, y: 0 },
}

/**
 * 第 2 关 · 降落 —— 核心物理概念：下沉气流。
 *
 * 与第 1 关对称成对：第 1 关教"热主升"，这一关教"冷主降"。
 * 布局极简：一道缓缓加深的山谷，目标在右侧更深处。
 * 飞机自左侧高空入场、随风右行——不操作时它会掠过目标上空、撞上远处谷壁，
 * 玩家需在航线下方布置冷源：冷空气下沉，把飞机稳稳压进谷底目标。
 * 放得太早太低会摔在目标之前，太晚则掠过——落点的取舍即本关的谜题。
 * （设计说明：曾试验"翻山"概念——热气流沿坡托升越脊，但轻质量飞机在强热柱中
 * 轨迹混沌、对摆放过于敏感；降落概念的冷下沉路径平滑、容错宽，故选型于此。）
 */
function ground2(x: number): number {
  // 左浅右深的谷地：y=42（左）缓降至 y=50（右，x≥30）
  return 42 + 8 * smoothstep(clamp01(x / 30))
}

export const LEVEL_2: LevelDef = {
  id: 2,
  name: '降落',
  tagline: '下沉气流',
  hint: '飞机飞得太高会掠过目标。在它前方放冷源——冷空气下沉，把它稳稳压进山谷。',
  win: {
    title: '稳稳降落！',
    text: '下沉气流托着飞机缓缓落进山谷。热让风上升，冷让风下沉——落点由你定。',
  },
  world: { w: 76, h: 56, cell: 0.75 },
  ground: ground2,
  budget: { hot: 2, cold: 3 },
  spawn: { x: -5, y: 8, vx: 16 },
  goal: { x: 62, r: 7.5 },
  ambient: { x: 2.6, y: 0 },
}

/** 未来关卡占位（仅用于标题页展示，未实现）。 */
export const UPCOMING_LEVELS: Array<Pick<LevelDef, 'id' | 'name' | 'tagline'>> = [
  { id: 3, name: '蒲公英的旅行', tagline: '风向与分量' },
  { id: 4, name: '热气球嘉年华', tagline: '浮力与平衡' },
  { id: 5, name: '能量循环', tagline: '风能 → 电能 → 热能' },
]

export const LEVELS: LevelDef[] = [LEVEL_1, LEVEL_2]

```


## Assistant

Grid: 76/0.75 ≈ 101.33 → nx = Math.round(76/0.75) = 101, ny = Math.round(56/0.75) = 75. So the grid is 101×75 ≈ 7575 cells.

Per fluid step:
- applyBuoyancy: 1 pass over the interior (~7.4k cells)
- applyVorticity: 2 passes (~14.8k), with Math.sqrt per cell
- 3 fields × advectMacCormack: each = 2 advectPass (each ~7.4k, with floor/bilinear operations) + 1 correction pass with 3×3 min/max (9 reads + comparisons) ≈ 7.4k×3 = 22k heavy operations per field → 3 fields = ~66k
- project: divergence pass + 12 iterations × 2 parities... no wait, iterations=12, each iteration is parity 0,1 → covers all cells once per iteration → 12 passes × 7.4k = 88k cell updates, each with 4 solid checks, 4 reads, 1 write. Plus gradient subtraction pass 7.4k.
- enforceBoundary: full 7.5k scan.

Total per step: roughly 150k~200k cell operations, each containing several array accesses. At 60Hz that's ~10M cell operations per second, plus the heavy inner work. In JIT-compiled JS, probably 2~4ms per frame on desktop; on an iPhone with JIT (Safari 17.4+) probably 4~8ms; in WKWebView without JIT (in-app browsers like WeChat? — "Android Webview" was mentioned, so the user might be distributing via WebView)... could be 20~40ms. Hmm.

Wait — an important observation: `iterations: 12` in FLUID_TUNING, but the report says "×39 passes/step"? 12 iterations × (something)... actually the report says "×~39 passes/step" — buoyancy 1 + vorticity 2 + advection 3×3=9 + div 1 + pressure 12×2? No. Let me count: buoyancy 1, vorticity 2, MacCormack 3 fields × (2 passes + 1 correction) = 9, projection: div 1 + GS 12 + gradient 1 = 14, boundary 1. Total = 1+2+9+14+1 = 27 passes. Close enough to 39 depending on how you count parity sub-passes (12 iterations × 2 parity passes = 24 → total 37 ≈ 39).

OK. Next let's look at the render side: batch.ts, gl.ts, render.ts, controller.ts, particles.ts, trail.ts. These determine the CPU-side vertex construction cost and draw calls.


### Tool: ReadFile: sfgame-web/src/core/batch.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/core/batch.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/core/batch.ts`

**Output:**
```typescript
/**
 * 彩色三角网格的即时模式构建器（纯计算，无 DOM/WebGL 依赖，可无头测试）。
 *
 * 顶点格式：x, y, r, g, b, a（颜色 0..1，非预乘 alpha），平铺于 Float32Array。
 * 渲染层（ui/gl.ts）只负责把 data.subarray(0, count * VERTEX_STRIDE) 上传 GPU
 * 并以 TRIANGLES 绘制——所有图元（矩形/线段/椭圆/弧/径向渐变）都在此展开为三角形。
 *
 * 设计动机：Canvas 2D 的描边/填充是"状态机 + 路径对象"，批量提交只能靠分桶近似
 * （透明度/颜色离散化）；逐顶点着色后每个图元可携带精确颜色与透明度，
 * 整帧一次 draw call，且线段宽度不再受 GL lineWidth 恒为 1 的平台限制。
 */

/** 每顶点浮点数个数：x, y, r, g, b, a */
export const VERTEX_STRIDE = 6

/** 椭圆/圆弧展开的默认分段数（世界尺度下半径 0.3~12，20 段已足够圆滑） */
export const DISC_SEGMENTS = 20

export class MeshBatch {
  /** 顶点数据平铺数组（容量 ≥ count * VERTEX_STRIDE） */
  data: Float32Array
  /** 已写入的顶点数 */
  count = 0

  constructor(capacity = 32768) {
    this.data = new Float32Array(capacity * VERTEX_STRIDE)
  }

  reset() {
    this.count = 0
  }

  private ensure(extra: number) {
    const need = (this.count + extra) * VERTEX_STRIDE
    if (need <= this.data.length) return
    let cap = this.data.length
    while (cap < need) cap *= 2
    const next = new Float32Array(cap)
    next.set(this.data)
    this.data = next
  }

  private push(x: number, y: number, r: number, g: number, b: number, a: number) {
    const o = this.count * VERTEX_STRIDE
    const d = this.data
    d[o] = x
    d[o + 1] = y
    d[o + 2] = r
    d[o + 3] = g
    d[o + 4] = b
    d[o + 5] = a
    this.count++
  }

  /** 单色三角形 */
  tri(
    x0: number, y0: number,
    x1: number, y1: number,
    x2: number, y2: number,
    r: number, g: number, b: number, a: number,
  ) {
    this.ensure(3)
    this.push(x0, y0, r, g, b, a)
    this.push(x1, y1, r, g, b, a)
    this.push(x2, y2, r, g, b, a)
  }

  /** 轴对齐矩形（2 三角形），y0 < y1 */
  rect(x0: number, y0: number, x1: number, y1: number, r: number, g: number, b: number, a: number) {
    this.ensure(6)
    this.push(x0, y0, r, g, b, a)
    this.push(x1, y0, r, g, b, a)
    this.push(x0, y1, r, g, b, a)
    this.push(x1, y0, r, g, b, a)
    this.push(x1, y1, r, g, b, a)
    this.push(x0, y1, r, g, b, a)
  }

  /** 垂直渐变矩形：顶部颜色 → 底部颜色（逐顶点插值，等价两端色标的线性渐变） */
  rectVGrad(
    x0: number, y0: number, x1: number, y1: number,
    r0: number, g0: number, b0: number, a0: number,
    r1: number, g1: number, b1: number, a1: number,
  ) {
    this.ensure(6)
    this.push(x0, y0, r0, g0, b0, a0)
    this.push(x1, y0, r0, g0, b0, a0)
    this.push(x0, y1, r1, g1, b1, a1)
    this.push(x1, y0, r0, g0, b0, a0)
    this.push(x1, y1, r1, g1, b1, a1)
    this.push(x0, y1, r1, g1, b1, a1)
  }

  /**
   * 线段 → 沿法线加宽的四边形（GL lineWidth 在多平台被钳制为 1，宽度必须几何化）。
   * 端点为平头（butt cap）：拖尾/轨迹由连续短段组成，段间缝隙可忽略。
   */
  stroke(x0: number, y0: number, x1: number, y1: number, w: number, r: number, g: number, b: number, a: number) {
    const dx = x1 - x0
    const dy = y1 - y0
    const len = Math.sqrt(dx * dx + dy * dy)
    if (len < 1e-8) return
    const hw = w / 2 / len
    const nx = -dy * hw
    const ny = dx * hw
    this.ensure(6)
    this.push(x0 + nx, y0 + ny, r, g, b, a)
    this.push(x1 + nx, y1 + ny, r, g, b, a)
    this.push(x0 - nx, y0 - ny, r, g, b, a)
    this.push(x1 + nx, y1 + ny, r, g, b, a)
    this.push(x1 - nx, y1 - ny, r, g, b, a)
    this.push(x0 - nx, y0 - ny, r, g, b, a)
  }

  /** 斜接长度钳制系数（相对半宽）：过锐转角等效斜切，防尖刺 */
  private static MITER_LIMIT = 4

  /**
   * 折线描边：相邻线段在转角处按角平分线斜接（miter）相连，转角无缝无缺口。
   * 首尾端点平头；零长段自动跳过。要求折线方向一致（各段法线同侧），
   * 适用于地形等单调折线；随机折回的轨迹仍用逐段 stroke。
   * pts 为 [x0,y0,x1,y1,...] 平铺，n 为浮点数个数（偶数）。
   */
  polyline(pts: Float32Array, n: number, w: number, r: number, g: number, b: number, a: number) {
    if (n < 4) return
    const hw = w / 2
    const limit = MeshBatch.MITER_LIMIT * hw
    // 上段状态：起点 (sx,sy)、起点端斜接向量 (mx,my)（已含半宽）、上段单位法线 (pnx,pny)
    let sx = 0
    let sy = 0
    let mx = 0
    let my = 0
    let pnx = 0
    let pny = 0
    let ready = false
    for (let i = 0; i + 3 < n; i += 2) {
      const ax = pts[i]
      const ay = pts[i + 1]
      const bx = pts[i + 2]
      const by = pts[i + 3]
      const dx = bx - ax
      const dy = by - ay
      const len = Math.sqrt(dx * dx + dy * dy)
      if (len < 1e-8) continue
      const nx = -dy / len
      const ny = dx / len
      if (!ready) {
        // 首段：起点平头
        sx = ax
        sy = ay
        mx = nx * hw
        my = ny * hw
        pnx = nx
        pny = ny
        ready = true
        continue
      }
      // 本段起点 = 上段终点：斜接向量 = 两段法线之和（角平分线方向）
      let tx = pnx + nx
      let ty = pny + ny
      let fl = Math.sqrt(tx * tx + ty * ty)
      if (fl < 1e-6) {
        // 180° 折返：退化，按平头退避
        tx = nx
        ty = ny
        fl = 1
      } else {
        tx /= fl
        ty /= fl
        fl = hw / (pnx * tx + pny * ty) // = hw / cos(θ/2)
        if (fl > limit) fl = limit
      }
      const ex = tx * fl
      const ey = ty * fl
      // 上段四边形：起点 ±(mx,my)，终点 ±(ex,ey)
      this.ensure(6)
      this.push(sx + mx, sy + my, r, g, b, a)
      this.push(ax + ex, ay + ey, r, g, b, a)
      this.push(sx - mx, sy - my, r, g, b, a)
      this.push(ax + ex, ay + ey, r, g, b, a)
      this.push(ax - ex, ay - ey, r, g, b, a)
      this.push(sx - mx, sy - my, r, g, b, a)
      sx = ax
      sy = ay
      mx = ex
      my = ey
      pnx = nx
      pny = ny
    }
    // 末段：终点平头
    if (ready) {
      const bx = pts[n - 2]
      const by = pts[n - 1]
      const ex = pnx * hw
      const ey = pny * hw
      this.ensure(6)
      this.push(sx + mx, sy + my, r, g, b, a)
      this.push(bx + ex, by + ey, r, g, b, a)
      this.push(sx - mx, sy - my, r, g, b, a)
      this.push(bx + ex, by + ey, r, g, b, a)
      this.push(bx - ex, by - ey, r, g, b, a)
      this.push(sx - mx, sy - my, r, g, b, a)
    }
  }

  /** 椭圆扇形填充：rot 为长轴旋转角（弧度）。rx = ry 时即正圆。 */
  disc(
    cx: number, cy: number, rx: number, ry: number, rot: number, seg: number,
    r: number, g: number, b: number, a: number,
  ) {
    if (rx <= 0 || ry <= 0 || a <= 0) return
    this.ensure(seg * 3)
    const cos = Math.cos(rot)
    const sin = Math.sin(rot)
    let px = 0
    let py = 0
    for (let i = 0; i <= seg; i++) {
      const th = (i / seg) * Math.PI * 2
      const ex = rx * Math.cos(th)
      const ey = ry * Math.sin(th)
      const qx = cx + ex * cos - ey * sin
      const qy = cy + ex * sin + ey * cos
      if (i > 0) this.tri(cx, cy, px, py, qx, qy, r, g, b, a)
      px = qx
      py = qy
    }
  }

  /**
   * 径向渐变圆盘：中心色 → 边缘色逐顶点线性插值。
   * 与 Canvas 2D 两端色标（0 处 / 1 处）的 createRadialGradient 视觉等价，
   * 免去每帧建渐变对象或烘焙精灵位图。
   */
  discGrad(
    cx: number, cy: number, radius: number, seg: number,
    cr: number, cg: number, cb: number, ca: number,
    er: number, eg: number, eb: number, ea: number,
  ) {
    if (radius <= 0) return
    this.ensure(seg * 3)
    let px = 0
    let py = 0
    for (let i = 0; i <= seg; i++) {
      const th = (i / seg) * Math.PI * 2
      const qx = cx + radius * Math.cos(th)
      const qy = cy + radius * Math.sin(th)
      if (i > 0) {
        this.ensure(3)
        this.push(cx, cy, cr, cg, cb, ca)
        this.push(px, py, er, eg, eb, ea)
        this.push(qx, qy, er, eg, eb, ea)
      }
      px = qx
      py = qy
    }
  }

  /** 椭圆描边（分段 stroke 逼近），线宽 w */
  ring(
    cx: number, cy: number, rx: number, ry: number, rot: number, seg: number, w: number,
    r: number, g: number, b: number, a: number,
  ) {
    if (rx <= 0 || ry <= 0 || a <= 0) return
    const cos = Math.cos(rot)
    const sin = Math.sin(rot)
    let px = 0
    let py = 0
    for (let i = 0; i <= seg; i++) {
      const th = (i / seg) * Math.PI * 2
      const ex = rx * Math.cos(th)
      const ey = ry * Math.sin(th)
      const qx = cx + ex * cos - ey * sin
      const qy = cy + ex * sin + ey * cos
      if (i > 0) this.stroke(px, py, qx, qy, w, r, g, b, a)
      px = qx
      py = qy
    }
  }

  /** 圆弧描边：角度 a0 → a1（弧度，y 向下坐标系），线宽 w */
  arc(
    cx: number, cy: number, radius: number, a0: number, a1: number, seg: number, w: number,
    r: number, g: number, b: number, a: number,
  ) {
    if (radius <= 0 || a <= 0 || a1 === a0) return
    let px = cx + radius * Math.cos(a0)
    let py = cy + radius * Math.sin(a0)
    for (let i = 1; i <= seg; i++) {
      const th = a0 + ((a1 - a0) * i) / seg
      const qx = cx + radius * Math.cos(th)
      const qy = cy + radius * Math.sin(th)
      this.stroke(px, py, qx, qy, w, r, g, b, a)
      px = qx
      py = qy
    }
  }

  /** 虚线圆：on/off 为弧长（世界单位），从角度 0 起按周长铺排 */
  dashRing(
    cx: number, cy: number, radius: number, on: number, off: number, w: number,
    r: number, g: number, b: number, a: number,
  ) {
    const circ = Math.PI * 2 * radius
    const period = on + off
    if (circ <= 0 || period <= 0) return
    let s = 0
    while (s < circ) {
      const segLen = Math.min(on, circ - s)
      const a0 = s / radius
      const a1 = (s + segLen) / radius
      const segs = Math.max(2, Math.ceil(segLen / 0.5))
      this.arc(cx, cy, radius, a0, a1, segs, w, r, g, b, a)
      s += period
    }
  }
}

```


## Assistant

Alright, let me read render.ts, gl.ts, particles.ts, trail.ts, and controller.ts.


### Tool: ReadFile: sfgame-web/src/ui/render.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/ui/render.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/ui/render.ts`

**Output:**
```typescript
import { MeshBatch } from '../core/batch'
import { GlRenderer } from './gl'
import type { Tracers } from '../sim/particles'
import { TRAIL_FADE } from '../sim/particles'
import type { Trail } from '../sim/trail'
import type { Vec2 } from '../sim/types'
import type { LevelSimulation } from '../game/simulation'
import type { PressVisual } from '../game/types'
import { LONG_PRESS_MS } from './input'

export interface SceneState {
  sim: LevelSimulation
  tracers: Tracers
  planeTrail: Trail
  press: PressVisual | null
  now: number
}

/** 颜色统一 0..1 浮点（非预乘 alpha），与着色器逐顶点格式一致 */
type RGB = readonly [number, number, number]

const rgb = (r: number, g: number, b: number): RGB => [r / 255, g / 255, b / 255]

const HOT = rgb(255, 90, 60)
const COLD = rgb(61, 139, 255)
/** 飞机拖尾：深石墨蓝灰（比所有空气线条更深更冷），α 取高以盖过奶油底色 */
const TRAIL_INK = rgb(64, 74, 106)
const INK_DARK = rgb(61, 52, 39)
const GOAL = rgb(47, 191, 113)
const SKY_TOP = rgb(255, 248, 234)
const SKY_BOTTOM = rgb(248, 226, 187)
const GROUND_FILL = rgb(236, 220, 186)
const GROUND_EDGE = rgb(216, 193, 147)
const SUN = rgb(255, 196, 83)
const PAPER = rgb(255, 253, 248)
const FLAG_POLE = rgb(107, 91, 69)

// ---------- 视觉参数（世界单位，关卡 76×56 尺度） ----------

const SUN_POS = { x: 12, y: 9.5 }
const SUN_RADIUS = 4
const SUN_BREATH_AMP = 0.12
const SUN_BREATH_PERIOD = 700

const TEMP_LEVELS = 5
/** 温度归一化基准：实测四源解粒子温度 p95≈5.3，取 5 定"赤热"满饱和档 */
const T_REF = 5
const LINE_COLORS: RGB[] = [
  rgb(61, 139, 255),
  rgb(116, 154, 208),
  rgb(170, 168, 160),
  rgb(212, 129, 110),
  rgb(255, 90, 60),
]
/** 头部点不透明度按档递减：自然温度最透（半透明浅灰），冷热端更实 */
const HEAD_ALPHA = [0.8, 0.65, 0.45, 0.65, 0.85]
const LINE_ALPHA_MAX = 0.2
const TRACER_LINE_WIDTH = 0.3
const TRACER_HEAD_RADIUS = 0.3
const GUST_BASE = 0.7
const GUST_BOOST = 0.6
const GUST_FULL_SPEED = 4
/** 风速平方低于此值视为静止空气：不画线也不画粒子（"有风才见线"） */
const CALM_AIR_SPEED2 = 0.16
const VISIBLE_ALPHA = 0.02
const SOURCE_POP_RATE = 9
const SOURCE_GLOW_RADIUS = 4.8
const SOURCE_CORE_RADIUS = 1.15

/** 地形采样：基础步长；平坦段最大合并间距；相邻段转角阈值（弧度，≈1.1°） */
const TERRAIN_STEP = 0.25
const TERRAIN_MAX_STEP = 2
const TERRAIN_ANG_TOL = 0.02

/** 飞机阴影：顶光垂直投影（光从 12 点方向直照到 6 点方向）——
 * 影子即飞机在地面的竖直投影（同 x），随坡度旋转贴合地形，任意高度可见 */
const SHADOW_RADIUS = 1.5
const SHADOW_RY = 0.32
const SHADOW_LIFT = 0.12
const SHADOW_MAX_ALPHA = 0.3

/**
 * WebGL 渲染器：极简矢量风，整帧所有图元汇入一个顶点批、一次 draw call。
 * 世界坐标 y 向下；视口按 contain 适配，世界边界外由延伸的天空与大地填充，
 * 竖屏/宽屏下没有"死"留白。
 *
 * 相对 Canvas 2D 实现的差异（性能动因见 docs/issues/#7.md）：
 * - iOS 的 Canvas 2D 为 CPU 栅格化，逐帧上万段描边是瓶颈；GPU 三角形光栅化消解之
 * - 逐顶点颜色替代"按透明度/温度分桶 Path2D"：透明度连续、无分桶近似误差
 * - 线段宽度几何化（GL lineWidth 多平台恒 1）：stroke 展开为四边形
 * - 径向渐变用扇形逐顶点插值，免每帧 createRadialGradient 与精灵位图缓存
 * - 静态背景不再离屏缓存：天空/地形/光晕每帧重建仅数百顶点，GPU 负载可忽略
 */
export class Renderer {
  private canvas: HTMLCanvasElement
  private gl: GlRenderer | null
  private batch = new MeshBatch()
  private cssW = 0
  private cssH = 0
  private scale = 1
  private ox = 0
  private oy = 0
  /** 地形采样点 scratch：自适应采样按需扩容（最坏 = 视口宽 / 基础步长） */
  private terrainPts = new Float32Array(256)
  /** 各粒子本帧包络与温度档：头部绘制免重复采样 */
  private tracerEnv = new Float32Array(0)
  private tracerTemp = new Uint8Array(0)

  constructor(canvas: HTMLCanvasElement) {
    this.canvas = canvas
    this.gl = GlRenderer.create(canvas)
    if (!this.gl) console.warn('WebGL 不可用，画布将保持空白')
  }

  resize(cssW: number, cssH: number, dpr: number) {
    this.cssW = cssW
    this.cssH = cssH
    // dpr 体现在帧缓冲尺寸上：GL viewport 直接用 canvas 设备像素尺寸
    this.canvas.width = Math.max(1, Math.round(cssW * dpr))
    this.canvas.height = Math.max(1, Math.round(cssH * dpr))
  }

  toWorld(clientX: number, clientY: number): Vec2 | null {
    const rect = this.canvas.getBoundingClientRect()
    if (rect.width === 0 || rect.height === 0) return null
    const wx = (clientX - rect.left - this.ox) / this.scale
    const wy = (clientY - rect.top - this.oy) / this.scale
    const level = this.world
    if (!level) return null
    if (wx < -0.5 || wx > level.w + 0.5 || wy < -0.5 || wy > level.h + 0.5) return null
    return { x: wx, y: wy }
  }

  private world: { w: number; h: number } | null = null

  draw(scene: SceneState) {
    const gl = this.gl
    if (!gl || this.cssW === 0) return
    const { sim, tracers, planeTrail, press, now } = scene
    const { w, h } = sim.level.world
    this.world = sim.level.world

    this.scale = Math.min(this.cssW / w, this.cssH / h)
    this.ox = (this.cssW - w * this.scale) / 2
    this.oy = (this.cssH - h * this.scale) / 2
    const viewL = -this.ox / this.scale
    const viewT = -this.oy / this.scale
    const viewR = viewL + this.cssW / this.scale
    const viewB = viewT + this.cssH / this.scale

    const b = this.batch
    b.reset()
    this.drawSky(b, viewL, viewT, viewR, viewB, h)
    this.drawTerrain(b, sim, viewL, viewR, viewB)
    this.drawSunHalo(b)
    this.drawGoalStatic(b, sim)
    this.drawSun(b, now)
    this.drawGoal(b, sim, now)
    this.drawSources(b, sim, press)
    this.drawTracers(b, sim, tracers)
    this.drawPlaneTrail(b, sim, planeTrail)
    this.drawPlane(b, sim)
    if (press && press.kind === 'place') this.drawPress(b, press, now)
    gl.draw(b, viewL, viewT, viewR, viewB)
  }

  /** 天空渐变锚定世界 0..h；视口超出部分由渐变端色自然延伸 */
  private drawSky(b: MeshBatch, viewL: number, viewT: number, viewR: number, viewB: number, h: number) {
    const topBandEnd = Math.min(viewB, 0)
    if (viewT < topBandEnd) b.rect(viewL, viewT, viewR, topBandEnd, ...SKY_TOP, 1)
    const gradTop = Math.max(viewT, 0)
    const gradBottom = Math.min(viewB, h)
    if (gradTop < gradBottom) b.rectVGrad(viewL, gradTop, viewR, gradBottom, ...SKY_TOP, 1, ...SKY_BOTTOM, 1)
    const bottomBandTop = Math.max(viewT, h)
    if (bottomBandTop < viewB) b.rect(viewL, bottomBandTop, viewR, viewB, ...SKY_BOTTOM, 1)
  }

  /**
   * 地形延伸出世界边界：填满竖屏/宽屏视口。
   * 采样自适应：smoothstep 地形弯曲段每 0.25 单位取点，平坦段合并到 2 单位
   * （转角角度变化超过阈值才加密）——视觉平滑的同时点数更少；
   * 边缘线用斜接折线（batch.polyline），转角处无缝不"断裂"。
   */
  private drawTerrain(b: MeshBatch, sim: LevelSimulation, viewL: number, viewR: number, viewB: number) {
    const ground = sim.level.ground
    // 最坏情况点数 = 视口宽 / 基础步长（含左右延伸端点）
    const need = Math.ceil((viewR - viewL) / TERRAIN_STEP + 3) * 2
    if (this.terrainPts.length < need) this.terrainPts = new Float32Array(need)
    const pts = this.terrainPts
    let n = 0
    pts[n++] = viewL
    pts[n++] = ground(viewL)
    let lastX = viewL
    let lastY = pts[n - 1]
    let lastAng = 0
    let haveAng = false
    for (let x = Math.max(0, viewL) + TERRAIN_STEP; x <= viewR + 1e-9; x += TERRAIN_STEP) {
      const y = ground(x)
      const ang = Math.atan2(y - lastY, x - lastX)
      if (!haveAng || Math.abs(ang - lastAng) > TERRAIN_ANG_TOL || x - lastX >= TERRAIN_MAX_STEP) {
        pts[n++] = x
        pts[n++] = y
        lastX = x
        lastY = y
        lastAng = ang
        haveAng = true
      }
    }
    // 终点对齐视口右缘（末段不足一步时补齐）
    if (viewR - lastX > 1e-6) {
      pts[n++] = viewR
      pts[n++] = ground(viewR)
    }

    for (let i = 0; i + 3 < n; i += 2) {
      const ax = pts[i]
      const ay = pts[i + 1]
      const bx = pts[i + 2]
      const by = pts[i + 3]
      b.tri(ax, ay, bx, by, ax, viewB, ...GROUND_FILL, 1)
      b.tri(bx, by, bx, viewB, ax, viewB, ...GROUND_FILL, 1)
    }
    b.polyline(pts, n, 0.5, ...GROUND_EDGE, 1)
  }

  private drawSunHalo(b: MeshBatch) {
    b.discGrad(SUN_POS.x, SUN_POS.y, SUN_RADIUS * 3, 28, ...SUN, 0.4, ...SUN, 0)
  }

  /** 目标区静态部分：感应虚线圆 + 光柱 */
  private drawGoalStatic(b: MeshBatch, sim: LevelSimulation) {
    const goal = sim.level.goal
    const gy = sim.level.ground(goal.x)
    b.dashRing(goal.x, gy - 2, goal.r, 1.2, 1.4, 0.28, ...GOAL, 0.35)
    b.rectVGrad(goal.x - 1.6, gy - 12, goal.x + 1.6, gy, ...GOAL, 0, ...GOAL, 0.14)
  }

  private drawSun(b: MeshBatch, now: number) {
    const r = SUN_RADIUS + SUN_BREATH_AMP * Math.sin(now / SUN_BREATH_PERIOD)
    b.disc(SUN_POS.x, SUN_POS.y, r, r, 0, 24, ...SUN, 1)
  }

  private drawGoal(b: MeshBatch, sim: LevelSimulation, now: number) {
    const g = sim.level.goal
    const gy = sim.level.ground(g.x)
    const rx = g.r * 0.62 * (1 + 0.06 * Math.sin(now / 320))

    b.disc(g.x, gy - 0.1, rx, 1.0, 0, 24, ...GOAL, 0.3)
    b.ring(g.x, gy - 0.1, rx, 1.0, 0, 24, 0.3, ...GOAL, 0.75)

    // 旗帜：波幅与顺风倾斜随目标处实测风速——风与画面同呼吸
    const air = Renderer.tmpAir
    sim.fluid.sampleVelocity(g.x, gy - 5, air)
    const wind = Math.min(1.4, Math.sqrt(air.x * air.x + air.y * air.y))
    const top = gy - 6
    b.stroke(g.x, gy, g.x, top, 0.34, ...FLAG_POLE, 1)
    const wave = (0.35 + wind * 0.55) * Math.sin(now / 240)
    const lean = 0.1 * wind
    b.tri(g.x, top, g.x + 3.1 + lean, top + 0.9 + wave, g.x, top + 2.1, ...GOAL, 1)
  }

  private drawSources(b: MeshBatch, sim: LevelSimulation, press: PressVisual | null) {
    for (const s of sim.sources) {
      const grabbed = press?.kind === 'remove' && press.sourceId === s.id
      // pop 钳制非负：防任何 future 路径出现 time < born 导致源隐形
      const pop = Math.max(0, 1 - Math.exp(-(sim.time - s.born) * SOURCE_POP_RATE))
      const pulse = 1 + 0.05 * Math.sin(sim.time * 4 + s.id * 1.7)
      const c = s.kind === 'hot' ? HOT : COLD

      if (pop > VISIBLE_ALPHA) {
        b.discGrad(s.x, s.y, SOURCE_GLOW_RADIUS * pop, 20, ...c, 0.32, ...c, 0)
      }

      const coreR = SOURCE_CORE_RADIUS * pop * pulse * (grabbed ? 1.18 : 1)
      b.disc(s.x, s.y, coreR, coreR, 0, 18, ...PAPER, 1)
      b.ring(s.x, s.y, coreR, coreR, 0, 18, 0.34, ...c, 0.9)
      b.disc(s.x, s.y, 0.42 * pop, 0.42 * pop, 0, 12, ...c, 0.95)

      if (grabbed) b.dashRing(s.x, s.y, 2.2, 0.9, 1.1, 0.24, ...INK_DARK, 0.55)
    }
  }

  /** 共享采样临时量：热路径零分配 */
  private static tmpAir = { x: 0, y: 0 }

  /**
   * 风的线条（streakline）+ 头部点。
   * 逐段计算颜色（温度档）与透明度（路程存留 × 生命包络 × 阵风系数），
   * 直接写入顶点——无需 Canvas 2D 时代的两级分桶近似。
   */
  private drawTracers(b: MeshBatch, sim: LevelSimulation, tracers: Tracers) {
    const { trailX, trailY, trailO, trailN, odo, count } = tracers
    const trailLen = tracers.trailLen
    const fluid = sim.fluid
    const air = Renderer.tmpAir
    if (this.tracerEnv.length < count) {
      this.tracerEnv = new Float32Array(count)
      this.tracerTemp = new Uint8Array(count)
    }
    const envs = this.tracerEnv
    const levels = this.tracerTemp

    for (let i = 0; i < count; i++) {
      const env = tracers.envelope(i)
      if (env <= VISIBLE_ALPHA) {
        envs[i] = 0
        continue
      }
      fluid.sampleVelocity(tracers.x[i], tracers.y[i], air)
      const sp2 = air.x * air.x + air.y * air.y
      if (sp2 < CALM_AIR_SPEED2) {
        envs[i] = 0
        continue
      }
      envs[i] = env
      const f = Math.max(-1, Math.min(1, fluid.sampleTemp(tracers.x[i], tracers.y[i]) / T_REF))
      const tl = Math.min(TEMP_LEVELS - 1, ((f + 1) * 2.5) | 0)
      levels[i] = tl

      const n = trailN[i]
      if (n === 0) continue
      const gust = GUST_BASE + GUST_BOOST * Math.min(1, Math.sqrt(sp2) / GUST_FULL_SPEED)
      const c = LINE_COLORS[tl]
      const base = i * trailLen
      const odoI = odo[i]
      let px = trailX[base]
      let py = trailY[base]
      for (let k = 0; k < n; k++) {
        const nx = k + 1 < n ? trailX[base + k + 1] : tracers.x[i]
        const ny = k + 1 < n ? trailY[base + k + 1] : tracers.y[i]
        const a = (1 - (odoI - trailO[base + k]) / TRAIL_FADE) * env * gust
        if (a > VISIBLE_ALPHA) {
          b.stroke(px, py, nx, ny, TRACER_LINE_WIDTH, c[0], c[1], c[2], Math.min(1, a) * LINE_ALPHA_MAX)
        }
        px = nx
        py = ny
      }
    }

    // 头部点在所有线条之后绘制，保证点在线上
    for (let i = 0; i < count; i++) {
      const env = envs[i]
      if (env <= 0) continue
      const tl = levels[i]
      const c = LINE_COLORS[tl]
      b.disc(
        tracers.x[i], tracers.y[i],
        TRACER_HEAD_RADIUS, TRACER_HEAD_RADIUS, 0, 10,
        c[0], c[1], c[2], HEAD_ALPHA[tl] * env,
      )
    }
  }

  /** 纸飞机拖尾：按路程淡出的石墨蓝灰轨迹（停驻时可见），宽度与透明度随存留连续变化 */
  private drawPlaneTrail(b: MeshBatch, sim: LevelSimulation, trail: Trail) {
    const n = trail.count
    if (n === 0) return
    const p = sim.plane
    let px = trail.xAt(0)
    let py = trail.yAt(0)
    for (let k = 0; k < n; k++) {
      const nx = k + 1 < n ? trail.xAt(k + 1) : p.x
      const ny = k + 1 < n ? trail.yAt(k + 1) : p.y
      const ret = trail.retentionAt(k)
      if (ret > VISIBLE_ALPHA) {
        b.stroke(px, py, nx, ny, 0.1 + 0.26 * ret, TRAIL_INK[0], TRAIL_INK[1], TRAIL_INK[2], 0.5 * ret)
      }
      px = nx
      py = ny
    }
  }

  /** 飞机机体局部顶点（未旋转）：机头 / 上翼 / 中折 / 下翼 */
  private static readonly PLANE_LOCAL = [
    [1.85, 0],
    [-1.35, -1.12],
    [-0.6, 0],
    [-1.35, 1.12],
  ] as const
  private planeWorld = new Float32Array(8)

  private drawPlane(b: MeshBatch, sim: LevelSimulation) {
    const p = sim.plane
    // 顶光垂直投影：影子 = 飞机正下方的地面投影（同 x），坡度旋转贴合地形
    const g0 = sim.level.ground(p.x - SHADOW_RADIUS)
    const g1 = sim.level.ground(p.x + SHADOW_RADIUS)
    const slope = Math.atan2(g1 - g0, SHADOW_RADIUS * 2)
    const sy = sim.level.ground(p.x) - SHADOW_LIFT
    b.disc(p.x, sy, SHADOW_RADIUS, SHADOW_RY, slope, 16, ...INK_DARK, SHADOW_MAX_ALPHA)

    const speed = Math.sqrt(p.vx * p.vx + p.vy * p.vy)
    const idle = Math.max(0, 1 - speed / 3)
    // 俯仰随垂直速度：下落低头、上升抬头（低速时收拢为待机摆动）
    const pitch = Math.max(-0.32, Math.min(0.32, p.vy * 0.1))
    const rot = p.angle + 0.07 * Math.sin(p.clock * 1.8) * idle + pitch
    const cos = Math.cos(rot)
    const sin = Math.sin(rot)
    const w = this.planeWorld
    for (let i = 0; i < 4; i++) {
      const [lx, ly] = Renderer.PLANE_LOCAL[i]
      w[i * 2] = p.x + lx * cos - ly * sin
      w[i * 2 + 1] = p.y + lx * sin + ly * cos
    }
    // 机体：机头-上翼-中折 / 机头-中折-下翼
    b.tri(w[0], w[1], w[2], w[3], w[4], w[5], ...PAPER, 1)
    b.tri(w[0], w[1], w[4], w[5], w[6], w[7], ...PAPER, 1)
    const outline = [0, 1, 2, 3, 0]
    for (let i = 0; i < 4; i++) {
      const a = outline[i] * 2
      const c = outline[i + 1] * 2
      b.stroke(w[a], w[a + 1], w[c], w[c + 1], 0.16, ...INK_DARK, 0.5)
    }
    b.stroke(w[0], w[1], w[4], w[5], 0.12, ...INK_DARK, 0.26)
  }

  private drawPress(b: MeshBatch, press: PressVisual, now: number) {
    const progress = Math.min(1, (now - press.start) / LONG_PRESS_MS)
    b.disc(press.x, press.y, 1.5, 1.5, 0, 18, ...HOT, 0.12)
    b.ring(press.x, press.y, 1.5, 1.5, 0, 18, 0.26, ...HOT, 0.75)
    if (progress > 0.04) {
      b.arc(
        press.x, press.y, 2.2,
        -Math.PI / 2, -Math.PI / 2 + progress * Math.PI * 2, 24, 0.4,
        ...COLD, 0.9,
      )
    }
  }
}

```


## Assistant

Now, I'll check particles.ts (Tracers), trail.ts, gl.ts, and controller.ts.


### Tool: ReadFile: sfgame-web/src/sim/particles.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/sim/particles.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/sim/particles.ts`

**Output:**
```typescript
import type { Fluid } from './fluid'
import type { SourceKind, WorldBounds } from './types'

interface SourcePoint {
  x: number
  y: number
  kind: SourceKind
}

/** 每颗粒子记录的轨迹点数（实例可调：移动端缩短以控制描边负载） */
export const TRAIL_LEN = 24
const TRAIL_SAMPLE = 0.45
/** 轨迹点自记录后随路程淡出的总长度 */
export const TRAIL_FADE = 10
/** 生命首尾的淡入/淡出时长（秒），避免粒子凭空闪现 */
const FADE_IN = 0.5
const FADE_OUT = 0.7
/** 随机重生的位置尝试次数（地形下无空位时放弃） */
const RESPAWN_TRIES = 8
const PLUME_PER_STEP = 2
/** 羽流粒子绕源散开的半径 / 生命范围（秒） */
const PLUME_RADIUS = 1.6
const PLUME_LIFE_MIN = 0.9
const PLUME_LIFE_SPAN = 1.2

/**
 * 示踪粒子（拉格朗日）：被动平流于风场，把看不见的气流可视化。
 * 颜色由所在位置的局部温度决定（热偏红、冷偏蓝、中性灰），
 * 透明度随风速增大——"有风的地方才看得见风"。
 *
 * 每颗粒子另有一条按**路程**淡出的短轨迹（streakline），
 * 营造真实的风场线条感；粒子停驻时轨迹不消失。
 */
export class Tracers {
  count: number
  /** 轨迹点数上限（按设备档位传入，控制描边负载） */
  readonly trailLen: number
  x: Float32Array
  y: Float32Array
  life: Float32Array
  maxLife: Float32Array
  /** 各粒子累计路程（里程表） */
  odo: Float32Array
  /** 轨迹点坐标与写入时里程，按 count×trailLen 平铺 */
  trailX: Float32Array
  trailY: Float32Array
  trailO: Float32Array
  /** 各粒子当前轨迹点数（≤ trailLen） */
  trailN: Uint8Array

  private lastOdo: Float32Array
  private world: WorldBounds
  private groundY: (x: number) => number
  /** 采样复用对象：热路径零分配 */
  private air = { x: 0, y: 0 }

  constructor(count: number, world: WorldBounds, groundY: (x: number) => number, trailLen = TRAIL_LEN) {
    this.count = count
    this.trailLen = trailLen
    this.world = world
    this.groundY = groundY
    this.x = new Float32Array(count)
    this.y = new Float32Array(count)
    this.life = new Float32Array(count)
    this.maxLife = new Float32Array(count)
    this.odo = new Float32Array(count)
    this.trailX = new Float32Array(count * trailLen)
    this.trailY = new Float32Array(count * trailLen)
    this.trailO = new Float32Array(count * trailLen)
    this.trailN = new Uint8Array(count)
    this.lastOdo = new Float32Array(count)
    for (let i = 0; i < count; i++) this.respawn(i, true)
  }

  private respawn(i: number, scatter = false) {
    const { w } = this.world
    for (let tries = 0; tries < RESPAWN_TRIES; tries++) {
      const x = 2 + Math.random() * (w - 4)
      const ceil = this.groundY(x) - 1.5
      if (ceil < 3) continue
      const y = 2 + Math.random() * (ceil - 2)
      this.x[i] = x
      this.y[i] = y
      this.maxLife[i] = 2.5 + Math.random() * 4
      this.life[i] = scatter ? Math.random() * this.maxLife[i] : this.maxLife[i]
      this.resetTrail(i)
      return
    }
    this.x[i] = -100
    this.y[i] = -100
    this.life[i] = 0.1
    this.resetTrail(i)
  }

  private resetTrail(i: number) {
    this.odo[i] = 0
    this.lastOdo[i] = 0
    this.trailN[i] = 0
  }

  private recordTrail(i: number) {
    const base = i * this.trailLen
    const len = this.trailLen
    const n = this.trailN[i]
    if (n < len) {
      this.trailX[base + n] = this.x[i]
      this.trailY[base + n] = this.y[i]
      this.trailO[base + n] = this.odo[i]
      this.trailN[i] = n + 1
    } else {
      // 满：整体前移丢弃最旧点
      this.trailX.copyWithin(base, base + 1, base + len)
      this.trailY.copyWithin(base, base + 1, base + len)
      this.trailO.copyWithin(base, base + 1, base + len)
      this.trailX[base + len - 1] = this.x[i]
      this.trailY[base + len - 1] = this.y[i]
      this.trailO[base + len - 1] = this.odo[i]
    }
    this.lastOdo[i] = this.odo[i]
  }

  /** 生命首尾淡入淡出包络（0..1），供渲染层调制整体透明度。 */
  envelope(i: number): number {
    const age = this.maxLife[i] - this.life[i]
    const env = Math.min(1, age / FADE_IN, this.life[i] / FADE_OUT)
    return env < 0 ? 0 : env
  }

  step(dt: number, fluid: Fluid, sources: ReadonlyArray<SourcePoint>) {
    const air = this.air
    for (let i = 0; i < this.count; i++) {
      this.life[i] -= dt
      if (this.life[i] <= 0) {
        this.respawn(i)
        continue
      }
      fluid.sampleVelocity(this.x[i], this.y[i], air)
      const nx = this.x[i] + (air.x + (Math.random() - 0.5) * 0.5) * dt
      const ny = this.y[i] + (air.y + (Math.random() - 0.5) * 0.5) * dt
      const dx = nx - this.x[i]
      const dy = ny - this.y[i]
      this.odo[i] += Math.sqrt(dx * dx + dy * dy)
      this.x[i] = nx
      this.y[i] = ny
      if (this.odo[i] - this.lastOdo[i] >= TRAIL_SAMPLE) this.recordTrail(i)
      const gy = this.groundY(this.x[i]) - 0.4
      if (
        this.y[i] > gy ||
        this.y[i] < 0.5 ||
        this.x[i] < 0.5 ||
        this.x[i] > this.world.w - 0.5
      ) {
        this.respawn(i)
      }
    }

    // 在活跃的源附近补充"羽流"粒子，强化因果感
    if (sources.length > 0) {
      for (let n = 0; n < PLUME_PER_STEP; n++) {
        const s = sources[(Math.random() * sources.length) | 0]
        const i = (Math.random() * this.count) | 0
        const ang = Math.random() * Math.PI * 2
        const rad = Math.random() * PLUME_RADIUS
        const x = s.x + Math.cos(ang) * rad
        const y = s.y + Math.sin(ang) * rad
        if (y > this.groundY(x) - 0.6 || y < 1) continue
        this.x[i] = x
        this.y[i] = y
        this.maxLife[i] = PLUME_LIFE_MIN + Math.random() * PLUME_LIFE_SPAN
        this.life[i] = this.maxLife[i]
        this.resetTrail(i)
      }
    }
  }
}

```


### Tool: ReadFile: sfgame-web/src/ui/gl.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/ui/gl.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/ui/gl.ts`

**Output:**
```typescript
import { MeshBatch, VERTEX_STRIDE } from '../core/batch'

/**
 * WebGL 1.0 最小渲染层：一个程序（位置 + 逐顶点颜色）、一个动态顶点缓冲、
 * 每帧一次 bufferSubData + 一次 drawArrays(TRIANGLES)。
 *
 * 选 WebGL 1 而非 WebGL2/WebGPU：前者在 iOS Safari（iOS 8+）与 Android WebView
 * 上全量可用且由 Metal/厂商驱动 GPU 加速——正是替代 iOS CPU 栅格化 Canvas 2D
 * 的兼容性最优解。alpha:false 画布不透明，天空由场景自身铺满。
 */

const VS = `
attribute vec2 aPos;
attribute vec4 aColor;
uniform vec4 uView;
varying vec4 vColor;
void main() {
  vec2 t = (aPos - uView.xy) / uView.zw;
  gl_Position = vec4(t.x * 2.0 - 1.0, 1.0 - t.y * 2.0, 0.0, 1.0);
  vColor = aColor;
}
`

const FS = `
precision mediump float;
varying vec4 vColor;
void main() {
  gl_FragColor = vColor;
}
`

export class GlRenderer {
  private gl: WebGLRenderingContext
  private canvas: HTMLCanvasElement
  private program: WebGLProgram | null = null
  private buffer: WebGLBuffer | null = null
  private aPos = 0
  private aColor = 0
  private uView: WebGLUniformLocation | null = null
  /** 已上传容量（字节），增长时重建缓冲（bufferSubData 不能扩容） */
  private uploadedBytes = 0
  private lost = false

  private constructor(canvas: HTMLCanvasElement, gl: WebGLRenderingContext) {
    this.canvas = canvas
    this.gl = gl
    // iOS 内存压力可能回收上下文：preventDefault 声明恢复意图，restored 后重建资源
    canvas.addEventListener('webglcontextlost', (e) => {
      e.preventDefault()
      this.lost = true
    })
    canvas.addEventListener('webglcontextrestored', () => {
      this.lost = false
      // 恢复后旧对象已失效：init 失败已清指针，draw 检查 program 自动跳过
      if (!this.init()) console.error('WebGL 资源重建失败，渲染已暂停')
    })
    this.init()
  }

  /** 上下文不可用（极老旧内核）时返回 null，由调用方降级为不渲染。 */
  static create(canvas: HTMLCanvasElement): GlRenderer | null {
    const opts: WebGLContextAttributes = {
      alpha: false,
      antialias: true,
      depth: false,
      stencil: false,
      powerPreference: 'high-performance',
    }
    const gl =
      (canvas.getContext('webgl', opts) ??
        canvas.getContext('experimental-webgl', opts)) as WebGLRenderingContext | null
    if (!gl) return null
    return new GlRenderer(canvas, gl)
  }

  /** 删除旧 program/buffer：恢复后已失效；init 重试前也清掉上次失败的残留。 */
  private dispose() {
    const gl = this.gl
    if (this.program) gl.deleteProgram(this.program)
    if (this.buffer) gl.deleteBuffer(this.buffer)
    this.program = null
    this.buffer = null
    this.uploadedBytes = 0
  }

  /** 失败返回 false（调用方据其决定停用渲染）；失败路径清理已建对象。 */
  private init(): boolean {
    const gl = this.gl
    this.dispose()
    const vs = this.compile(gl.VERTEX_SHADER, VS)
    const fs = this.compile(gl.FRAGMENT_SHADER, FS)
    if (!vs || !fs) {
      if (vs) gl.deleteShader(vs)
      if (fs) gl.deleteShader(fs)
      return false
    }
    const program = gl.createProgram()
    if (!program) {
      gl.deleteShader(vs)
      gl.deleteShader(fs)
      return false
    }
    gl.attachShader(program, vs)
    gl.attachShader(program, fs)
    gl.linkProgram(program)
    // 链接完成后 shader 即可释放（program 持有链接结果）
    gl.deleteShader(vs)
    gl.deleteShader(fs)
    if (!gl.getProgramParameter(program, gl.LINK_STATUS)) {
      console.error('WebGL 着色器链接失败：' + gl.getProgramInfoLog(program))
      gl.deleteProgram(program)
      return false
    }

    this.program = program
    this.aPos = gl.getAttribLocation(program, 'aPos')
    this.aColor = gl.getAttribLocation(program, 'aColor')
    this.uView = gl.getUniformLocation(program, 'uView')
    this.buffer = gl.createBuffer()
    this.uploadedBytes = 0

    gl.disable(gl.DEPTH_TEST)
    gl.enable(gl.BLEND)
    gl.blendFunc(gl.SRC_ALPHA, gl.ONE_MINUS_SRC_ALPHA)
    return true
  }

  private compile(type: number, source: string): WebGLShader | null {
    const gl = this.gl
    const shader = gl.createShader(type)
    if (!shader) return null
    gl.shaderSource(shader, source)
    gl.compileShader(shader)
    if (!gl.getShaderParameter(shader, gl.COMPILE_STATUS)) {
      console.error('WebGL 着色器编译失败：' + gl.getShaderInfoLog(shader))
      gl.deleteShader(shader)
      return null
    }
    return shader
  }

  /**
   * 绘制一批三角形。viewL/T/R/B 为视口的世界坐标范围（y 向下），
   * 着色器把它映射到裁剪空间；dpr 只影响 gl.viewport 的设备像素尺寸。
   */
  draw(batch: MeshBatch, viewL: number, viewT: number, viewR: number, viewB: number) {
    if (this.lost || !this.program || !this.buffer || batch.count === 0) return
    const gl = this.gl
    gl.viewport(0, 0, this.canvas.width, this.canvas.height)
    gl.clearColor(0.992, 0.969, 0.925, 1)
    gl.clear(gl.COLOR_BUFFER_BIT)

    const bytes = batch.count * VERTEX_STRIDE * 4
    gl.bindBuffer(gl.ARRAY_BUFFER, this.buffer)
    if (bytes > this.uploadedBytes) {
      gl.bufferData(gl.ARRAY_BUFFER, batch.data, gl.DYNAMIC_DRAW)
      this.uploadedBytes = batch.data.byteLength
    } else {
      gl.bufferSubData(gl.ARRAY_BUFFER, 0, batch.data.subarray(0, batch.count * VERTEX_STRIDE))
    }

    gl.useProgram(this.program)
    gl.uniform4f(this.uView, viewL, viewT, viewR - viewL, viewB - viewT)
    gl.enableVertexAttribArray(this.aPos)
    gl.enableVertexAttribArray(this.aColor)
    gl.vertexAttribPointer(this.aPos, 2, gl.FLOAT, false, VERTEX_STRIDE * 4, 0)
    gl.vertexAttribPointer(this.aColor, 4, gl.FLOAT, false, VERTEX_STRIDE * 4, 8)
    gl.drawArrays(gl.TRIANGLES, 0, batch.count)
  }
}

```


## Assistant

Next, I'll look at controller.ts to check how everything is wired together. Also trail.ts and bodies.ts briefly. Furthermore, I'll check the quality tier in sf-game.ts (dpr limits, particle count).


### Tool: ReadFile: sfgame-web/src/ui/controller.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/ui/controller.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/ui/controller.ts`

**Output:**
```typescript
import { GameLoop } from '../core/loop'
import { sfx } from '../core/sfx'
import { Tracers, TRAIL_LEN } from '../sim/particles'
import { Trail } from '../sim/trail'
import type { SourceKind } from '../sim/types'
import { LevelSimulation } from '../game/simulation'
import type { HudState, LevelDef, PressVisual, SourcePlacement } from '../game/types'
import { GestureInput } from './input'
import { Renderer } from './render'

/** 示踪粒子分档（由高到低）：帧率压力下逐级降级，保住 60fps。
 * 渲染已 GPU 化，粒子数主要消耗 CPU 采样预算，档位只按模拟成本取舍。 */
const TRACER_TIERS = [400, 320, 240, 180, 128, 96]
const COARSE_TRACER_TIER = 2
const PLANE_TRAIL_MAX_POINTS = 150
const PLANE_TRAIL_SAMPLE = 0.3
const PLANE_TRAIL_FADE = 42
/** 风场采样探针（世界坐标的固定比例点）：底噪声源的均匀覆盖 */
const WIND_PROBE_FX = [0.22, 0.5, 0.78]
const WIND_PROBE_FY = [0.2, 0.35]
const LAND_ALT_BEFORE = 0.9
const LAND_ALT_AFTER = 0.55
const LAND_IMPACT_MIN = 0.8
/** 落地音最小间隔（ms）：贴地滚动颠簸时防止连发叠加成轰隆 */
const LAND_SOUND_MIN_INTERVAL = 150
/** dpr 档位（GPU 栅格化后仅持续过载时的最后手段，上限放宽到 2）。 */
const DPR_TIERS_COARSE = [2, 1.5, 1.0]
const DPR_TIERS_FINE = [2, 1.5]
/** 持续该时长（帧）帧开销超限才降级，避免偶发卡顿误触发。 */
const SLOW_FRAMES_TO_DEGRADE = 150
/** 帧开销 EMA 超该毫秒数视为需要降级（60fps 预算 16.7ms，留余量）。 */
const FRAME_BUDGET_MS = 13
const FRAME_EMA_SMOOTH = 0.95
export interface ControllerEvents {
  onHud(state: HudState): void
  /** 放置被拒绝（预算耗尽或位置无效），供 HUD 抖动提示 */
  onDeny(kind: SourceKind): void
  /** 源集合变化（放置/移除/整体应用后），供 URL 状态双向同步 */
  onSources?(sources: SourcePlacement[]): void
}

/**
 * 游戏控制器：把无头模拟（Simulation）、渲染、手势输入与音效组装起来。
 * Lit 层只负责画布之外的 UI，并通过事件接收 HUD 状态。
 */
export class GameController {
  private sim: LevelSimulation
  private tracers: Tracers
  /** 纸飞机拖尾：按路程淡出，停驻时轨迹保留 */
  private planeTrail: Trail
  private renderer: Renderer
  private loop: GameLoop
  private input: GestureInput
  private events: ControllerEvents
  /** 尺寸适配宿主：画布随其缩放。默认取画布的父元素（light DOM 下可用）。 */
  private host: HTMLElement
  private ro: ResizeObserver | null = null
  private press: PressVisual | null = null
  private lastPhase: 'playing' | 'won' = 'playing'
  private windProbes: { x: number; y: number }[]
  private tmpAir = { x: 0, y: 0 }
  private tracerLevel: number
  private dprTier = 0
  private dprTiers: number[]
  private frameEma = 0
  private slowFrames = 0
  private tickMs = 0
  private lastLand = -Infinity
  private rate = 1
  private fitW = 0
  private fitH = 0
  private world: { w: number; h: number }
  private ground: (x: number) => number

  constructor(
    canvas: HTMLCanvasElement,
    level: LevelDef,
    events: ControllerEvents,
    host?: HTMLElement,
  ) {
    this.events = events
    this.host = host ?? canvas.parentElement ?? canvas
    this.world = level.world
    this.ground = level.ground
    this.sim = new LevelSimulation(level)
    const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    const coarse = window.matchMedia('(pointer: coarse)').matches
    this.dprTiers = coarse ? DPR_TIERS_COARSE : DPR_TIERS_FINE
    // 触屏设备初始粒子档略低（CPU 采样预算），帧率压力下由降级阶梯继续收敛
    this.tracerLevel = reduced
      ? TRACER_TIERS.length - 1
      : coarse
        ? COARSE_TRACER_TIER
        : 0
    this.tracers = new Tracers(
      TRACER_TIERS[this.tracerLevel],
      this.world,
      this.ground,
      TRAIL_LEN,
    )
    this.planeTrail = new Trail(PLANE_TRAIL_MAX_POINTS, PLANE_TRAIL_SAMPLE, PLANE_TRAIL_FADE)
    const { w, h } = level.world
    this.windProbes = WIND_PROBE_FX.flatMap((fx) =>
      WIND_PROBE_FY.map((fy) => ({ x: fx * w, y: fy * h })),
    )
    this.renderer = new Renderer(canvas)
    this.loop = new GameLoop({ tick: this.tick, render: this.render })
    this.input = new GestureInput(canvas, {
      toWorld: (cx, cy) => this.renderer.toWorld(cx, cy),
      hitSource: (w) => this.sim.hitSource(w.x, w.y),
      sourceGrabbed: (s) => {
        this.press = {
          kind: 'remove',
          x: s.x,
          y: s.y,
          start: performance.now(),
          sourceId: s.id,
        }
      },
      sourceReleased: (s) => {
        this.press = null
        if (this.sim.removeSource(s.id)) {
          sfx.remove()
          this.pushHud()
          this.emitSources()
        }
      },
      pressStarted: (w) => {
        this.press = { kind: 'place', x: w.x, y: w.y, start: performance.now() }
      },
      longPressConfirmed: (w) => this.tryPlace(w.x, w.y, 'cold'),
      tap: (w) => this.tryPlace(w.x, w.y, 'hot'),
      pressCancelled: () => {
        this.press = null
      },
      secondaryTap: (w) => this.tryPlace(w.x, w.y, 'cold'),
    })
  }

  start() {
    if ('ResizeObserver' in window) {
      this.ro = new ResizeObserver(() => this.fit())
      this.ro.observe(this.host)
    }
    this.fit()
    window.addEventListener('resize', this.fit)
    this.loop.start()
    this.pushHud()
  }

  setRate(rate: number) {
    this.rate = rate
    this.loop.setRate(rate)
  }

  destroy() {
    this.loop.stop()
    this.input.destroy()
    this.ro?.disconnect()
    this.ro = null
    window.removeEventListener('resize', this.fit)
    // 淡出风声：否则返回标题页后风声残留
    sfx.fadeOutWind()
  }

  restart() {
    this.sim.restart()
    this.planeTrail.clear()
    this.press = null
    this.lastPhase = 'playing'
    this.pushHud()
  }

  private pushHud() {
    this.events.onHud(this.sim.hudState())
  }

  /** 像素比：随档位下调（持续重载时的最后手段），帧缓冲尺寸决定 GPU 光栅化负载。 */
  private pixelRatio(): number {
    return Math.min(window.devicePixelRatio || 1, this.dprTiers[this.dprTier])
  }

  private fit = () => {
    const rect = this.host.getBoundingClientRect()
    const w = Math.round(rect.width)
    const h = Math.round(rect.height)
    if (w === 0 && h === 0) return
    // 尺寸未变则跳过：防止 ResizeObserver 抖动/循环导致画布每帧重建（iOS 已知坑）
    if (w === this.fitW && h === this.fitH) return
    this.fitW = w
    this.fitH = h
    this.renderer.resize(w, h, this.pixelRatio())
  }

  private tryPlace(x: number, y: number, kind: SourceKind) {
    this.press = null
    const source = this.sim.placeSource(x, y, kind)
    if (source) {
      if (kind === 'hot') sfx.placeHot()
      else sfx.placeCold()
      this.pushHud()
      this.emitSources()
    } else {
      sfx.deny()
      this.events.onDeny(kind)
    }
  }

  /** 按目标列表应用源放置（URL 状态变化）。差异算法在 LevelSimulation（无头可测）。
   * silent：挂载时的初始应用，不回写 URL（避免把非规范 URL 规范化成一条多余历史）。 */
  applySources(list: SourcePlacement[], silent = false) {
    // 胜利结算让位：让玩家看到新状态（若仍满足胜利条件，下一帧会自然重新判定）
    if (this.sim.phase === 'won') this.sim.phase = 'playing'
    this.sim.applySources(list)
    this.pushHud()
    if (!silent) this.emitSources()
  }

  private emitSources() {
    // 对齐 URL 精度（1 位小数），保证往返稳定、等值跳过生效
    this.events.onSources?.(
      this.sim.sources.map((s) => ({
        x: Math.round(s.x * 10) / 10,
        y: Math.round(s.y * 10) / 10,
        kind: s.kind,
      })),
    )
  }

  private tick = (dt: number) => {
    const t0 = performance.now()
    const p = this.sim.plane
    const altBefore = this.sim.level.ground(p.x) - p.y
    const vyBefore = p.vy

    this.sim.step(dt)
    this.tracers.step(dt, this.sim.fluid, this.sim.sources)
    this.planeTrail.push(p.x, p.y)

    sfx.updateWind(this.fieldWind(), this.planeRelWind(), dt)
    sfx.setPlanePan(p.x, this.world.w)
    const altAfter = this.sim.level.ground(p.x) - p.y
    if (altBefore > LAND_ALT_BEFORE && altAfter <= LAND_ALT_AFTER && Math.abs(vyBefore) > LAND_IMPACT_MIN) {
      const now = performance.now()
      if (now - this.lastLand > LAND_SOUND_MIN_INTERVAL) {
        this.lastLand = now
        sfx.land(Math.abs(vyBefore))
      }
    }

    if (this.sim.phase !== this.lastPhase) {
      this.lastPhase = this.sim.phase
      if (this.sim.phase === 'won') sfx.win()
      this.pushHud()
    }

    this.tickMs += performance.now() - t0
  }

  private fieldWind(): number {
    const fluid = this.sim.fluid
    let sum = 0
    for (const pr of this.windProbes) {
      fluid.sampleVelocity(pr.x, pr.y, this.tmpAir)
      sum += Math.hypot(this.tmpAir.x, this.tmpAir.y)
    }
    const p = this.sim.plane
    fluid.sampleVelocity(p.x, p.y, this.tmpAir)
    sum += Math.hypot(this.tmpAir.x, this.tmpAir.y)
    return sum / (this.windProbes.length + 1)
  }

  private planeRelWind(): number {
    const p = this.sim.plane
    this.sim.fluid.sampleVelocity(p.x, p.y, this.tmpAir)
    return Math.hypot(p.vx - this.tmpAir.x, p.vy - this.tmpAir.y)
  }

  private render = () => {
    const t0 = performance.now()
    this.renderer.draw({
      sim: this.sim,
      tracers: this.tracers,
      planeTrail: this.planeTrail,
      press: this.press,
      now: performance.now(),
    })
    const cost = performance.now() - t0 + this.tickMs
    this.tickMs = 0
    this.frameEma = this.frameEma === 0 ? cost : this.frameEma * FRAME_EMA_SMOOTH + cost * (1 - FRAME_EMA_SMOOTH)
    // 帧预算按速率放大：倍速下每帧本就要消化 rate×tick，慢帧是预期而非故障；
    // 且主导成本（流体）不可降级，阶梯在高速率下只会白降画质、救不回帧率
    if (this.frameEma > FRAME_BUDGET_MS * this.rate) {
      // 先降示踪粒子（观感影响小），粒子到底仍不够再降 dpr
      if (++this.slowFrames > SLOW_FRAMES_TO_DEGRADE) {
        this.slowFrames = 0
        if (this.tracerLevel < TRACER_TIERS.length - 1) {
          this.tracerLevel++
          this.tracers = new Tracers(
            TRACER_TIERS[this.tracerLevel],
            this.world,
            this.ground,
            TRAIL_LEN,
          )
        } else if (this.dprTier < this.dprTiers.length - 1) {
          this.dprTier++
          this.fit()
        }
      }
    } else {
      this.slowFrames = 0
    }
  }
}

```


### Tool: ReadFile: sfgame-web/src/sim/trail.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/sim/trail.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/sim/trail.ts`

**Output:**
```typescript
/**
 * 路程驱动的拖尾轨迹（拉格日记录）。
 *
 * 与"按时间淡出"的常规拖尾不同：每个轨迹点记录写入时的里程表（odometer），
 * 淡出程度 = 物体自该点之后走过的路程 / fadeDist。
 * 由此物体停住时可见轨迹不会消失，运动越快旧轨迹被"甩掉"得越干脆。
 *
 * 环形缓冲、等距采样（每走过 sampleDist 记一点），按从旧到新索引（0 为最旧）。
 * 无 DOM 依赖，可无头测试。
 */
export class Trail {
  readonly maxPoints: number
  readonly sampleDist: number
  readonly fadeDist: number
  /** 累计路程（世界单位） */
  odometer = 0
  /** 缓冲中的点数（≤ maxPoints） */
  count = 0

  private xs: Float32Array
  private ys: Float32Array
  /** 各点写入时的里程 */
  private os: Float32Array
  private head = 0
  private lastX = Number.NaN
  private lastY = Number.NaN
  private lastOdo = 0

  constructor(maxPoints: number, sampleDist: number, fadeDist: number) {
    this.maxPoints = maxPoints
    this.sampleDist = sampleDist
    this.fadeDist = fadeDist
    this.xs = new Float32Array(maxPoints)
    this.ys = new Float32Array(maxPoints)
    this.os = new Float32Array(maxPoints)
  }

  /** 记录物体当前位置；路程未达采样间隔则只推进里程表。 */
  push(x: number, y: number) {
    if (Number.isNaN(this.lastX)) {
      this.lastX = x
      this.lastY = y
      this.record(x, y)
      return
    }
    const dx = x - this.lastX
    const dy = y - this.lastY
    const d = Math.sqrt(dx * dx + dy * dy)
    if (d === 0) return
    this.odometer += d
    this.lastX = x
    this.lastY = y
    if (this.odometer - this.lastOdo >= this.sampleDist) this.record(x, y)
  }

  private record(x: number, y: number) {
    if (this.count < this.maxPoints) {
      const i = this.count++
      this.xs[i] = x
      this.ys[i] = y
      this.os[i] = this.odometer
    } else {
      // 已满：覆写最旧槽位（head），head 前移指向新的最旧点
      const i = this.head
      this.xs[i] = x
      this.ys[i] = y
      this.os[i] = this.odometer
      this.head = (this.head + 1) % this.maxPoints
    }
    this.lastOdo = this.odometer
  }

  private indexOf(k: number): number {
    return this.count < this.maxPoints ? k : (this.head + k) % this.maxPoints
  }

  xAt(k: number): number {
    return this.xs[this.indexOf(k)]
  }

  yAt(k: number): number {
    return this.ys[this.indexOf(k)]
  }

  odoAt(k: number): number {
    return this.os[this.indexOf(k)]
  }

  pointAt(k: number): { x: number; y: number; odo: number } {
    const i = this.indexOf(k)
    return { x: this.xs[i], y: this.ys[i], odo: this.os[i] }
  }

  /** 轨迹点当前的存留比例：1 最新，≤0 应丢弃（物体已走过 fadeDist 以上）。 */
  retentionAt(k: number): number {
    const r = 1 - (this.odometer - this.odoAt(k)) / this.fadeDist
    return r < 0 ? 0 : r > 1 ? 1 : r
  }

  clear() {
    this.count = 0
    this.head = 0
    this.odometer = 0
    this.lastOdo = 0
    this.lastX = Number.NaN
    this.lastY = Number.NaN
  }
}

```


## Assistant

Now I have the full picture. Let me analyze the per-frame cost on iPhone:

**Per tick (60Hz):**
1. `LevelSimulation.step`:
   - For each source (max ~6): `addHeat` over the radius 3.4/0.75 ≈ 4.5 cells → about 80 cells each, cheap.
   - `fluid.step`:
     - applyBuoyancy: ~7.4k inner cells, with solid check
     - applyVorticity: 2 passes over ~7.4k, with sqrt + abs
     - 3× MacCormack advection: each = 2 advectPass (~7.4k, each with floor + bilinear, ~30 ops) + 1 correction pass (~7.4k, with 3×3 min/max = 9 reads + branch). So per field: ~22k heavy cell ops, ×3 fields = 66k.
     - project: div pass 7.4k + 12 GS iterations × 7.4k (each ~10 ops + 4 solid checks) + gradient pass 7.4k ≈ 104k cell ops.
     - enforceBoundary: full-array scan of 7.6k, with branches.
   - Total ≈ ~200k cell ops per step, heavy index arithmetic. Realistically ~2–4ms/frame on desktop V8/JSC with JIT, maybe 5–10ms on A-series chips with JIT. But in WKWebView (no JIT — e.g., in-app browsers, WeChat etc.) could be 3–10× slower → 15–40ms. Hmm.
   - `stepBody`: cheap (a few sampleVelocity calls + ground check).
2. `tracers.step`: 240–400 particles, each with sampleVelocity + groundY eval + random. groundY is a JS closure with smoothstep — no problem. ~0.1–0.3ms.
3. `sfx.updateWind`: let me check — WebAudio parameter updates, should be cheap but verify.

**Per render frame (60Hz):**
1. `Renderer.draw`: builds the vertex batch:
   - drawTracers: for each of 240–400 particles, up to 24 trail points → stroke per visible segment. Worst case ~400×24 ≈ 9600 stroke calls → 6 vertices each → 57.6k vertices. Plus per-segment sampleVelocity + sampleTemp calls (~2 calls per particle).
   - Wait — actually per particle: sampleVelocity once + sampleTemp once (at head position), then loop over trail segments. OK.
   - Each `stroke` call: sqrt + 6 pushes × 6 writes = 36 array writes. 9600 strokes ≈ 350k float writes + function call overhead. Hmm, function call overhead of `push` (private method call per vertex!) — 57.6k push calls per frame just for tracer lines. Plus plane trail ≤150 segments, terrain, sources...
   - Total vertices probably 60–80k → upload 80k × 6 × 4B ≈ 1.9MB per frame via bufferSubData. On iPhone GPU this upload is fine (Metal UMA), but 60fps × 2MB = 120MB/s memcpy — no problem.
   - CPU cost of batch building in JS: probably 1–3ms on desktop; slower on iOS.

2. `gl.draw`: single drawArrays of ~60–80k vertices, ~20–27k triangles, full-screen overdraw? Sky/terrain cover the whole screen, alpha blending on. Fillrate: at dpr 2 on iPhone (e.g., 1170×2532 ≈ 3MP) — trivial for A-series GPU.

**Where does iOS actually hurt?**

The boss said "部分ok" — the WebGL refactor is accepted, but iOS still needs work. They suspect "物理模拟数值计算性能较差" (poor physics numerical computation performance). Let me think about what dominates on iPhone:

- Fluid solver: ~200k cell ops × 60fps = 12M ops/s. JSC with JIT on iOS Safari 17.4+: A-series chips are fast; probably 3–6ms per frame. On older iPhones (A12/A13), 6–10ms is possible. This is the single largest CPU cost. On 120Hz ProMotion devices, rendering is capped at 60 but simulation is still 60 — fine.
- WKWebView without JIT: everything 3–10× slower; fluid could hit 20–50ms — unplayable. But does the boss target WebView on iOS? They mentioned "Android Webview 性能好很多" — so the app runs in WebView on Android, and presumably also on iOS (e.g., embedding in some app? WeChat?). On iOS, all browsers and WebViews are WKWebView; JIT was only allowed for Safari browser (iOS 17.4+) — no wait: does JIT now also work in WKWebView on iOS 17.4+? Let me recall: WebKit's JIT was enabled for Safari in iOS 17.4 due to EU DMA... actually the DMA required allowing alternative browser engines; did Apple also enable JIT for WKWebView on iOS 17.4? Hmm. My recollection: as of iOS 17.4, JavaScriptCore JIT was enabled for all WKWebView uses (to comply with fairness requirements). Before that, only Safari had JIT. Let me verify on the web later — important for the decision.

Either way, first principles: **the biggest remaining CPU cost on iOS is the fluid solver in JS**. The boss hinted at JS code generation / wasm (MoonBit) / WebGL. WebGL GPGPU for the solver is complex (ping-pong FBOs, float textures — does iOS support rendering to OES_texture_float? Historically iOS supported half-float color buffers via EXT_color_buffer_half_float; full float rendering in WebGL1 on iOS is not allowed). GPGPU fluid on WebGL1 on iOS is possible (well-known technique, e.g., Pavel Dobryakov's WebGL fluid simulation — works on iOS) but with half-float textures and many passes. That adds significant complexity. Also 7.5k cells is small; GPU round-trip latency per pass (12+ texture ping-pongs per frame) might not even win.

**WASM via MoonBit**: the boss explicitly suggested MoonBit ("可以用 moonbit 语言"). WASM performance in iOS Safari is excellent (near-native, no JIT needed — wasm has its own tiered compiler, allowed even in contexts where JS JIT isn't; WKWebView supports WebAssembly). Compiling the solver to wasm would probably give 2–4× on desktop, and possibly 5–20× on JIT-less interpreters. This is the most robust solution for "iPhone users (and in-app WebViews) also get a good experience".

But: complexity. Adding MoonBit toolchain to build: install moon (npm package? `moonbit` core via curl script; is there an npm wrapper?). Hmm. MoonBit installation: `curl -fsSL https://cli.moonbit-lang.com/install/unix.sh | bash` — installs `moon`. Is there also an npm package "@moonbit/moonc"? Come to think of it... MoonBit provides moonc via npm? I recall an npm package called "moonbit" but... not sure. vite plugin: `vite-plugin-moonbit`? Not sure about maturity.

Complexity concern: AGENTS.md/development.md emphasizes 少即是多 (less is more), avoiding dependencies. The previous decision rejected WASM citing complexity. The boss's "部分ok，需要再深思、查资料和实践" + re-listing wasm/moonbit suggests they want me to seriously reconsider — perhaps they tested on iPhone and it's still not smooth enough.

Alternative: **deeply optimize the JS solver itself**. Before going wasm, let me estimate how much headroom there is:

Current cost structure per step (nx=101, ny=75, inner ~99×73=7227 cells):
1. buoyancy: 1 pass, cheap (1 mul, 1 sub per cell)
2. vorticity: pass 1 computes curl (4 reads, 2 subs, 2 divs), pass 2 (inner 97×71=6887): 4 abs, 2 subs, 2 divs, sqrt, normalize, 2 muls, 2 adds — moderately heavy
3. MacCormack × 3 fields: each field: advectPass×2 (each cell: bounds clamp, floor×2, bilinear 4 reads/8 muls) + correction (each cell: 3×3 = 9 reads with branch min/max, plus correction arithmetic). Correction pass is expensive: 9 reads + ~10 comparisons.
   - Optimization: correction's inner 3×3 loop uses `ii + jj * nx` recomputed; can be unrolled with row pointers. Modest gain.
   - Bigger: `advectPass` uses u0[idx], v0[idx] — note MacCormack forward/backward uses same velocity field. Two passes per field = 6 advectPass total per step.
4. project: div pass + 12 GS iterations + gradient pass. GS is 12/27 of the total.
   - **Key optimization**: warm-started GS converges fast; 12 iterations for this game's gentle flow may be overkill. But changing iteration count changes physics numerics → breaks solutions tests (±2s). Risky but testable. The tests guard within ±2s of recorded clear time, so slight numeric drift is tolerable. Hmm, but "不降低原有任何好的体验" — physics behavior changes might change gameplay feel. Reducing iterations slightly increases divergence → velocity damping, plume behavior changes. ±2s tests would catch gross changes.
   - GS inner loop: each cell has 4 `solid[neighbor]` checks → branchy. Could precompute neighbor solid masks into typed arrays, or use the trick: p for solids is pinned to 0? No — current code uses p[idx] itself for solid neighbors (Neumann BC). Instead, we could precompute per-cell neighbor indices with clamped "mirror" addressing and store them. Or, flatten solid into a float mask and do branchless selection: pL = p[idx-1] * liquidL? That changes semantics: currently pL = solid ? p[idx] : p[idx-1]. Multiplicative alternative is different. Keep branch but precompute `solidL/R/U/D` bytes? 4 extra Uint8Array reads instead of idx arithmetic + branch... actually current code already indexes solid[idx±1] directly (1 read each), fine.
   - Red-black GS parity loop overhead: 12 iterations × 2 parities × row loop. OK.

5. enforceBoundary: loops over all cells including interior with branches — optimize: skip interior rows if no solids? Per-row solid count? Simpler: boundary enforcement only needed where solids exist. Could precompute an Int32Array of solid cell indices once in setGroundMask → loop just those (ground cells ≈ 1/4 of grid? Level 1: ground descends to bottom-right; solid count probably 1.5–2k). And solid cells are already set to 0 each pass anyway (advect etc sets dst=0 for solids), so enforceBoundary is largely redundant... but keep it cheap via index list.

Micro-optimizations like these might shave 20–40% off the solver in JS. Good, but if the real problem is no-JIT on iOS WebView, it won't transform the experience.

**The decisive question: what is actually slow on the boss's iPhone?**

Evidence:
- Original #7 report: "ios Safari 可能由于 js 引擎或 jit 限制有更差的性能（目前发现可能是物理模拟数值计算性能较差）" — they suspect the physics.
- After WebGL refactor, marked "部分ok" — rendering fixed but overall still not great.

Since I can't test on iPhone (per AGENTS.md no E2E/CU experiments allowed), I should:
1. Build a headless benchmark for the sim loop that runs in bun (JSC with JIT — similar to Safari with JIT) and measures ms/step. Also measure in Node? Not necessary.
2. Reason carefully about iOS: Safari with JIT (iOS 17.4+) vs older devices without JIT.
3. Attack the largest cost: fluid solver.

**Strategic options for the solver:**

A. **Hand-optimize the JS solver** (medium effort, robust):
   - Precompute solid cell index list for enforceBoundary.
   - Skip solid checks in GS via precomputed per-cell neighbor "address" arrays? Actually branch prediction on nearly-constant solid pattern is good; the 4 solid[] reads per GS cell are 25% of its memory traffic. Alternative: pad the grid with 1-cell solid border (already has one) and set p=something for solid cells so the Neumann condition holds automatically? Classical trick: for Neumann BC, set p_solid = p_interior_neighbor — but that's implicit. Another classical trick: precompute a `UInt32Array` of 4 neighbor indices per cell with clamping for solids... memory 7.5k×4×4 = 120KB, fine; replaces branch with indirection — probably a wash or loss.
   - **Reduce GS cost with multigrid?** Complex. No.
   - **Half-resolution pressure solve?** Changes physics noticeably. No.
   - **Iteration reduction 12→10 or adaptive**: measurable win (GS is ~45% of step cost). Test with solutions test.
   - Unroll MacCormack correction 3×3 min/max with row base indices.
   - Replace `Math.floor` with `| 0` where positive. Minor.
   - Estimate: total 20–35% solver speedup.

B. **Move solver to Web Worker** (medium–high effort, robust for perceived smoothness):
   - Simulation in worker, postMessage transfer Float32Arrays (transferable, zero-copy) to main thread for rendering + plane state. Latency: 1-frame pipeline. Complexity: sync of source placement, restart semantics, phase/win detection crossing thread. Architecturally significant change — violates "less is more"? But it's a Web-native capability. However, iOS WKWebView: Workers work (since iOS 5). And JIT-less context: worker JS is also interpreter-only (same speed), but off the main thread → input/render stays smooth; simulation runs at its own pace with fixed step accumulator. This converts jank into potentially slow-but-smooth sim... but if sim can't keep up with 60Hz, gameplay slows. Hmm, actually if we have a fixed-step loop with acc capped at MAX_FRAME, sim falling behind = slow-motion. Better than jank? For "好的体验" probably yes. But big complexity + subtle sync bugs.
   - Also: rendering needs fluid fields to draw tracer particles (sampleVelocity for 400 particles + tracer trail colors need sampleTemp). Transferring u, v, t (3×7.5k×4B = 90KB) per frame, zero-copy with transferables but requires re-transfer back (ping-pong buffers). Doable but fiddly.

C. **WASM solver (MoonBit)** (high effort, best CPU speedup, boss's hint):
   - 2–4× solver speedup on JIT-ed engines; 10×+ on interpreter. Deterministic, keeps physics identical (bit-exact? float32 same ops → same result if op order preserved; JS already uses Float32Array, wasm f32 ops — should be nearly bit-exact, solutions tests unaffected).
   - Toolchain: MoonBit. Let me investigate the integration: is there `moonbit` on npm? What I actually recall: official install is `curl ... bash` (moonup). Also `npm i -g moonbit`? Not sure. vite needs to run `moon build` to produce .wasm and glue JS. MoonBit's wasm output is self-contained (no runtime), with JS bindings generated via `moon generate`? MoonBit's wasm build produces `target/wasm-gc/release/build/<pkg>/<pkg>.wasm` + a JS... MoonBit wasm bindings: can use `@moonbit/wasm`? Hmm, need to research. Also wasm-gc vs wasm: does MoonBit target wasm-gc by default now? For broad iOS compat, plain core wasm (MVP) is needed — wasm-gc requires iOS 18.2+/Safari 18? wasm-gc shipped in Safari 18.3? Actually V8/SpiderMonkey shipped wasm-gc in 2023; Safari shipped wasm-gc in Safari 18.2 (December 2024)? Hmm. To be safe, target classic wasm32. MoonBit backend: `--target wasm` (classic) vs `wasm-gc`. I think classic wasm32 is still supported.
   - Data marshalling: allocate grid buffer in wasm memory, expose u/v/t via JS views over wasm memory; JS calls `step(dt)` and reads fields directly from the same ArrayBuffer (no copy). sampleVelocity can stay in JS (read wasm memory view) — per-frame sampling (~400×2 + misc) is cheap in JS; only the heavy per-cell loops move to wasm. Or move sampling too (export function call overhead per call ~ns, fine).
   - Build integration: run `moon build` via vite plugin or npm script (prebuild + watch). Dev dependency: needs `moon` CLI on dev machines. CI? No CI here. The boss themselves would need moon installed to rebuild... friction. Alternative: commit prebuilt .wasm binary? Binary in git — ugly but pragmatic? Hmm.
   - This is "引入更多复杂度" — but the boss explicitly said wasm is OK ("可以用 moonbit 语言"), and now says "大胆执行" (execute boldly).

D. **WebGL GPGPU solver**: complex, fragile on old iOS, latency. No.

E. **Algorithmic reduction**: smaller grid? cell 0.75→1.0 = 76×56 = 4256 cells (44% fewer). Changes physics/feel — solutions tests would catch time drift; ±2s tolerance might absorb it but "不降低原有任何好的体验" is at risk. Keep.

**Decision framework:**

First principle: iPhone users deserve smooth gameplay. The dominant cost on iOS is the fluid solver on main thread. The rendering is GPU now.

I think the correct move is a **layered attack**:
1. **JS solver optimization** (low risk, immediate 20–35%): unrolled min/max, precomputed solid lists, floor→bit tricks, hoisting, reducing GS memory traffic. Keep numerics bit-identical where possible (fluid tests + solutions tests guard).
2. **WASM via MoonBit for the solver** — the transformative move for no-JIT iOS context, explicitly endorsed by the boss. Design with graceful fallback: if wasm load fails, use JS solver (both implementations share config; Fluid class picks engine). Keep both? "Less is more" — two implementations is maintenance burden. Hmm. But robustness: wasm load is async; game can start with JS and switch when ready? That adds complexity.

Wait — actually let me reconsider whether WASM is really the right call, given I can't test on iPhone and the boss said "深思...查资料和实践". Maybe I should do the research first (WebGPU/WASM status on iOS, JIT facts), run benchmarks, then decide.

Also: **is the physics really the remaining bottleneck after WebGL?** Let me reconsider other suspects:

- **Tracer drawing cost on CPU**: 400 particles × 24-point trails: per frame, per particle: envelope + sampleVelocity + sampleTemp + loop over up to 24 segments, each with stroke() (private push call sqrt + 6×). ~9600 stroke calls ≈ 57.6k push() calls ≈ 345k array writes. Plus batch.ensure() check. On desktop ~1–2ms; on iPhone maybe 3–5ms in JS. Not negligible! Optimization: specialize tracer segment drawing — write vertices directly into batch array without push overhead, or reduce trail points on coarse tier.
  - Actually wait, in coarse tier trailLen 24 × 240 particles = 5760 segments — still large.
  - Big idea: **polyline batching per particle**: stroke() per segment recomputes normal etc. Can't use miter polyline (random walks fold back — noted in comment). But can inline segment→quad expansion with direct array writes: eliminate 6 push calls per segment. Probably 30–40% faster in that pass.
  - Bigger idea: **move trail rendering to GPU?** Trail data is already in a Float32Array (trailX/trailY/trailO); we can upload as attributes and build quads in vertex shader with geometry shader tricks (WebGL1 has no geometry shader; standard trick: instancing — ANGLE_instanced_arrays is broadly supported including iOS; or per-segment 6-vertex expansion with duplicated data). Instancing: 240×23 ≈ 5520 instances × 6 floats (x0,y0,x1,y1,width,alpha) = 132KB upload — same order as current vertex upload (57.6k verts × 24B = 1.38MB!) — 10× less upload data and zero CPU geometry work. Requires ANGLE_instanced_arrays (iOS 8+, basically universal). Hmm, this is real complexity: second shader program, per-instance attribute layout, still need per-segment color (temp level) — pass color per instance. Feasible but... let me weigh it. Current CPU cost of tracer drawing is maybe 1–3ms on iPhone; worth doing? Probably medium value. Keep as option.

- **dpr**: iPhone dpr is 3 but we cap at 2. Fullscreen 1170×2532 at dpr2 → 2340... no wait: iPhone 15 css 393×852, dpr 3 → 1179×2556 device px; capped dpr 2 → 786×1704 ≈ 1.34MP. Fine for GPU.

- **120Hz ProMotion**: render is capped at 60 via `now - lastRender >= SIM_DT_MS` logic. OK.

- **sfx.updateWind** — check cost; probably fine.

Let me also look at the sim loop frequency: `SIM_DT = 1/60`, at rate 1 → 1 tick per frame. Fluid step is once per tick. OK.

**Another big idea — reduce fluid cost by solving pressure every other tick?** Introduces visible artifacts (divergence → compressive look). Risky for feel.

**Warm start iterations**: 12 iterations with warm start is probably converging in ~6; can adapt: monitor max delta, early-exit when small. Early-exit changes numerics only when it kicks in... bit-exactness vs adaptive: fine if we keep same iteration order when not early-exiting. Solutions tests would tell us. This is potentially a big win: if we usually early-exit at 6–8 iterations, GS cost drops 30–50%. But measuring max delta requires extra reduction pass... can track max delta during GS sweep itself (accumulate max |p_new - p_old|). Zero extra pass. Then early-exit threshold tuning. Hmm — but warm-start GS: delta is small from start since p from last frame is close; early-exit might trigger almost immediately at loose thresholds, reducing quality. The current fixed 12 iterations might already be near-minimal. Let me benchmark first, then decide.

OK, plan is clear. Now let me think what "深思熟虑" deliverable looks like:

**Phase 0: Instrument (evidence)**
- Write a headless benchmark (bun script or vitest bench): measure fluid.step(), tracer step, batch construction cost with realistic config, on this machine (M-series, JSC via bun ≈ modern Safari JIT). Establish per-component budget in ms. This grounds every optimization decision. Not a "test" per se; a script under tests/ or scripts/. AGENTS.md says vitest for tests; long simulations need explicit timeouts. Could make it `tests/perf.bench.ts`... does vitest have bench via bun? `bun run test` = vitest; vitest bench is separate (`vitest bench`). Simpler: standalone bun script `scripts/bench.ts` run manually. Hmm, but keep repo tidy: maybe place it as a test file that asserts performance ceiling? Perf assertions are flaky across machines. Script is better. Actually — let me make it a vitest test with generous ceiling that doubles as regression guard? No — flaky. Script.

**Phase 1: JS solver optimization (bit-identical target)**
Micro-optimize fluid.ts inner loops:
- advectPass: hoist row arithmetic, replace Math.floor with bitwise (x is clamped ≥0.5 so `x | 0` works; watch out for negatives — clamp first so positive, ok), unroll bilinear weights (precompute w0..w3).
- MacCormack correction: unroll 3×3 min/max with 3 row bases.
- project GS: hoist solid/p/div references; parity row-start computation is fine; consider storing `h2*div[idx]` as pre-multiplied field (save 1 mul per GS cell per iteration → 12 muls per cell per step). Do it: `divH2[idx] = h2 * div` once, GS reads it.
- enforceBoundary: precomputed solid index list.
- vorticity: fine; maybe `1/h2` multiplication instead of division. Divisions: `/ h2` → `* inv2h`. JSC probably optimizes but make explicit.
- buoyancy: skip solid branch by iterating solid list? No — buoyancy touches all interior cells (v -= k*t where t is mostly small; solid cells have t=0 anyway! Because advection zeros solid cells and enforceBoundary zeros t on solids). Wait: applyBuoyancy skips solids but t[idx] is already 0 on solids → `v[idx] -= k*0` is harmless... but v[idx] is also 0 on solids and stays 0 — harmless either way. So solid check can be dropped entirely from buoyancy (operates on all interior cells, solids have t=0 → no-op). Saves branch + load. Same logic: after advection (dst=0 for solids) and boundary enforcement, the invariant solid ⇒ (u,v,t)=0 holds across step. applyVorticity reads u,v neighbors — solid neighbors have 0 values but curl computation uses differences; currently we skip solid cells (curl=0) and second pass skips solids. Removing branch changes results? First pass: for non-solid cells curl is computed from neighbors which are 0 for solids — currently same (reads u/v directly). For solid idx, curl[idx] is set to 0; second pass reads curl[idx±1/nx] for non-solid cells — if a neighbor is solid, its curl is... currently set to 0 explicitly. If we don't zero it, garbage from previous frame? curl is overwritten each step only in pass 1 loop over interior (j 1..ny-2, i 1..nx-1); solid cells within interior are currently explicitly set to curl=0. If we remove that, pass 2 would read stale curl for solid cells → non-physical force near terrain. Keep the zeroing, but can we combine: keep pass 1 as-is (branch is needed for zeroing solid curl). Or zero curl once via solid list before pass 1, then branchless pass 1... same cost. Leave vorticity largely as-is, minor tweaks.

Estimated improvement: 15–30% on fluid.

**Phase 2: WASM decision** — research + prototype.
Given the boss's explicit hint + "大胆执行", lean towards implementing a MoonBit wasm solver **as an opt-in accelerator with JS fallback**, but let me first check toolchain reality (moon CLI availability via bun/npm, build integration in vite, wasm target compatibility with iOS). If toolchain is too heavy for this repo, fall back to AssemblyScript? No — boss said MoonBit. Or hand-written WAT? No.

Actually — wait. Let me think about **JS code generation** option the boss mentioned: "js 代码生成" e.g., generating specialized unrolled code as string, new Function(). Marginal gain over hand-optimized JS. Skip.

Also reconsider **Worker** option: if wasm works, worker not needed for speed. Worker also helps to not block input during heavy frames. If wasm brings step to <2ms even on slow engines (wasm near-native everywhere), main-thread is fine.

**Phase 3: render CPU optimizations**
- Inline tracer segment expansion (bypass push()); direct writes.
- Consider reducing default tracer trail length on coarse pointer (24→18?) — visual regression risk; boss said don't reduce existing good experiences. Trails were tuned in #4. Keep length; optimize the code path instead.
- `disc`/`ring` for head dots: 400 particles × disc(10 segments) = 400×10×3 = 12k verts + cos/sin ×400×11. Precompute unit circle tables per segment count! cos/sin table for seg=10, 18, 20, 24, 28 — significant CPU savings and easy. Actually disc() recomputes cos(th)/sin(th) for each disc. With ~420 discs per frame × ~12–30 segments × 2 trig calls = ~15k trig calls per frame. Precomputed table = big easy win.
- Similarly source rings/arcs are few — negligible.

**Phase 4: adaptive quality improvements**
Current degradation ladder lowers tracer count after 150 slow frames (EMA 13ms × rate) — 2.5 seconds of jank before action. With better measurement, we can also degrade *render* quality (trail points) before particle count. Minor tuning.

Additionally: **consider 60Hz simulation vs 120Hz render interpolation**: on ProMotion iPhones, we render at 60; visually fine.

Another possibility: **run fluid step only when needed**? No — it's always evolving.

Let me quickly verify sfx.ts and bodies.ts too before starting. Also check package.json scripts and test setup, pitfalls skill (D section) — AGENTS.md says to read pitfalls before perf work.

Research tasks (web):
1. iOS Safari / WKWebView JIT status for JS and Wasm (2025–2026): Safari 17.4+ JIT; WKWebView JIT status; wasm support quality (Safari's wasm is LLVM-based tiering, fast on iOS 15+? Wasm in Safari historically slower than V8 but good; iOS 17 improved? There was a big wasm perf push in WebKit around 2021 (iOS 15) — "up to 3.5×". By 2024–25 wasm in Safari is solid, including in WKWebView (wasm doesn't require JIT entitlement — Safari's wasm compiler is a non-JIT AOT-like compiler... works in WKWebView, yes).
2. MoonBit: wasm target status, how to build + load in vite project, npm availability of toolchain, wasm32 vs wasm-gc output and iOS compat.

Kick off: read remaining files (sfx.ts, bodies.ts, package.json, pitfalls D), then benchmarks, then web research (can we parallelize with agents? web_fetch I can do myself; moonbit docs probably via web). 

Given the scale, let me set up a todo list.

Todos:
1. Read pitfalls skill + remaining core files (sfx/bodies) to complete the picture
2. Build headless perf benchmark (fluid/tracers/batch per-component ms) to establish evidence
3. Web research: iOS JS JIT/WKWebView and wasm status; MoonBit wasm build integration
4. Optimize JS solver and rendering CPU hot paths (bit-identical guard via tests)
5. Introduce MoonBit wasm solver (JS fallback) depending on research findings
6. Full verification bun run check + update docs/issues/#7.md report

Let me start. First, read pitfalls SKILL.md (sections D, G, H), sfx.ts, bodies.ts, package.json.


## Assistant

代码已核实完毕，当前状态与记忆一致（WebGL 渲染 + JS 流体求解器）。现在开始进行深度诊断。先补齐信息，然后再下判断。


### Tool: ReadFile: skills/pitfalls/SKILL.md
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/skills/pitfalls/SKILL.md"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/skills/pitfalls/SKILL.md`

**Output:**
```markdown
---
name: pitfalls
description: 本项目（Lit 3 + Canvas 2D + vite/bun 单页游戏）实踩并验证过的疑难杂症避坑手册。当遇到以下信号时使用：布局右溢/间距消失、居中元素宽度只有一半、滚动条异常、刷新后状态丢失、后退/前进不生效、iOS 或移动端卡顿/无声/手势异常、模拟时间判定失效、长测试超时。每条含症状、根因、修法、可提前识别的信号。修问题先查本手册，再查网络。
---

# 疑难杂症避坑手册

本项目实踩并验证过的坑。每条按「症状 → 根因 → 修法 → 信号」组织。
新增条目：在对应分类追加 `### Xn` 小节，并在「快速检索」补一行 `症状 → Xn`。

## 快速检索（症状 → 条目）

- 布局右溢、右侧间距消失 → A2、A4
- 居中元素（提示条/弹层）宽度只有容器一半 → B4
- 顶部滚不回去、底部间距不可见 → A3
- 刷新后页面/状态丢失 → C6
- 后退/前进"没反应"、源删不掉 → C5
- 后退后历史条目异常 → C3、C4、C7
- iOS 卡顿/掉帧 → D1、D2、D5、D7
- Canvas 2D 描边/渐变负载高、想上 WebGL → D7
- WebGL 上下文恢复后白屏/资源泄漏 → D9
- 淡出/裁剪逻辑让物体整体消失 → D8
- 切后台回前台无声 → F2
- 长按弹系统菜单/双击缩放 → E1
- 右键放错源 → E2
- 整数秒时间判定失效 → G1
- vitest 长模拟超时 → G3
- 搜索类算法跑不完 → G4
- 布局测量与预期不符 → A1、H1

## A. Lit + Shadow DOM

### A1 布局测量三件套不一致时先查 transform/zoom
`getBoundingClientRect().width`（含 transform）、`offsetWidth`（布局宽）、`getComputedStyle().width`（CSS 宽）三者应一致；不一致 → 有 `transform`/`zoom`/未回流。逐级祖先查 `getComputedStyle(el).transform !== 'none'`。

### A2 全局 `* { box-sizing: border-box }` 不穿透 shadow DOM
**症状**：窄屏右溢；`width:100% + padding` 的盒子比预期宽；`max-width` 反而把卡片钉在超出视口的宽度。
**根因**：全局样式表不进影子根，组件内实际是 `content-box`——`width:100%` 是内容宽，padding 再加到盒子外，总宽超过 `max-width` 时被按 border-box 语义压缩，越界。
**修法**：每个 Lit 组件 `static styles` 开头自声明 `*, *::before, *::after { box-sizing: border-box }`。
**信号**：自定义元素内任何尺寸与预期不符，先查这个。

### A3 `place-items: center` 溢出双向裁切
**症状**：内容超高时顶部滚不回去、底部内边距看不见。
**根因**：grid/flex 居中在溢出时两端同时被裁。
**修法**：容器保留 `display: flex`（column），子项用 `margin: auto`——适配时居中，溢出时 margin 塌缩为 0、从顶部可滚动、底部间距可见。
**信号**：任何"居中 + overflow:auto"的组合。

### A4 grid 中百分比宽度循环解析
**症状**：`width: min(35rem, 100%)` 或 `max-width: 100%` 在窄屏解析出比视口还大的值，右溢。
**根因**：grid 自动轨道按项目 max-content 定尺寸，`100%` 解析到被撑大的轨道（循环）。
**修法**：flex 列 + `width: 100%; max-width: 35rem; margin: auto`；或纯块级 + `margin: 0 auto`。
**信号**：单测/隔离复现（内容很小）测不出来——**必须用真实内容量级的页面测**（H1 探测法）。

### A5 装饰器 + `useDefineForClassFields: false`
`@query()` 只生成 getter，字段必须 `!` 断言且**不能带初始化器**，否则运行时报 "has only a getter"。

### A6 不要在 updated()/firstUpdated() 内设置响应式属性
触发 `change-in-update` 告警；派生状态用 `willUpdate`。

### A7 事件名必须静态
`@hudchange=` 可以，`@${var}=` 不行。

### A8 canvas 的 parentElement 恒为 null
canvas 是 shadow root 直接子节点，不能隐式推断宿主，尺寸适配的宿主必须显式传入。

## B. 布局 / 响应式 / 单位

### B1 根字号随视口缩放实现全站 rem 适配
`html { font-size: clamp(14px, calc(12.5px + min(0.7vw, 0.38vh)), 17px) }`——宽、高双向约束，矮窗（横屏手机/小笔记本）自动收紧防纵向溢出。组件内一律 rem，不再逐屏写断点。
**信号**：任何新尺寸都用 rem，不用 px（px 仅保留特殊情形：发丝线、动画位移、胶囊 999px、媒体查询断点、env(safe-area)、阴影）。

### B2 纵向溢出兜底必须可滚动且不裁切
横屏手机等极矮视口放不下时，正确行为是"可滚动 + 底部间距可见"（见 A3），而不是裁切。能接受极小视口有滚动条，主流屏幕（≥560px 高）应无。

### B3 headless Chrome 测量注意
`--window-size` 是最外层窗口，内层视口更小且有 500px 最小宽。测量用 `window.innerWidth/innerHeight`，断言布局用注入脚本读 computed style + bounding rect（详见 H）。

### B4 绝对定位居中元素宽度上限只有容器一半
**症状**：`position: absolute; left: 50%; transform: translateX(-50%)` 居中的提示条/弹层，`max-width: 92%` 设了却永远到不了，实际宽度只有容器一半。
**根因**：无显式宽度的绝对定位元素按 shrink-to-fit 定宽，其可用空间 = 包含块 − left 偏移 = 50%，`max-width` 只是上限、不是目标宽度。
**修法**：加 `width: max-content`（宽度贴内容成胶囊，`max-width` 恢复封顶换行职责）；或直接给显式宽度。
**信号**：任何 `absolute + left:50% + 无 width` 的组合，且内容比预期窄。

## C. URL 状态 / 撤销重做

### C1 分隔符避百分号转义
`URLSearchParams` 必转 `,` `;` 空格。用 `-`/`_` 做分隔符 + 枚举值缩写（`h`/`c`），全程零 `%`。

### C2 写读分离防反馈环
`set/clear` 不回调订阅者（写方自知）；`onChange` 仅响应外部 URL 变化（popstate）。否则"写入→回读→再写入"死循环。
**信号**：set 之后 onChange 又触发。

### C3 等值 set 必须跳过
按**编码后的字符串**比较，不是引用/值比较——防历史污染（pushState 多余条目）。

### C4 微任务批量写入
同帧多次 set/clear 只 pushState 一次（否则一次操作多条历史）。

### C5 差异算法比对"目标列表"
**症状**：后退/前进"没反应"、源删不掉。
**根因**：移除时与"场上当前状态"比对 → 永不删除。
**修法**：与目标列表比对；存活源保留原 id/born（不重播生长动画）。位置容差对齐 URL 精度（1 位小数 → 容差 ≥0.05）。

### C6 所有重要状态进 URL
纯组件状态刷新即丢。页面视图也要持久化（如 `?view=solutions`），优先级规则要确定（view 优先于 level）。

### C7 非规范 URL 规范化会多一条历史
挂载时初始应用用 silent（不回写 URL）。

### C8 iOS bfcache 后退 popstate 不可靠
`pageshow` 兜底重对齐；幂等——URL 未变则 sync 无变化零开销。

## D. 渲染性能（Canvas 2D）

### D1 移动端 iOS Canvas 2D 是 CPU 栅格化
每帧 `createRadialGradient` 既贵又有累积风险 → 渐变烘焙成精灵位图，每帧只 drawImage。

### D2 渲染必须节流
120Hz 屏 rAF 以 120Hz 触发，但模拟只步进 60Hz——只在"有模拟步进的帧"渲染，否则双倍负载渐进掉帧。倍速（GameLoop.setRate）下每帧都步进，渲染再封顶 60Hz（距上次渲染 ≥ SIM_DT 才画）——高速率只放大 tick 成本，不放大渲染负载。

### D3 批量描边替代逐段提交
按透明度/温度等分桶 Path2D，每桶一次 stroke。移动端从最多 ~150 次路径提交降到个位数。
**信号**：每帧路径提交数 = 元素数 × 段数。

### D4 热路径零分配
每帧 `{x:0,y:0}` 之类的临时对象 → 复用字段。采样临时量用共享对象。

### D5 ResizeObserver 抖动/循环
尺寸未变则跳过 resize，否则画布每帧重建（iOS 已知坑）。

### D6 自适应降级要防误触发
帧开销 EMA（平滑 0.95）+ 慢帧计数（如持续 150 帧超 13ms 才降级）——偶发卡顿不降级；先降粒子档，到底再降 dpr。
**倍速下帧预算按速率放大**（预算 × rate）：每帧本就要消化 rate×tick，慢帧是预期而非故障；且主导成本（流体）不可降级，阶梯在高速率下只会白降画质。**追赶封顶**：单帧最多消化 60 模拟步（≈1s 模拟），暂停回归在 16× 下不会单帧冻结。

### D7 Canvas 2D → WebGL1 批量渲染（#7 性能重构的结论与要点）
iOS Safari 的 Canvas 2D 是 CPU 栅格化（D1），逐帧上万段 Path2D 描边是瓶颈；WebGL1 在 iOS 8+/全部 WebView 可用且 GPU 加速，是兼容性最优解（WebGPU 太新、WASM 物理收益不抵工具链复杂度）。落地要点：
- **公共 API 不变**：Renderer 的 constructor/resize/toWorld/draw 保持原签名，控制器与 UI 零改动。
- **顶点批 `core/batch.ts`（纯计算无 DOM，可无头测试）+ `ui/gl.ts`（上下文/着色器/缓冲薄层）**：整帧一个动态 VBO、一次 drawArrays(TRIANGLES)。
- **GL `lineWidth` 多平台恒为 1**：线宽必须几何化——线段沿法线展开为四边形（`stroke()`），别指望 `gl.lineWidth`。
- **逐顶点颜色取代分桶**：透明度/颜色不再离散分桶（Canvas 的 strokeStyle 状态机所迫），每段直接带精确 RGBA，一次提交。
- **径向渐变 = 扇形逐顶点插值**：中心色→边缘色线性插值即等价两端色标的 createRadialGradient，免每帧建渐变与精灵烘焙。
- **顶点缓冲是 float32**：无头测试断言用 toBeCloseTo（容差 1e-5），别用 toEqual 精确比较。
- **上下文回收**：iOS 内存压力会销毁 WebGL 上下文——`webglcontextlost` 要 preventDefault，`webglcontextrestored` 重建程序/缓冲。
- **混合**：`SRC_ALPHA / ONE_MINUS_SRC_ALPHA`（非预乘），与 Canvas rgba 语义一致；`alpha:false` 画布不透明，天空由场景自铺满。
- 静态背景不必再离屏缓存：天空/地形/光晕每帧重建仅数百顶点，GPU 可忽略，还省掉 bgKey/bg 位图的缓存复杂度。

### D9 webglcontextrestored 重建失败会静默白屏 + 重复恢复泄漏 GPU 对象
**症状**：iOS 内存压力回收上下文后画面空白；或多次恢复后显存持续上涨。
**根因**：restored 回调里重建程序/缓冲，但 (1) 重建失败（编译/链接错误）时静默早退，`program`/`buffer` 仍是已随上下文销毁的旧对象，draw 继续误用 → 白屏且无重试路径；(2) 每次重建都不删旧 shader/program/buffer → 重复恢复反复泄漏。
**修法**：`init()` 开头 `dispose()` 删旧对象（恢复后旧对象本已失效）；失败路径删除已创建对象并置空指针返回 false，restored 回调据返回值报错——draw 检查 `!program` 跳过，不碰失效对象；shader 在 link 成功后即可 delete。
**信号**：上下文恢复相关代码出现"早退不清资源"或"重试不清理旧对象"。

### D8 淡出/裁剪的早退别跳过物体本体
旧 `drawPlane` 在 `alt >= SHADOW_FADE_ALT` 时直接 `return`——连飞机本体都不画，高空飞机凭空消失。**影子淡出是"局部效果"，早退只能跳过影子那段**；任何"某效果随条件淡出"的代码，先确认早退范围不含主体绘制。重构渲染时优先审这类 early-return。

## E. 手势 / 移动端

### E1 `touch-action: none` + `user-scalable=no`
否则长按弹系统菜单、双击缩放。

### E2 右键与左键冲突
`pointerdown` 只处理 `button === 0`（右键若放行会先走热源 tap 流程），右键放冷源走 `contextmenu`（preventDefault）。

### E3 长按判定用定时器 + 位移阈值
长按达阈值即确认（380ms），位移超过 slop（14px）取消；pointercancel 清理所有轨道与定时器。

## F. 音频（WebAudio）

### F1 iOS 必须用户手势中创建/恢复 AudioContext
`pointerdown` 时 unlock（`ctx.resume()`）。

### F2 后台挂起 + 恢复可见立即恢复
`visibilitychange`：hidden → suspend；visible 且 suspended → resume（**不依赖下次触摸**，否则切回页面无声）。

### F3 WebKit 音频图持有已完成节点
一次性音源播完后显式断开整条节点链（ended 事件），否则长会话累积 → GC 压力与掉帧。

## G. 物理模拟 / 测试

### G1 时间累积浮点误差
`t += 1/60` 累积后 `Math.floor(t) === t` 在整数秒处失效（只命中一次）。用整数步数：`step % 60 === 0`。

### G2 确定性模拟可作测试断言
无随机项的模拟，通关时刻确定 → 测试断言"解可通关"且通关时间与记录一致（±2s 容差）。

### G3 vitest 长模拟必须显式传超时
`test('...', () => {...}, 30000)`——默认 5s 必挂。

### G4 高时间复杂度的搜索算法先评估
运行前估算复杂度与耗时（如 13³ 网格 × 45s 模拟 ≈ 数小时——先算再跑）。用有界候选集 + 早退。

### G5 批量"删注释"编辑事故
用整块替换删注释时，oldString 稍不精确就把常量/方法本体一起删掉。**每次批量编辑后立即 `tsc -b` 验证**；删常量前先 `rg` 确认其引用位置。

## H. 布局调试方法论（实测有效）

### H1 数值化探测替代目测截图
注入探针脚本读 `getComputedStyle` + `offsetWidth` + `getBoundingClientRect` + 祖先 transform，多尺寸（320–1920）headless Chrome 验证。
**隔离复现不可信**：布局 bug 往往只在真实内容量级下触发（如 A4 百分比循环），必须用真实页面测。

### H2 三数值不一致 → 先查 transform/zoom（A1）；样式对但布局错 → 查 box-sizing（A2）

### H3 Lit 3 样式在 `shadowRoot.adoptedStyleSheets`
无 `<style>` 标签，查生效规则读 `cssRules` 的 `cssText`。

```


### Tool: TodoList
**Status**: completed

**Output:**
```
- [-] 读 pitfalls skill 与剩余热点代码（sfx/bodies），补全诊断依据
- [ ] 建立无头性能基准：fluid/tracers/batch 逐组件 ms 证据
- [ ] 查资料：iOS Safari/WKWebView 的 JS JIT 与 WASM 现状、MoonBit wasm 集成方式
- [ ] 优化 JS 热路径（求解器 + 渲染批构建），数值不漂移
- [ ] 按研究结论落地 wasm 求解器（MoonBit，含 JS 回退）
- [ ] bun run check 全绿 + 更新 docs/issues/#7.md 报告
```


## User

重要注意，要做好测试和验证，要稳定性、一致性。要能够在真实浏览器环境测试验证。继续


## Assistant

The user is emphasizing the following: perform testing and verification thoroughly, with stability and consistency, and be able to test and verify in a real browser environment. "继续" — continue.

This is an important supplement: they want real-browser verification (this overrides/refines the AGENTS.md guidance — AGENTS.md states that R&D should not do Computer Use or E2E unless necessary or unless the user requests it — here the user is explicitly requesting real-browser testing/verification). Therefore, we can and should use chrome-devtools MCP for performance measurement in a real browser. On desktop Chrome, we can use CPU throttling to roughly emulate a slower device and compare JS vs wasm. We can't do iOS Safari testing here, but desktop Chrome + CPU throttling can provide evidence. We could also check whether Safari is available on this Mac (macOS Safari = WebKit JSC — actually a pretty good proxy for iOS Safari's JS engine! The JavaScriptCore on macOS is the same engine family as iOS's). If Safari/WebKit automation is available (WRDP - WebKit Remote Debugging Protocol), even better. The issue mentioned "Safari 的 WRDP". Let's check what's available on this machine: Safari with Web Inspector enabled? We can launch Safari via computer_use tools (macOS). Hmm, though the user said real-browser testing is needed — the chrome-devtools MCP is already available. For Safari, if Develop menu / remote automation is enabled, we can use safari-remote-automation via WRDP. Let's check.

Verification plan:
1. Unit tests (vitest) — existing ones plus new ones for the changes.
2. Headless benchmarks via bun for the solver (before/after comparison).
3. Real-browser performance harness: a dedicated dev page or benchmark script injected via chrome-devtools MCP into a vite dev server, using performance.now() measurement, run with CPU throttling (e.g., 4x/6x slowdown to emulate a mobile-class CPU), comparing before/after optimization and JS vs wasm. Also measure FPS in actual gameplay? AGENTS.md says gameplay verification should rely on the player, but the user is now explicitly requesting real-browser testing — what we'll do is performance verification, not gameplay E2E: measure frame time during scripted simulation runs (e.g., drive the loop headlessly inside the page using sources). The performance harness page can be made minimal and cleanly (a test-only URL parameter or a separate entry? Keep it tidy: probably a `?bench` mode in index.html… hmm, better not to pollute the app. A separate html entry like `bench.html` that vite dev serves (vite serves any html) but exclude from the build? Actually vite builds with multiple entry points listed in rollupOptions.input — default builds only index.html, so bench.html would only be served in dev. That's clean: the dev-only bench page imports from src and measures.

Let's also think about Safari: on macOS, the user can enable "Show Develop menu". `safaridriver --enable` requires authentication. We can try computer_use to open Safari and use the WebKit inspector… that's heavy. Alternative: WebKit nightly? Probably not installed. Let's check the machine: is Chrome available? The chrome-devtools MCP probably launches Chrome itself. First let's do the engineering work, and use chrome-devtools MCP with CPU throttling for real-browser verification.

Now, let's proceed: first finish reading the remaining hot code (sfx.ts, bodies.ts), check package.json, and then build the benchmarks.

Also an important point: the user's message says "要能够在真实浏览器环境测试验证" — meaning my deliverable should include a repeatable way to verify in a real browser. Let's build the bench page accordingly.

Now, let's continue reading the files.


## Assistant

明白：这一轮把「真实浏览器环境的可重复性能验证」作为一等交付物。继续补齐诊断信息。


### Tool: ReadFile: sfgame-web/src/sim/bodies.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/sim/bodies.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/sim/bodies.ts`

**Output:**
```typescript
import type { Fluid } from './fluid'
import type { WorldBounds } from './types'

/**
 * 拉格朗日视角的质点刚体：受重力与"向空气速度收敛"的气动阻力驱动。
 * 纸飞机、气球等都可以用该模型表达（参数不同而已）。
 * 与流体为单向耦合：风推动物体，物体不反作用于风（Game Jam 范围取舍）。
 */
export interface Body {
  x: number
  y: number
  vx: number
  vy: number
  /** 视觉姿态（弧度），随速度方向平滑转动 */
  angle: number
  radius: number
  /** 向空气速度收敛的速率（1/s），越大越"轻" */
  dragK: number
  /** 重力加速度（世界单位/s²），y 向下为正 */
  gravity: number
  /** 内部时钟，用于待机摆动 */
  clock: number
}

export interface BodyOptions {
  radius: number
  dragK: number
  gravity: number
}

export function createBody(x: number, y: number, opts: BodyOptions): Body {
  return {
    x,
    y,
    vx: 0,
    vy: 0,
    angle: 0,
    radius: opts.radius,
    dragK: opts.dragK,
    gravity: opts.gravity,
    clock: Math.random() * Math.PI * 2,
  }
}

const tmpAir = { x: 0, y: 0 }

/** 可贴地滑行的最大坡度（每单位水平位移的地形抬升量）。
 * 超过该值（如崖壁）禁止 snap 抬升——飞机不能凭水平风"瞬移爬墙"。 */
const MAX_SLIDE_SLOPE = 1.0
const WALL_RESTITUTION = 0.35
/** 贴地摩擦：每次接触后水平速度的保留比例（防被微风吹离托举位置） */
const GROUND_FRICTION = 0.3

export function stepBody(
  body: Body,
  fluid: Fluid,
  dt: number,
  groundY: (x: number) => number,
  world: WorldBounds,
) {
  const px = body.x
  fluid.sampleVelocity(body.x, body.y, tmpAir)
  const k = Math.min(1, body.dragK * dt)
  body.vx += (tmpAir.x - body.vx) * k
  body.vy += body.gravity * dt + (tmpAir.y - body.vy) * k
  body.x += body.vx * dt
  body.y += body.vy * dt

  const r = body.radius
  // 边界墙只反弹"正在向外运动"的物体：从画布外飞入的物体（如关卡开场）不受拦截
  if (body.x < r) {
    if (body.vx < 0) {
      body.x = r
      body.vx = Math.abs(body.vx) * WALL_RESTITUTION
    }
  } else if (body.x > world.w - r) {
    if (body.vx > 0) {
      body.x = world.w - r
      body.vx = -Math.abs(body.vx) * WALL_RESTITUTION
    }
  }
  if (body.y < r) {
    if (body.vy < 0) {
      body.y = r
      body.vy = Math.abs(body.vy) * WALL_RESTITUTION
    }
  }

  const pground = groundY(px) - r * 0.5
  const ground = groundY(body.x) - r * 0.5
  if (body.y > ground) {
    // 地形在一帧内抬升过陡（崖壁）：不沿坡"瞬移"抬升，视作墙壁横向弹回
    const dx = body.x - px
    if (dx > 1e-6 && pground - ground > MAX_SLIDE_SLOPE * dx) {
      body.x = px
      if (body.y > pground) body.y = pground
      body.vx = -Math.abs(body.vx) * WALL_RESTITUTION
      if (body.vy > 0) body.vy = -body.vy * 0.1
    } else {
      body.y = ground
      if (body.vy > 0) body.vy = -body.vy * 0.1
      body.vx *= GROUND_FRICTION
    }
  }

  const speed = Math.sqrt(body.vx * body.vx + body.vy * body.vy)
  body.clock += dt * (1.5 + speed * 0.4)
  if (speed > 1.2) {
    const target = Math.atan2(body.vy, body.vx)
    let diff = target - body.angle
    while (diff > Math.PI) diff -= Math.PI * 2
    while (diff < -Math.PI) diff += Math.PI * 2
    body.angle += diff * Math.min(1, 9 * dt)
  }
}

```


### Tool: ReadFile: sfgame-web/src/core/sfx.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/core/sfx.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/core/sfx.ts`

**Output:**
```typescript
const STORAGE_KEY = 'zaofeng.muted'
const MASTER_GAIN = 0.5

/**
 * Paul Kellet 粉红噪声近似滤波：白噪声经一阶 IIR 组逼近 1/f 功率谱。
 * 1/f 谱正是自然风湍流的能量分布特征，比白噪声听感真实得多。
 */
function makePinkNoiseBuffer(ctx: AudioContext): AudioBuffer {
  const len = ctx.sampleRate * 2
  const buffer = ctx.createBuffer(1, len, ctx.sampleRate)
  const data = buffer.getChannelData(0)
  let b0 = 0
  let b1 = 0
  let b2 = 0
  let b3 = 0
  let b4 = 0
  let b5 = 0
  let b6 = 0
  for (let i = 0; i < len; i++) {
    const white = Math.random() * 2 - 1
    b0 = 0.99886 * b0 + white * 0.0555179
    b1 = 0.99332 * b1 + white * 0.0750759
    b2 = 0.969 * b2 + white * 0.153852
    b3 = 0.8665 * b3 + white * 0.3104856
    b4 = 0.55 * b4 + white * 0.5329522
    b5 = -0.7616 * b5 - white * 0.016898
    data[i] = (b0 + b1 + b2 + b3 + b4 + b5 + b6 + white * 0.5362) * 0.11
    b6 = white * 0.115926
  }
  return buffer
}

export interface WindVoiceOptions {
  /** 零风速时的带通中心频率 */
  baseFreq: number
  /** 风速升到 vFull 时增加的频率跨度（风越大声越"亮"） */
  freqSpan: number
  /** 响度达到峰值对应的风速 */
  vFull: number
  maxGain: number
  /** 响度平滑时间常数（秒），避免抖动 */
  tau: number
}

/**
 * 持续的"风声"：粉红噪声 → 带通滤波 → 增益。
 * 物理驱动：响度 ∝ (v/vFull)²（气动噪声随流速非线性增长的近似），
 * 带通中心频率随风速上移（快风含更多高频湍流成分）。
 */
class WindVoice {
  private opts: WindVoiceOptions
  private gainNode: GainNode
  private filter: BiquadFilterNode
  /** 可选立体声定位（仅飞机摩擦声用：按横向位置左右移动） */
  private panner: StereoPannerNode | null = null
  private level = 0

  constructor(
    ctx: AudioContext,
    dest: AudioNode,
    noise: AudioBuffer,
    opts: WindVoiceOptions,
    stereo = false,
  ) {
    this.opts = opts
    const src = ctx.createBufferSource()
    src.buffer = noise
    src.loop = true
    this.filter = ctx.createBiquadFilter()
    this.filter.type = 'bandpass'
    this.filter.frequency.value = opts.baseFreq
    this.filter.Q.value = 0.85
    this.gainNode = ctx.createGain()
    this.gainNode.gain.value = 0
    src.connect(this.filter)
    this.filter.connect(this.gainNode)
    if (stereo) {
      this.panner = ctx.createStereoPanner()
      this.gainNode.connect(this.panner)
      this.panner.connect(dest)
    } else {
      this.gainNode.connect(dest)
    }
    src.start()
  }

  setPan(value: number) {
    if (this.panner) this.panner.pan.value = value
  }

  update(speed: number, dt: number) {
    const t = Math.min(1, Math.max(0, speed) / this.opts.vFull)
    const target = t * t * this.opts.maxGain
    const k = 1 - Math.exp(-dt / this.opts.tau)
    this.level += (target - this.level) * k
    this.gainNode.gain.value = this.level
    this.filter.frequency.value = this.opts.baseFreq + this.opts.freqSpan * t
  }

  /** 淡出到静音并复位内部电平（离开游戏场景时调用，防风声残留） */
  silence() {
    this.level = 0
    this.gainNode.gain.setTargetAtTime(0, this.gainNode.context.currentTime, 0.25)
  }
}

/**
 * 零依赖音效：WebAudio 合成。
 * 两层声音：离散 UI 音（振荡器）+ 连续物理风声（噪声滤波，见 WindVoice）。
 */
class Sfx {
  private ctx: AudioContext | null = null
  private master: GainNode | null = null
  private noiseBuf: AudioBuffer | null = null
  private bed: WindVoice | null = null
  private planeWind: WindVoice | null = null
  private unlockArmed = false
  muted = false

  constructor() {
    try {
      this.muted = localStorage.getItem(STORAGE_KEY) === '1'
    } catch {
    }
  }

  /**
   * 解锁音频（浏览器自动播放策略：AudioContext 须在用户手势内创建/恢复）。
   * 首次调用即挂全局一次性监听（pointerdown/keydown）——整个应用任意位置
   * 的第一次用户交互即完成解锁，此后音频恒可播放，不依赖具体交互路径。
   */
  unlock() {
    if (!this.unlockArmed) {
      this.unlockArmed = true
      const fire = () => this.unlock()
      document.addEventListener('pointerdown', fire, { once: true })
      document.addEventListener('keydown', fire, { once: true })
    }
    if (!this.ctx) {
      const Ctor =
        window.AudioContext ??
        (window as unknown as { webkitAudioContext?: typeof AudioContext })
          .webkitAudioContext
      if (!Ctor) return
      try {
        this.ctx = new Ctor()
        this.master = this.ctx.createGain()
        this.master.gain.value = this.muted ? 0 : MASTER_GAIN
        this.master.connect(this.ctx.destination)
        this.noiseBuf = makePinkNoiseBuffer(this.ctx)
        this.bed = new WindVoice(this.ctx, this.master, this.noiseBuf, {
          baseFreq: 180,
          freqSpan: 420,
          vFull: 9,
          maxGain: 0.16,
          tau: 0.6,
        })
        this.planeWind = new WindVoice(this.ctx, this.master, this.noiseBuf, {
          baseFreq: 420,
          freqSpan: 900,
          vFull: 8,
          maxGain: 0.3,
          tau: 0.18,
        }, true)
        // 页面隐藏时挂起音频，避免后台持续出声；恢复可见时立即恢复（不依赖下次触摸）
        document.addEventListener('visibilitychange', () => {
          if (!this.ctx) return
          if (document.hidden) void this.ctx.suspend()
          else if (this.ctx.state === 'suspended') void this.ctx.resume()
        })
      } catch {
        this.ctx = null
        this.master = null
        return
      }
    }
    if (this.ctx.state === 'suspended') void this.ctx.resume()
  }

  /**
   * 每模拟步驱动物理风声。
   * @param fieldWind 全场代表风速（风越大，底噪越响）
   * @param planeRel 飞机相对空气的速度——摩擦声的真实来源：
   *   随风飘（相对速度≈0）时安静，逆风/坠落切割空气时呼啸
   */
  updateWind(fieldWind: number, planeRel: number, dt: number) {
    if (!this.ctx || !this.bed || !this.planeWind) return
    this.bed.update(fieldWind, dt)
    this.planeWind.update(planeRel, dt)
  }

  /** 离开游戏场景时淡出风声：防止标题页/解法页残留风声（游戏内 tick 会重新驱动） */
  fadeOutWind() {
    this.bed?.silence()
    this.planeWind?.silence()
  }

  /** 飞机摩擦声左右定位：按飞机横向位置（x∈[0,worldW]）在声道间移动 */
  setPlanePan(x: number, worldW: number) {
    this.planeWind?.setPan(Math.max(-1, Math.min(1, (x / worldW) * 2 - 1)))
  }

  /** 落地/擦地撞击声：响度与低通截止随撞击速度增大（动能 → 声能） */
  land(impact: number) {
    if (this.muted || !this.ctx || !this.master || !this.noiseBuf) return
    if (impact < 0.6) return
    try {
      const t0 = this.ctx.currentTime
      const src = this.ctx.createBufferSource()
      src.buffer = this.noiseBuf
      src.playbackRate.value = 0.7 + Math.random() * 0.4
      const lp = this.ctx.createBiquadFilter()
      lp.type = 'lowpass'
      lp.frequency.value = 260 + 320 * Math.min(1, impact / 6)
      const g = this.ctx.createGain()
      const peak = Math.min(0.5, 0.06 + impact * 0.045)
      g.gain.setValueAtTime(peak, t0)
      g.gain.exponentialRampToValueAtTime(0.0001, t0 + 0.17)
      src.connect(lp)
      lp.connect(g)
      g.connect(this.master)
      src.start(t0, Math.random() * 1.4, 0.22)
      this.releaseWhenDone(src, [src, lp, g])
    } catch {
    }
  }

  toggleMuted(): boolean {
    this.muted = !this.muted
    try {
      localStorage.setItem(STORAGE_KEY, this.muted ? '1' : '0')
    } catch {
    }
    if (this.master && this.ctx) {
      // 连续风声层由主增益统一静音
      this.master.gain.setTargetAtTime(this.muted ? 0 : MASTER_GAIN, this.ctx.currentTime, 0.05)
    }
    return this.muted
  }

  /**
   * 一次性音源播完后显式断开整条节点链：不这么做的话 WebKit 的音频图
   * 会持有已完成节点（长会话累积 → GC 压力与掉帧）。ended 事件确保
   * 播放结束（或 stop 到达）后才断开，中途不静音。
   */
  private releaseWhenDone(
    src: AudioScheduledSourceNode,
    nodes: Array<AudioNode | AudioScheduledSourceNode>,
  ) {
    const cleanup = () => {
      for (const n of nodes) {
        try {
          n.disconnect()
        } catch {
        }
      }
    }
    try {
      src.addEventListener('ended', cleanup, { once: true })
    } catch {
      // 极端环境（无 ended 事件）下退化为延迟清理
      window.setTimeout(cleanup, 2000)
    }
  }

  private tone(
    f0: number,
    f1: number,
    dur: number,
    type: OscillatorType,
    peak: number,
    delay = 0,
  ) {
    if (this.muted || !this.ctx || !this.master) return
    try {
      const t0 = this.ctx.currentTime + delay
      const osc = this.ctx.createOscillator()
      const gain = this.ctx.createGain()
      osc.type = type
      osc.frequency.setValueAtTime(Math.max(1, f0), t0)
      osc.frequency.exponentialRampToValueAtTime(Math.max(1, f1), t0 + dur)
      gain.gain.setValueAtTime(0.0001, t0)
      gain.gain.exponentialRampToValueAtTime(peak, t0 + 0.012)
      gain.gain.exponentialRampToValueAtTime(0.0001, t0 + dur)
      osc.connect(gain)
      gain.connect(this.master)
      osc.start(t0)
      osc.stop(t0 + dur + 0.05)
      this.releaseWhenDone(osc, [osc, gain])
    } catch {
    }
  }

  placeHot() {
    // 音高 ±5% 随机：连放不机械
    const f = 620 * (0.95 + Math.random() * 0.1)
    this.tone(f, f * 0.52, 0.14, 'sine', 0.5)
  }

  placeCold() {
    const f = 340 * (0.95 + Math.random() * 0.1)
    this.tone(f, f * 0.59, 0.16, 'sine', 0.5)
  }

  remove() {
    this.tone(260, 180, 0.07, 'triangle', 0.3)
  }

  deny() {
    this.tone(150, 120, 0.09, 'square', 0.1)
  }

  /**
   * UI 交互音效族：同一家族（短促软 sine/triangle、音量低于玩法反馈音），
   * 语义区分——前进上行、后退下行、重置双音、通用单音。
   */
  uiClick() {
    this.tone(620, 480, 0.06, 'sine', 0.22)
  }

  uiEnter() {
    this.tone(520, 860, 0.09, 'sine', 0.25)
  }

  uiBack() {
    this.tone(520, 320, 0.08, 'sine', 0.22)
  }

  uiReset() {
    this.tone(440, 440, 0.05, 'triangle', 0.25)
    this.tone(440, 440, 0.05, 'triangle', 0.25, 0.07)
  }

  win() {
    const notes = [523.25, 659.25, 783.99, 1046.5]
    notes.forEach((f, i) => this.tone(f, f, 0.22, 'triangle', 0.32, i * 0.09))
  }
}

export const sfx = new Sfx()

```


## Assistant

sfx is low-cost (a few parameter writes per tick). bodies are also low-cost. The main cost items are the fluid solver, tracer updates, and tracer drawing (batch construction).

OK, so let me check package.json and the test setup, and then write a headless benchmark.


## Assistant

热点全部定位：`fluid.step()`（主体）、`tracers.step()`、`drawTracers` 顶点批构建。sfx/bodies 均为轻量。现在建无头基准拿证据。


### Tool: ReadFile: sfgame-web/package.json
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/package.json"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/package.json`

**Output:**
```json
{
  "name": "sfgame-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "~7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "module": "index.ts"
}

```


### Tool: ReadFile: sfgame-web/tests/fluid.test.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/tests/fluid.test.ts"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/tests/fluid.test.ts`

**Output:**
```typescript
import { expect, test } from 'vitest'
import { Fluid } from '../src/sim/fluid'

function makeFluid() {
  return new Fluid({
    nx: 48,
    ny: 36,
    cell: 1.5,
    buoyancy: 2.0,
    tMax: 9,
    heatRate: 18,
    sourceRadius: 3.4,
    velDamping: 0.996,
    tDamping: 0.99,
    iterations: 12,
    vorticity: 0.5,
  })
}

const DT = 1 / 60

test('热源上方产生上升气流（y 向下，上升即 v < 0）', () => {
  const f = makeFluid()
  const wx = 36
  const wy = 38
  for (let i = 0; i < 120; i++) {
    f.addHeat(wx, wy, 16 * DT)
    f.step(DT)
  }
  const air = { x: 0, y: 0 }
  f.sampleVelocity(wx, wy - 6, air)
  expect(air.y).toBeLessThan(0)
  expect(-air.y).toBeGreaterThan(2)
})

test('热源侧下方产生横向补充气流（对流闭合）', () => {
  const f = makeFluid()
  const wx = 36
  const wy = 38
  for (let i = 0; i < 120; i++) {
    f.addHeat(wx, wy, 16 * DT)
    f.step(DT)
  }
  const air = { x: 0, y: 0 }
  f.sampleVelocity(wx - 8, wy + 2, air)
  expect(air.x).toBeGreaterThan(0)
})

test('冷源产生下沉气流', () => {
  const f = makeFluid()
  const wx = 36
  const wy = 20
  for (let i = 0; i < 120; i++) {
    f.addHeat(wx, wy, -16 * DT)
    f.step(DT)
  }
  const air = { x: 0, y: 0 }
  f.sampleVelocity(wx, wy + 6, air)
  expect(air.y).toBeGreaterThan(0)
})

test('温度场被 tMax 钳制', () => {
  const f = makeFluid()
  for (let i = 0; i < 300; i++) {
    f.addHeat(30, 30, 16 * DT)
    f.step(DT)
  }
  let maxT = 0
  for (let i = 0; i < f.t.length; i++) {
    if (f.t[i] > maxT) maxT = f.t[i]
  }
  expect(maxT).toBeLessThanOrEqual(9 + 1e-4)
  expect(maxT).toBeGreaterThan(0)
})

test('固体掩码内无速度', () => {
  const f = makeFluid()
  f.setGroundMask(() => 45)
  f.addHeat(30, 40, 5)
  for (let i = 0; i < 60; i++) f.step(DT)
  const air = { x: 0, y: 0 }
  f.sampleVelocity(30, 46.5, air)
  expect(Math.abs(air.x)).toBe(0)
  expect(Math.abs(air.y)).toBe(0)
})

```


## Assistant

OK, now writing the benchmark script. This should measure the following:
1. Cost of `fluid.step()` at the actual level grid (101×75, cell 0.75), using a realistic heat source
2. Cost of `tracers.step()`
3. Batch build cost of drawTracers (need to reproduce the tracer drawing logic — actually, can we import Renderer? No, Renderer needs canvas/DOM. Instead, benchmark MeshBatch.stroke at the scale of ~9600 segments, plus the cos/sin cost of disc.)
4. Cost of a full LevelSimulation.step (including bodies)

Write it as a bun script under sfgame-web/scripts/bench.ts. Also make it deterministic (fixed RNG seed? Tracers use Math.random internally, and bench can't seed it. That's fine for perf measurement).

Next, run it with bun to get baseline numbers.


### Tool: WriteFile: Writing to sfgame-web/scripts/bench.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/scripts/bench.ts",
  "content": "/**\n * 无头性能基准（bun scripts/bench.ts）：逐组件测量每帧（60Hz）预算占用。\n * 用法：bun scripts/bench.ts [seconds]——seconds 为模拟时长（默认 20s）。\n *\n * 组件与真实游戏一致：第 1 关网格（76×56/cell 0.75）、4 源、移动端粒子档 240。\n * 输出 ms/frame 均值与 p95，对照 16.7ms 帧预算判断瓶颈构成。\n */\nimport { Fluid } from '../src/sim/fluid'\nimport { LevelSimulation } from '../src/game/simulation'\nimport { LEVEL_1 } from '../src/game/levels'\nimport { Tracers } from '../src/sim/particles'\nimport { MeshBatch } from '../src/core/batch'\nimport { SIM_DT } from '../src/core/loop'\n\nconst SIM_SECONDS = Number(process.argv[2] ?? 20)\nconst STEPS = Math.round(SIM_SECONDS / SIM_DT)\nconst TRACER_COUNT = 240 // 移动端初始档\nconst TRAIL_LEN = 24\n\nfunction ground1(x: number): number {\n  const clamp01 = (t: number) => (t < 0 ? 0 : t > 1 ? 1 : t)\n  const smooth = (t: number) => t * t * (3 - 2 * t)\n  return 48 - 26 * smooth(clamp01((x - 36) / 12))\n}\n\n/** 预热 JIT，避免测量段混入编译开销 */\nfunction warmup(sim: LevelSimulation, n = 300) {\n  for (let i = 0; i < n; i++) sim.step(SIM_DT)\n}\n\nfunction stats(samples: number[]) {\n  const sorted = [...samples].sort((a, b) => a - b)\n  const mean = samples.reduce((s, v) => s + v, 0) / samples.length\n  return { mean, p95: sorted[Math.floor(samples.length * 0.95)] }\n}\n\n// ---------- 1. 流体求解器（含源注入） ----------\n{\n  const sim = new LevelSimulation(LEVEL_1)\n  const spots: Array<[number, number, 'hot' | 'cold']> = [\n    [18, 42, 'hot'],\n    [30, 40, 'hot'],\n    [44, 30, 'hot'],\n    [52, 24, 'cold'],\n  ]\n  for (const [x, y, kind] of spots) sim.placeSource(x, y, kind, true)\n  warmup(sim)\n\n  const fluidSamples: number[] = []\n  const stepSamples: number[] = []\n  const rate = 10 * SIM_DT\n  for (let i = 0; i < STEPS; i++) {\n    const t0 = performance.now()\n    for (const s of sim.sources) {\n      sim.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)\n    }\n    sim.fluid.step(SIM_DT)\n    fluidSamples.push(performance.now() - t0)\n    const t1 = performance.now()\n    sim.step(SIM_DT)\n    stepSamples.push(performance.now() - t1)\n  }\n  const f = stats(fluidSamples)\n  const s = stats(stepSamples)\n  console.log(\n    `fluid.step      mean ${f.mean.toFixed(3)} ms  p95 ${f.p95.toFixed(3)} ms`,\n  )\n  console.log(\n    `LevelSim.step   mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（含刚体/源注入，不含粒子）`,\n  )\n}\n\n// ---------- 2. 示踪粒子推进 ----------\n{\n  const fluid = new Fluid({\n    nx: Math.round(76 / 0.75),\n    ny: Math.round(56 / 0.75),\n    cell: 0.75,\n    buoyancy: 2.0,\n    tMax: 9,\n    heatRate: 10,\n    sourceRadius: 3.4,\n    velDamping: 0.997,\n    tDamping: 0.99,\n    iterations: 12,\n    vorticity: 0.5,\n  })\n  fluid.setGroundMask(ground1)\n  fluid.setAmbient(1.8, 0)\n  const tracers = new Tracers(TRACER_COUNT, LEVEL_1.world, ground1, TRAIL_LEN)\n  const sources = [\n    { x: 18, y: 42, kind: 'hot' as const },\n    { x: 30, y: 40, kind: 'hot' as const },\n  ]\n  for (let i = 0; i < 600; i++) {\n    fluid.addHeat(18, 42, 10 * SIM_DT)\n    fluid.step(SIM_DT)\n    tracers.step(SIM_DT, fluid, sources)\n  }\n  const samples: number[] = []\n  for (let i = 0; i < STEPS; i++) {\n    fluid.addHeat(18, 42, 10 * SIM_DT)\n    fluid.step(SIM_DT)\n    const t0 = performance.now()\n    tracers.step(SIM_DT, fluid, sources)\n    samples.push(performance.now() - t0)\n  }\n  const s = stats(samples)\n  console.log(\n    `tracers.step    mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（${TRACER_COUNT} 粒子 × ${TRAIL_LEN} 轨迹点）`,\n  )\n}\n\n// ---------- 3. 顶点批构建（渲染侧 CPU） ----------\n{\n  const batch = new MeshBatch()\n  const n = 9600 // ≈ 400 粒子 × 24 段的最坏描边量\n  const samples: number[] = []\n  for (let iter = 0; iter < 300; iter++) {\n    batch.reset()\n    const t0 = performance.now()\n    for (let i = 0; i < n; i++) {\n      const x = (i % 100) * 0.7\n      const y = ((i / 100) | 0) * 0.7\n      batch.stroke(x, y, x + 0.55, y + 0.12, 0.3, 0.8, 0.4, 0.2, 0.1)\n    }\n    for (let i = 0; i < 400; i++) {\n      batch.disc((i % 20) * 3, ((i / 20) | 0) * 3, 0.3, 0.3, 0, 10, 0.5, 0.5, 0.5, 0.5)\n    }\n    samples.push(performance.now() - t0)\n  }\n  const s = stats(samples)\n  console.log(\n    `batch 构建      mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（${n} stroke + 400 disc，顶点数 ${batch.count}）`,\n  )\n}\n"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/scripts/bench.ts`

**Output:**

*Diff for `bench.ts`:*
```diff
/**
 * 无头性能基准（bun scripts/bench.ts）：逐组件测量每帧（60Hz）预算占用。
 * 用法：bun scripts/bench.ts [seconds]——seconds 为模拟时长（默认 20s）。
 *
 * 组件与真实游戏一致：第 1 关网格（76×56/cell 0.75）、4 源、移动端粒子档 240。
 * 输出 ms/frame 均值与 p95，对照 16.7ms 帧预算判断瓶颈构成。
 */
import { Fluid } from '../src/sim/fluid'
import { LevelSimulation } from '../src/game/simulation'
import { LEVEL_1 } from '../src/game/levels'
import { Tracers } from '../src/sim/particles'
import { MeshBatch } from '../src/core/batch'
import { SIM_DT } from '../src/core/loop'

const SIM_SECONDS = Number(process.argv[2] ?? 20)
const STEPS = Math.round(SIM_SECONDS / SIM_DT)
const TRACER_COUNT = 240 // 移动端初始档
const TRAIL_LEN = 24

function ground1(x: number): number {
  const clamp01 = (t: number) => (t < 0 ? 0 : t > 1 ? 1 : t)
  const smooth = (t: number) => t * t * (3 - 2 * t)
  return 48 - 26 * smooth(clamp01((x - 36) / 12))
}

/** 预热 JIT，避免测量段混入编译开销 */
function warmup(sim: LevelSimulation, n = 300) {
  for (let i = 0; i < n; i++) sim.step(SIM_DT)
}

function stats(samples: number[]) {
  const sorted = [...samples].sort((a, b) => a - b)
  const mean = samples.reduce((s, v) => s + v, 0) / samples.length
  return { mean, p95: sorted[Math.floor(samples.length * 0.95)] }
}

// ---------- 1. 流体求解器（含源注入） ----------
{
  const sim = new LevelSimulation(LEVEL_1)
  const spots: Array<[number, number, 'hot' | 'cold']> = [
    [18, 42, 'hot'],
    [30, 40, 'hot'],
    [44, 30, 'hot'],
    [52, 24, 'cold'],
  ]
  for (const [x, y, kind] of spots) sim.placeSource(x, y, kind, true)
  warmup(sim)

  const fluidSamples: number[] = []
  const stepSamples: number[] = []
  const rate = 10 * SIM_DT
  for (let i = 0; i < STEPS; i++) {
    const t0 = performance.now()
    for (const s of sim.sources) {
      sim.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)
    }
    sim.fluid.step(SIM_DT)
    fluidSamples.push(performance.now() - t0)
    const t1 = performance.now()
    sim.step(SIM_DT)
    stepSamples.push(performance.now() - t1)
  }
  const f = stats(fluidSamples)
  const s = stats(stepSamples)
  console.log(
    `fluid.step      mean ${f.mean.toFixed(3)} ms  p95 ${f.p95.toFixed(3)} ms`,
  )
  console.log(
    `LevelSim.step   mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（含刚体/源注入，不含粒子）`,
  )
}

// ---------- 2. 示踪粒子推进 ----------
{
  const fluid = new Fluid({
    nx: Math.round(76 / 0.75),
    ny: Math.round(56 / 0.75),
    cell: 0.75,
    buoyancy: 2.0,
    tMax: 9,
    heatRate: 10,
    sourceRadius: 3.4,
    velDamping: 0.997,
    tDamping: 0.99,
    iterations: 12,
    vorticity: 0.5,
  })
  fluid.setGroundMask(ground1)
  fluid.setAmbient(1.8, 0)
  const tracers = new Tracers(TRACER_COUNT, LEVEL_1.world, ground1, TRAIL_LEN)
  const sources = [
    { x: 18, y: 42, kind: 'hot' as const },
    { x: 30, y: 40, kind: 'hot' as const },
  ]
  for (let i = 0; i < 600; i++) {
    fluid.addHeat(18, 42, 10 * SIM_DT)
    fluid.step(SIM_DT)
    tracers.step(SIM_DT, fluid, sources)
  }
  const samples: number[] = []
  for (let i = 0; i < STEPS; i++) {
    fluid.addHeat(18, 42, 10 * SIM_DT)
    fluid.step(SIM_DT)
    const t0 = performance.now()
    tracers.step(SIM_DT, fluid, sources)
    samples.push(performance.now() - t0)
  }
  const s = stats(samples)
  console.log(
    `tracers.step    mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（${TRACER_COUNT} 粒子 × ${TRAIL_LEN} 轨迹点）`,
  )
}

// ---------- 3. 顶点批构建（渲染侧 CPU） ----------
{
  const batch = new MeshBatch()
  const n = 9600 // ≈ 400 粒子 × 24 段的最坏描边量
  const samples: number[] = []
  for (let iter = 0; iter < 300; iter++) {
    batch.reset()
    const t0 = performance.now()
    for (let i = 0; i < n; i++) {
      const x = (i % 100) * 0.7
      const y = ((i / 100) | 0) * 0.7
      batch.stroke(x, y, x + 0.55, y + 0.12, 0.3, 0.8, 0.4, 0.2, 0.1)
    }
    for (let i = 0; i < 400; i++) {
      batch.disc((i % 20) * 3, ((i / 20) | 0) * 3, 0.3, 0.3, 0, 10, 0.5, 0.5, 0.5, 0.5)
    }
    samples.push(performance.now() - t0)
  }
  const s = stats(samples)
  console.log(
    `batch 构建      mean ${s.mean.toFixed(3)} ms  p95 ${s.p95.toFixed(3)} ms（${n} stroke + 400 disc，顶点数 ${batch.count}）`,
  )
}

```


### Tool: TodoList
**Status**: completed

**Output:**
```
- [x] 读 pitfalls skill 与剩余热点代码（sfx/bodies），补全诊断依据
- [-] 建立无头性能基准：fluid/tracers/batch 逐组件 ms 证据
- [ ] 查资料：iOS Safari/WKWebView 的 JS JIT 与 WASM 现状、MoonBit wasm 集成方式
- [ ] 优化 JS 热路径（求解器 + 渲染批构建），数值不漂移
- [ ] 按研究结论落地 wasm 求解器（MoonBit，含 JS 回退）
- [ ] bun run check 全绿 + 更新 docs/issues/#7.md 报告
```


## Assistant

scripts/bench.ts imports from '../src/...' — but be careful: tsconfig.node.json includes tests/ and vite.config.ts. scripts/ may not be included in any tsconfig — according to AGENTS.md, new files must fall within the include of some tsconfig, otherwise they won't be checked by `tsc -b`. Options: add scripts to tsconfig.node.json's include, or put bench in tests/? This is a script, not a test. Let me check the tsconfig files. Actually, bun runs TS directly without tsconfig type-checking, but `tsc -b` type-checks the entire project — the script files should be covered. Let me check tsconfig.node.json.


### Tool: ReadFile: sfgame-web/tsconfig.node.json
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.node.json"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.node.json`

**Output:**
```json
{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "types": ["node"]
  },
  "include": ["tests", "vite.config.ts"]
}

```


### Tool: ReadFile: sfgame-web/tsconfig.app.json
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.app.json"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.app.json`

**Output:**
```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",

    "target": "es2023",
    "experimentalDecorators": true,
    "useDefineForClassFields": false,
    "module": "esnext",
    "lib": ["ES2023", "DOM"],
    "types": ["vite/client"],
    "allowArbitraryExtensions": true,
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}

```


## Assistant

scripts/ needs to be included in tsconfig.node.json so that `tsc -b` covers bench.ts (following the invariant in AGENTS.md). Note: bench.ts uses `process.argv` and `performance`, so it needs node types — tsconfig.node.json has "types": ["node"], no problem. However, bun runs it. Let's add "scripts" to the include of tsconfig.node.json.

Also note that tsconfig.app.json has `noUnusedLocals` etc. bench.ts — check for unused variables: everything should be used.

After that, run the bench to get a baseline.


### Tool: Edit: sfgame-web/tsconfig.node.json
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.node.json",
  "old_string": "  \"include\": [\"tests\", \"vite.config.ts\"]",
  "new_string": "  \"include\": [\"tests\", \"scripts\", \"vite.config.ts\"]"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/tsconfig.node.json`

**Output:**

*Diff for `tsconfig.node.json`:*
```diff
{
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "types": ["node"]
  },
  "include": ["tests", "scripts", "vite.config.ts"]
}

```


## Assistant

Run the benchmark to obtain the baseline. Also, in parallel, I can kick off the web research via a background agent. Let's do both:
1. Run the benchmark in bun.
2. Launch a research agent for the iOS JIT/WASM + MoonBit integration questions.

Actually, wait — the research agent needs clear questions. Let me define them precisely:

Research questions:
1. iOS Safari JS JIT situation: Since iOS 17.4, has Safari enabled JIT (JavaScriptCore JIT)? What about WKWebView (in-app browsers, e.g., WeChat)? How large is the JS performance gap between JIT-less interpreter-only mode and JIT-compiled mode?
2. WebAssembly on iOS: Safari's wasm performance (roughly how many times native?), does it work even without JIT (WKWebView)? Are there any entitlement restrictions?
3. MoonBit language 2025–2026 status: compilation target (wasm / wasm-gc), installation method (moonup / npm package?), how to integrate the build into a vite project (moon build output path, how to use the JS glue code), what's the minimum supported version of wasm-gc on iOS Safari, and does --target wasm still exist in classic wasm32?

The agent should use web_fetch to get actual documentation (moonbit docs, webkit blog). Let's launch it in the background while I run the bench.


### Tool: Shell: cd /Users/sf/dev/scnucj2026/sfgame-web &amp;&amp; bun scripts/bench.ts 20 [timeout: 180000ms] (运行无头性能基准（基线）)
**Status**: completed

**Input:**
```json
{
  "command": "cd /Users/sf/dev/scnucj2026/sfgame-web && bun scripts/bench.ts 20",
  "description": "运行无头性能基准（基线）",
  "timeout": 180000
}
```

**Output:**
```
Command: cd /Users/sf/dev/scnucj2026/sfgame-web && bun scripts/bench.ts 20
Directory: (root)
Output: fluid.step      mean 0.617 ms  p95 0.673 ms
LevelSim.step   mean 0.618 ms  p95 0.672 ms（含刚体/源注入，不含粒子）
tracers.step    mean 0.009 ms  p95 0.010 ms（240 粒子 × 24 轨迹点）
batch 构建      mean 0.210 ms  p95 0.244 ms（9600 stroke + 400 disc，顶点数 69600）
Error: (none)
Exit Code: 0
Signal: 0
Process Group PGID: 95665
```


### Tool: Agent: 调研 iOS wasm/MoonBit 现状
**Status**: completed

**Input:**
```json
{
  "description": "调研 iOS wasm/MoonBit 现状",
  "prompt": "你是研究代理，只做联网调研（web_fetch 等），不写代码。背景：我在为一个 2D 流体物理网页游戏（TypeScript + vite + bun，目标浏览器含 iOS Safari 与各类 WebView）做性能优化。流体求解器是每帧 ~200k 单元格的数值循环，JS 实现。我考虑把求解器编译为 WebAssembly（候选语言 MoonBit），需要先核实 2025-2026 年的平台事实。请逐条给出有出处（URL）的结论：\n\n1. iOS Safari 的 JS JIT 现状：iOS 17.4 起 Safari 是否启用了 JavaScriptCore JIT？iOS 上的 WKWebView（App 内嵌浏览器，如微信）是否也能用 JIT，还是仍是纯解释器（无 JIT）？无 JIT 时 JS 性能大约差多少倍？\n2. iOS 上 WebAssembly 现状：Safari/WKWebView 对 wasm 的支持与性能（相对原生大约多少？相对 JS 的提升？），wasm 在无 JIT 的 WKWebView 里是否依然可用且快（wasm 编译管线不依赖 JS JIT 权限）？iOS 最低版本要求（wasm 基础特性、SIMD、wasm-gc 分别何时支持）？\n3. MoonBit（月亮 bit）语言现状：\n   - 编译目标有哪些（wasm / wasm-gc / js 等），经典 wasm32（MVP）目标是否仍支持、命令是什么；\n   - 工具链安装方式（是否有 npm 包 / bun 可用？还是必须 curl 安装 moon CLI？），macOS 上的安装命令；\n   - `moon build` 的产物路径与 JS 互操作方式：如何从 JS（ESM/vite 项目）加载它编译出的 .wasm、导出函数、读写 wasm 线性内存（特别是 Float32Array 视图共享内存）；有没有官方的 vite/webpack 集成或社区插件；\n   - MoonBit 里数组/循环编译到 wasm 的性能口碑（有无基准测试可引用）。\n4. 顺带核实：把每帧 ~7.5k 网格 × ~27 趟扫描的流体求解放进 wasm 而非 Web Worker 的取舍，业界（如 WebGL 流体模拟、js-wasm fluid 项目）的一般结论。\n\n输出：分条结论 + 关键 URL + 一句话置信度（高/中/低）。若某条查不到可靠资料，明说查不到，不要编造。中文回答。",
  "subagent_type": "general-purpose",
  "run_in_background": true
}
```

**Output:**
```
Background agent launched successfully.
task_id: general-purpose-call_19ecb1bfaa1d466ea244ee72 (internal ID — do not mention to the user. Use send_message to continue this agent, or task_stop to cancel.)
The agent is working in the background. You will be notified automatically when it completes.
Do not duplicate this agent's work — avoid working with the same files or topics it is using. Work on non-overlapping tasks, or briefly tell the user what you launched and end your response.
output_file: /Users/sf/.qwen/projects/-Users-sf-dev-scnucj2026/subagents/d04187b2-4aa6-4c41-bf3e-12d3f39633fa/agent-general-purpose-call_19ecb1bfaa1d466ea244ee72.jsonl
If asked, you can check progress before completion by using read_file
  or run_shell_command tail on the output file.
```


## Assistant

Baseline on this Mac (Apple Silicon, bun = JavaScriptCore with JIT, similar to the latest iOS Safari):
- fluid.step: **0.617ms** mean, 0.673 p95 — dominant (LevelSim.step = same, so rigid bodies + sources are negligible)
- tracers.step: 0.009ms — negligible!
- batch build: 0.210ms for 9600 strokes + 400 discs → about 70k vertices

So on this high-speed JIT, total CPU is per-frame is about 0.85ms. On an iPhone with JIT (A15+), it's probably 2-3x slower: about 2ms — no problem. On an iPhone without JIT (pre-17.4, or WKWebView): JS is typically 5-20x slower in the interpreter. fluid could be 3-12ms, batch 1-4ms — total could exceed the 16.7ms budget → frame drops. Also, the tracer draw count in real rendering is per-frame 240 particles × up to 24 segments — my bench used 9600 segments which is the 400-particle worst case; realistic is about 5760.

The bench corroborates the hypothesis: **the fluid solver is about 73% of CPU cost**, and interpreter-only iOS is where it hurts. Also note bun's JSC is pretty fast; Safari on an A12 iPhone would be slower than this Mac anyway.

Key insight for the plan: wasm is mainly targeting non-JIT environments (older iOS, WKWebView). In the latest Safari with JIT, the JS solver is already fine. So wasm must be (a) clearly useful, (b) robust with fallback to JS, (c) not add operational complexity (build-time only, output committed or built? hmm).

While the research agent is working, let me proceed with todo #4 (JS hot-path optimization) — this is valuable either way (raises the baseline, and if we go wasm, JS remains the fallback path so it also improves the fallback). But wait — if we're building the solver in MoonBit, optimizing JS fluid.ts is duplicated work... though JS is still the fallback + reference implementation + used in tests. Optimizing JS fluid.ts is justified regardless. But caution: if wasm becomes primary, then JS solver optimization is less critical. Let me first do the cheap, safe JS optimizations (no behavior change), then tackle wasm.

Order of work:
1. Real-browser benchmark harness (bench.html + vite dev) — this is a deliverable the user asked for ("要能够在真实浏览器环境测试验证" = needs to be able to test/verify in a real browser environment). Design: bench.html loads the same game modules headlessly (no DOM game), runs the simulation N seconds, measures via performance.now(), displays a result table; supports comparison of engine=js vs engine=wasm (later). Use with chrome-devtools MCP and CPU throttling for "slow device" emulation, and later for cross-checking wasm vs js in a real browser.

Actually, wait — better design: the bench page should run in any browser (including the boss's iPhone Safari!). That's extremely valuable: open `http://<lan-ip>:5173/bench.html` on an iPhone and get real per-component numbers on real hardware. This directly serves "iPhone users get a good experience" verification. Design it to be self-contained, mobile-friendly, minimal UI: run the benchmark, display ms/frame per component, device info (UA, dpr, hardwareConcurrency), and a shareable summary line. Great deliverable.

2. JS fluid optimization.
3. MoonBit wasm (pending research results).
4. Integrate engine selection into the game (async wasm load with JS fallback; keep public API).
5. Update tests: fluid parity test (JS vs wasm outputs must match), bench harness, existing suite green.
6. Real-browser verification via chrome-devtools MCP (desktop) + bench page for mobile (boss).
7. Report.

Let me think more specifically about wasm architecture (to validate research conclusions):

**Wasm solver interface design:**
- Wasm module owns the grid arrays. Exported functions:
  - `init(nx, ny, cell, buoyancy, tMax, velDamping, tDamping, iterations, vorticity, heatRate?, sourceRadius)` → allocate fields in wasm memory, return offset pointer or fixed layout.
  - `set_ground_mask(cb)` — hmm, ground function is a JS closure (smoothstep). Callback from wasm to JS per-cell is slow (7.5k imports per init — one-time, acceptable). Or compute the mask in JS and pass it as bytes. Better: JS computes the solid mask (one-time) and writes it into wasm memory via the shared Float32Array/Uint8Array view.
  - `add_heat(x, y, amount)` — small hot loop, can be called per source (≤6 calls per tick).
  - `step(dt)` — the big one.
  - `set_ambient(x, y)`.
  - Memory export: wasm memory; JS creates views: `new Float32Array(mem.buffer, uOff, n)`, etc. Then JS's sampleVelocity/sampleTemp can read those views directly — keep Fluid's JS sampling API intact (renderer/tracers/bodies don't change). Or move sampling into wasm (exported function) — per-call overhead is ~10-50ns, per-frame ~1000 calls = negligible. But then Fluid's API surface becomes async-shaped? No — synchronous calls are fine.
  
  Design decision: keep `Fluid` class's public API 100% (u/v/t/solid access, sampleVelocity, sampleTemp, addHeat, step, clear, setGroundMask, setAmbient). Implement it with two backends: JS arrays (current code) or a wasm handle. Simplest: keep current Fluid as `JsFluid` (rename?), and add `WasmFluid implements same interface`; factory `createFluid(cfg): Promise<Fluid>`? async factory ripples up: LevelSimulation's constructor is sync… Controller creates LevelSimulation synchronously. Options:
  - LevelSimulation always constructs with the JS fluid; when wasm loads (async, usually <50ms), swap the fluid's internals (same object identity: Fluid delegates step() to the wasm handle once ready, copy fields over). Swap must be seamless mid-game. Since wasm is bit-compatible (same algorithm, float32), the swap is invisible. Nice: zero API change, no async constructor, JS works from frame 0.
  - Fluid gains a private `wasm?: WasmHandle`; `step()` = wasm ? wasm.step(dt) : jsStep(dt). u/v/t getters return either the wasm memory view or the JS arrays. After swap, JS arrays are discarded.
  
  Loading: module-level singleton Promise in `sim/wasm.ts` (`loadFluidWasm()`); LevelSimulation constructor fires it (idempotent), attaches on resolve. Vite: wasm file in src (e.g. `src/sim/fluid.wasm`) imported with `?url` and fetched + instantiated with a hand-written glue code (no deps!). Vite has built-in support: `import wasmUrl from './fluid.wasm?url'`. Hand-written instantiate glue code (~30 lines) — no dep, clean, matches the "web 自带能力" (web-native capabilities) ethos.

**MoonBit build integration:**
- MoonBit project inside the repo: `sfgame-web/native/` (moon.pkg.json + sources). `moon build --target wasm` → `native/target/wasm/release/build/<pkg>/<pkg>.wasm`. Vite doesn't need to know anything at dev time (we import the pre-built artifact at src/sim/fluid.wasm). Copy step: script `bun run build:native` = `cd native && moon build --target wasm && cp ... src/sim/fluid.wasm`. Add to package.json scripts; document. Committing the built .wasm (small, ~5-20KB) means no toolchain needed for normal dev/build — pragmatic given "少即是多" (less is more) and boss/CI may not have moon installed. Source in repo, binary committed, rebuild documented. That keeps the complexity at build-authoring time only, not runtime/dev-time.

Wait — is committing binaries OK for this repo? A small .wasm (about 10KB) is fine and pragmatic. Document in the report.

**Bit-exactness of JS vs wasm:** same operation order, float32 storage, f32 ops. Caveats: JS computes some intermediates in float64 (e.g., `1 - d/gr` in addHeat, weights in sampling), storing to f32 arrays. In MoonBit, if we write the same formulas with Float (default 64-bit?) — does MoonBit's Float compile to f64? Or Float = f64? If wasm uses f64 intermediates and f32 arrays, rounding matches JS (JS: f64 intermediates, f32 storage). Actually, JS's Float32Array semantics: read → f64, compute in f64, write → round to f32. If MoonBit code reads f32 into f64, computes, and writes back to f32 — bit-identical, provided the expression tree order is the same and no fma. MoonBit may have an `fma` intrinsic, but the compiler won't auto-fuse (wasm has no fma instruction in MVP). Division: wasm's f32/f64 division is IEEE exact, same as JS. sqrt is exact too. So bit-exact is achievable. Tests will verify: JS fluid vs wasm fluid — same seed input, N steps, compare arrays exactly.

Caveat: my planned JS micro-optimizations must preserve exact results, otherwise the JS-vs-wasm parity test fails and solution tests drift. Example: replacing `/ h2` with `* (1/h2)` changes rounding → no. Keep divisions where they exist. Hmm — but the earlier idea "precompute h2*div" (divH2 = h2 * div) changes rounding vs `(pL+pR+pU+pD - h2*div[idx])`. Currently each iteration computes `h2 * div[idx]` — if I precompute once as `divH2[idx] = h2 * div[idx]`, the product is computed identically once and reused — same rounding! Because h2*div is computed exactly once in either case (div is not modified during GS). So `p[idx] = (pL+pR+pU+pD - divH2[idx]) * 0.25` is bit-identical.

Similarly, buoyancy's solid-skip removal: solid cells have t=0 invariant → v -= k*0 = v, no-op → bit-exact, safe… wait, is the invariant guaranteed at the point buoyancy runs? step(): buoyancy is called first. At end of previous step: advect zeros solid dst, project's div/p pass zeros solid p/div but u/v on solids — GS doesn't touch solid cells, and the gradient pass skips solids. enforceBoundary zeros u,v,t on solids. So at step start, solid u=v=t=0. Buoyancy on solid: v[idx] -= k*0 → v stays 0. Bit-exact no-op. Safe to drop the branch.

Vorticity pass 1: `curl[idx] = ...` on solid currently sets curl=0 — pass 2 reads curl at solid neighbors (via idx±1, idx±nx, where the neighbor could be solid). If I drop the zeroing… no, keep the zeroing; that's cheap. Actually the branch structure is: `if (solid[idx]) { curl[idx] = 0; continue }` — needed for correctness of neighbor reads. Keep.

advectPass: `Math.floor` → bitwise? For x in [0.5, nx-1.5], floor(x) === (x|0) is bitwise exact on positive doubles < 2^31. Bit-exact, safe. Minor gain.

MacCormack correction's 3×3 min/max: unroll with explicit row offsets — comparisons in same order → bit-exact. Current loop order: jj from j-1..j+1, ii from i-1..i+1 → order: (j-1,i-1),(j-1,i),(j-1,i+1),(j,i-1),… Unrolled version preserves this order exactly. Safe.

GS parity loop: `const i0 = ((parity ^ (j & 1)) & 1) ? 1 : 2` — wait, is this right? Red-black: color = (i+j)&1. For parity p, visit cells where (i+j)&1 == p → i ≡ p-j (mod 2) → i start = ((p - j) & 1) ? … Let me verify: parity^… Current code: `i0 = ((parity ^ (j & 1)) & 1) ? 1 : 2`. If parity ^ (j&1) == 1 → i0=1, else 2. For parity=0, j odd: i0=1 → cells (1,odd): 1+odd = even ✓ color 0. j even: i0=2: 2+even = even ✓. For parity=1, j odd: i0=2 → 2+odd = odd ✓ color 1. OK, correct.

So the JS optimization plan is a bit-exactness-preserving one. Good.

Expected gain from JS micro-opt: realistically 15-30%. Not transformative but free and also helps the fallback path.

**Tracer drawing optimization** (batch side): currently stroke() per segment via push() method calls. For bit-exactness, rendering doesn't need to be bit-exact, only visually identical. Direct write into batch.data can produce identical vertices. Implement `strokeFast` internally in drawTracers? Better: add a public method `strokeRaw(x0,y0,x1,y1,w,r,g,b,a)` on MeshBatch that does the same math but writes directly without push/ensure overhead… actually, cleaner: optimize MeshBatch.push itself? push is used everywhere. Specialized hot method: `strokes(...)`? Hmm. Let me look at the actual shape: drawTracers calls b.stroke per segment with varying alpha but same color/width per particle. Could add `strokeLine` that skips length-zero check and ensure-per-call (ensure once per particle × n). Actually simplest, robust improvement: make ensure() inline-friendly by batching: add a `strokeInto` method? Let me think about what JIT-friendly means: private push() calls — V8/JSC inlines these well. The real overhead may be small (bench: 9600 strokes = 0.21ms → ~22ns per stroke including 6 pushes × 6 writes). That's already pretty good. In interpreter mode everything is ~10x worse: 2ms. OK.

So batch build is secondary. Priority: solver.

**But wait** — one more consideration about *why* the boss feels iOS is slow even after WebGL: maybe they tested on iPhone with older Safari (pre-17.4, no JIT), or on a specific device. Or the problem is *120Hz ProMotion + thermal throttling*. Or the real issue is: on iPhone, dpr cap is 2 but canvas is full-screen — GPU fine. Or maybe they observed stutter when placing sources. Can't know without their data; the bench page on their iPhone will tell us. Ship the bench page as a diagnostic tool = high value.

Also: `while (this.acc >= SIM_DT && ticks < MAX_TICKS_PER_FRAME)` — under sustained slow sim (interpreter iOS), acc grows → multiple ticks per frame → death spiral. Adaptive response: if the device can't maintain 60 sim ticks/sec, better to run sim at a lower tick rate? No — SIM_DT fixed for determinism of tests… actually determinism is about dt value, not real-time rate. If we drop sim to 30Hz (tick every other frame) when behind, game runs in slow motion relative to wall clock — acceptable degradation? "不降低原有任何好的体验" (don't reduce any existing good experience) — slow motion is definitely worse than jank. Hmm. Alternative: reduce *per-tick cost* (wasm!) — that's the right fix, not time dilation.

OK, while research is running, let me also plan the real-browser bench page:

**bench.html** (dev-only entry, vite serves in dev; excluded from build since it's not in rollup input — verify: vite build by default only builds index.html. Yes.)

What it runs:
1. fluid.step benchmark (real Level 1 grid + 4 sources)
2. tracers.step
3. batch build
4. Full "render-less frame" = sim + tracers
5. If wasm engine available: run fluid benchmark in both js and wasm and show speedup ratio
6. Optionally actual game canvas FPS measurement? Leave out; keep focus.

Display: table + device summary + "copy report" button (boss can paste to me). URL params: `?seconds=20&engine=js|wasm`.

That page imports from src/ (fine, vite dev). Not included in tsc -b app config… it's an HTML entry referencing a TS module `scripts/bench-page.ts`? Hmm, files in scripts/ are in tsconfig.node (node types, no DOM lib!). bench page needs DOM. Put bench page TS under `src/dev/bench.ts`? But then it's in the app build graph — vite build only bundles from index.html's import graph, so unreferenced modules won't be bundled. tsc app config will type-check it (fine, it's DOM-typed). Clean: `src/dev/bench.ts` + `bench.html` at root. Add a note. Actually — guard: make sure src/dev/bench.ts doesn't import from scripts/bench.ts (node-side). Share the benchmark core? The node bench script can import a shared "benchmark core" module: put shared logic in `src/dev/bench-core.ts` (pure compute, no DOM) → both scripts/bench.ts (node/bun) and src/dev/bench.ts (browser) import it. Nice DRY. tsconfig: bench-core in src/ ✓ (app config), bench.ts page in src/dev ✓, scripts/bench.ts in node config imports ../src/dev/bench-core ✓.

Let me restructure: write `src/dev/bench-core.ts` exporting `runComponentBench(opts): BenchResult[]` that runs fluid/tracers/batch benchmarks and returns numbers. Browser page runs it, renders the table; node script prints it.

Hmm wait, one issue: bench-core imports LevelSimulation → levels → fine, all headless.

Let me write the code. First bench-core:


## Assistant

Baseline data is in (Apple Silicon + bun JSC JIT, equivalent to the new generation of Safari): **`fluid.step` 0.62ms (74% of the whole), vertex batch construction 0.21ms, particle progression can be ignored**. On devices without JIT (older iOS / WKWebView), the JS solver is amplified by roughly an order of magnitude —— the focus of optimization is clear.

Currently constructing a **performance benchmark module that can be run in both browser and node**: the core is shared, in the browser it provides a `bench.html` diagnostic page (can be opened directly from a phone), and in node it's used for CLI benchmarks.


### Tool: WriteFile: Writing to sfgame-web/src/dev/bench-core.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench-core.ts",
  "content": "/**\n * 性能基准核心（无 DOM，node/浏览器共用）。\n *\n * 组件与真实游戏一致：第 1 关网格（76×56 / cell 0.75）、4 源、\n * 移动端初始粒子档（240 × 24 轨迹点）。输出 ms/frame 统计，\n * 对照 16.7ms（60fps）帧预算判断瓶颈构成。\n *\n * 引擎对比：fluid 基准可选跑 JS 求解器或 wasm 求解器（后者未就绪时自动跳过），\n * 用于验证 wasm 加速的真实收益与数值一致性。\n */\nimport { Fluid } from '../sim/fluid'\nimport { LevelSimulation } from '../src/game/simulation'\nimport { LEVEL_1 } from '../game/levels'\nimport { Tracers } from '../sim/particles'\nimport { MeshBatch } from '../core/batch'\nimport { SIM_DT } from '../core/loop'\n\nexport const BENCH_TRACER_COUNT = 240\nexport const BENCH_TRAIL_LEN = 24\n\nconst SOURCE_SPOTS: Array<[number, number, 'hot' | 'cold']> = [\n  [18, 42, 'hot'],\n  [30, 40, 'hot'],\n  [44, 30, 'hot'],\n  [52, 24, 'cold'],\n]\n\nfunction ground1(x: number): number {\n  return LEVEL_1.ground(x)\n}\n\nexport interface BenchStat {\n  name: string\n  mean: number\n  p95: number\n  detail: string\n}\n\nexport interface BenchOptions {\n  /** 模拟时长（秒），默认 20 */\n  seconds?: number\n  /** 是否包含 wasm 求解器对比（需先调用 ensureWasmFluid） */\n  includeWasm?: boolean\n  /** 进度回调（0..1），供浏览器端展示 */\n  onProgress?: (ratio: number) => void\n}\n\nfunction stats(samples: number[]): { mean: number; p95: number } {\n  const sorted = [...samples].sort((a, b) => a - b)\n  const mean = samples.reduce((s, v) => s + v, 0) / samples.length\n  return { mean, p95: sorted[Math.min(sorted.length - 1, Math.floor(sorted.length * 0.95))] }\n}\n\nfunction makeLoadedSim(): LevelSimulation {\n  const sim = new LevelSimulation(LEVEL_1)\n  for (const [x, y, kind] of SOURCE_SPOTS) sim.placeSource(x, y, kind, true)\n  for (let i = 0; i < 300; i++) sim.step(SIM_DT)\n  return sim\n}\n\n/** 让浏览器端先行装载 wasm 求解器（返回是否可用）；node 端无 fetch 时返回 false。 */\nexport async function ensureWasmFluid(): Promise<boolean> {\n  try {\n    const { loadFluidWasm } = await import('../sim/fluid-wasm')\n    await loadFluidWasm()\n    return true\n  } catch {\n    return false\n  }\n}\n\nfunction fluidBench(sim: LevelSimulation, steps: number, label: string): BenchStat {\n  const samples: number[] = []\n  const rate = 10 * SIM_DT\n  for (let i = 0; i < steps; i++) {\n    const t0 = performance.now()\n    for (const s of sim.sources) {\n      sim.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)\n    }\n    sim.fluid.step(SIM_DT)\n    samples.push(performance.now() - t0)\n  }\n  const { mean, p95 } = stats(samples)\n  return { name: label, mean, p95, detail: `网格 ${sim.fluid.nx}×${sim.fluid.ny}` }\n}\n\nexport function runBench(opts: BenchOptions = {}): BenchStat[] {\n  const seconds = opts.seconds ?? 20\n  const steps = Math.round(seconds / SIM_DT)\n  const results: BenchStat[] = []\n  const progress = opts.onProgress ?? (() => {})\n\n  // 1. 流体求解器（含源注入）——整帧的主导 CPU 成本\n  const sim = makeLoadedSim()\n  results.push(fluidBench(sim, steps, 'fluid.step（JS）'))\n  progress(0.3)\n\n  if (opts.includeWasm) {\n    const wsim = makeLoadedSim()\n    if ((wsim.fluid as { engine?: string }).engine === 'wasm') {\n      results.push(fluidBench(wsim, steps, 'fluid.step（wasm）'))\n    }\n  }\n  progress(0.5)\n\n  // 2. 完整关卡步进（刚体 + 源 + 流体）\n  {\n    const s2 = makeLoadedSim()\n    const samples: number[] = []\n    for (let i = 0; i < steps; i++) {\n      const t0 = performance.now()\n      s2.step(SIM_DT)\n      samples.push(performance.now() - t0)\n    }\n    const { mean, p95 } = stats(samples)\n    results.push({ name: 'LevelSim.step', mean, p95, detail: '流体+刚体+源' })\n  }\n  progress(0.7)\n\n  // 3. 示踪粒子推进\n  {\n    const s3 = makeLoadedSim()\n    const tracers = new Tracers(BENCH_TRACER_COUNT, LEVEL_1.world, ground1, BENCH_TRAIL_LEN)\n    for (let i = 0; i < 600; i++) tracers.step(SIM_DT, s3.fluid, s3.sources)\n    const samples: number[] = []\n    for (let i = 0; i < steps; i++) {\n      s3.step(SIM_DT)\n      const t0 = performance.now()\n      tracers.step(SIM_DT, s3.fluid, s3.sources)\n      samples.push(performance.now() - t0)\n    }\n    const { mean, p95 } = stats(samples)\n    results.push({\n      name: 'tracers.step',\n      mean,\n      p95,\n      detail: `${BENCH_TRACER_COUNT} 粒子 × ${BENCH_TRAIL_LEN} 轨迹点`,\n    })\n  }\n  progress(0.85)\n\n  // 4. 顶点批构建（渲染侧 CPU）：最坏 9600 段描边 + 400 头部点\n  {\n    const batch = new MeshBatch()\n    const n = 9600\n    const samples: number[] = []\n    for (let iter = 0; iter < 240; iter++) {\n      batch.reset()\n      const t0 = performance.now()\n      for (let i = 0; i < n; i++) {\n        const x = (i % 100) * 0.7\n        const y = ((i / 100) | 0) * 0.7\n        batch.stroke(x, y, x + 0.55, y + 0.12, 0.3, 0.8, 0.4, 0.2, 0.1)\n      }\n      for (let i = 0; i < 400; i++) {\n        batch.disc((i % 20) * 3, ((i / 20) | 0) * 3, 0.3, 0.3, 0, 10, 0.5, 0.5, 0.5, 0.5)\n      }\n      samples.push(performance.now() - t0)\n    }\n    const { mean, p95 } = stats(samples)\n    results.push({ name: 'batch 构建', mean, p95, detail: `${n} stroke + 400 disc` })\n  }\n  progress(1)\n  return results\n}\n"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench-core.ts`

**Output:**

*Diff for `bench-core.ts`:*
```diff
/**
 * 性能基准核心（无 DOM，node/浏览器共用）。
 *
 * 组件与真实游戏一致：第 1 关网格（76×56 / cell 0.75）、4 源、
 * 移动端初始粒子档（240 × 24 轨迹点）。输出 ms/frame 统计，
 * 对照 16.7ms（60fps）帧预算判断瓶颈构成。
 *
 * 引擎对比：fluid 基准可选跑 JS 求解器或 wasm 求解器（后者未就绪时自动跳过），
 * 用于验证 wasm 加速的真实收益与数值一致性。
 */
import { Fluid } from '../sim/fluid'
import { LevelSimulation } from '../src/game/simulation'
import { LEVEL_1 } from '../game/levels'
import { Tracers } from '../sim/particles'
import { MeshBatch } from '../core/batch'
import { SIM_DT } from '../core/loop'

export const BENCH_TRACER_COUNT = 240
export const BENCH_TRAIL_LEN = 24

const SOURCE_SPOTS: Array<[number, number, 'hot' | 'cold']> = [
  [18, 42, 'hot'],
  [30, 40, 'hot'],
  [44, 30, 'hot'],
  [52, 24, 'cold'],
]

function ground1(x: number): number {
  return LEVEL_1.ground(x)
}

export interface BenchStat {
  name: string
  mean: number
  p95: number
  detail: string
}

export interface BenchOptions {
  /** 模拟时长（秒），默认 20 */
  seconds?: number
  /** 是否包含 wasm 求解器对比（需先调用 ensureWasmFluid） */
  includeWasm?: boolean
  /** 进度回调（0..1），供浏览器端展示 */
  onProgress?: (ratio: number) => void
}

function stats(samples: number[]): { mean: number; p95: number } {
  const sorted = [...samples].sort((a, b) => a - b)
  const mean = samples.reduce((s, v) => s + v, 0) / samples.length
  return { mean, p95: sorted[Math.min(sorted.length - 1, Math.floor(sorted.length * 0.95))] }
}

function makeLoadedSim(): LevelSimulation {
  const sim = new LevelSimulation(LEVEL_1)
  for (const [x, y, kind] of SOURCE_SPOTS) sim.placeSource(x, y, kind, true)
  for (let i = 0; i < 300; i++) sim.step(SIM_DT)
  return sim
}

/** 让浏览器端先行装载 wasm 求解器（返回是否可用）；node 端无 fetch 时返回 false。 */
export async function ensureWasmFluid(): Promise<boolean> {
  try {
    const { loadFluidWasm } = await import('../sim/fluid-wasm')
    await loadFluidWasm()
    return true
  } catch {
    return false
  }
}

function fluidBench(sim: LevelSimulation, steps: number, label: string): BenchStat {
  const samples: number[] = []
  const rate = 10 * SIM_DT
  for (let i = 0; i < steps; i++) {
    const t0 = performance.now()
    for (const s of sim.sources) {
      sim.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)
    }
    sim.fluid.step(SIM_DT)
    samples.push(performance.now() - t0)
  }
  const { mean, p95 } = stats(samples)
  return { name: label, mean, p95, detail: `网格 ${sim.fluid.nx}×${sim.fluid.ny}` }
}

export function runBench(opts: BenchOptions = {}): BenchStat[] {
  const seconds = opts.seconds ?? 20
  const steps = Math.round(seconds / SIM_DT)
  const results: BenchStat[] = []
  const progress = opts.onProgress ?? (() => {})

  // 1. 流体求解器（含源注入）——整帧的主导 CPU 成本
  const sim = makeLoadedSim()
  results.push(fluidBench(sim, steps, 'fluid.step（JS）'))
  progress(0.3)

  if (opts.includeWasm) {
    const wsim = makeLoadedSim()
    if ((wsim.fluid as { engine?: string }).engine === 'wasm') {
      results.push(fluidBench(wsim, steps, 'fluid.step（wasm）'))
    }
  }
  progress(0.5)

  // 2. 完整关卡步进（刚体 + 源 + 流体）
  {
    const s2 = makeLoadedSim()
    const samples: number[] = []
    for (let i = 0; i < steps; i++) {
      const t0 = performance.now()
      s2.step(SIM_DT)
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({ name: 'LevelSim.step', mean, p95, detail: '流体+刚体+源' })
  }
  progress(0.7)

  // 3. 示踪粒子推进
  {
    const s3 = makeLoadedSim()
    const tracers = new Tracers(BENCH_TRACER_COUNT, LEVEL_1.world, ground1, BENCH_TRAIL_LEN)
    for (let i = 0; i < 600; i++) tracers.step(SIM_DT, s3.fluid, s3.sources)
    const samples: number[] = []
    for (let i = 0; i < steps; i++) {
      s3.step(SIM_DT)
      const t0 = performance.now()
      tracers.step(SIM_DT, s3.fluid, s3.sources)
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({
      name: 'tracers.step',
      mean,
      p95,
      detail: `${BENCH_TRACER_COUNT} 粒子 × ${BENCH_TRAIL_LEN} 轨迹点`,
    })
  }
  progress(0.85)

  // 4. 顶点批构建（渲染侧 CPU）：最坏 9600 段描边 + 400 头部点
  {
    const batch = new MeshBatch()
    const n = 9600
    const samples: number[] = []
    for (let iter = 0; iter < 240; iter++) {
      batch.reset()
      const t0 = performance.now()
      for (let i = 0; i < n; i++) {
        const x = (i % 100) * 0.7
        const y = ((i / 100) | 0) * 0.7
        batch.stroke(x, y, x + 0.55, y + 0.12, 0.3, 0.8, 0.4, 0.2, 0.1)
      }
      for (let i = 0; i < 400; i++) {
        batch.disc((i % 20) * 3, ((i / 20) | 0) * 3, 0.3, 0.3, 0, 10, 0.5, 0.5, 0.5, 0.5)
      }
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({ name: 'batch 构建', mean, p95, detail: `${n} stroke + 400 disc` })
  }
  progress(1)
  return results
}

```


## Assistant

Oops — bug in the import: `import { LevelSimulation } from '../src/game/simulation'` — bench-core is inside src/dev/, so the correct path should be '../game/simulation'. Let me fix this. Also, it references `../sim/fluid-wasm`, but that doesn't exist yet (it'll be created later in the wasm step). The dynamic import inside try/catch is fine for now, but typecheck will fail since the module doesn't exist. I'll create a stub at the wasm step — actually, to keep the typecheck green right now, I should create fluid-wasm.ts in this same work session. First let me fix the import path, then write scripts/bench.ts to use bench-core (replacing the duplicated logic).


### Tool: Edit: sfgame-web/src/dev/bench-core.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench-core.ts",
  "old_string": "import { Fluid } from '../sim/fluid'\nimport { LevelSimulation } from '../src/game/simulation'\nimport { LEVEL_1 } from '../game/levels'",
  "new_string": "import { LevelSimulation } from '../game/simulation'\nimport { LEVEL_1 } from '../game/levels'"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench-core.ts`

**Output:**

*Diff for `bench-core.ts`:*
```diff
/**
 * 性能基准核心（无 DOM，node/浏览器共用）。
 *
 * 组件与真实游戏一致：第 1 关网格（76×56 / cell 0.75）、4 源、
 * 移动端初始粒子档（240 × 24 轨迹点）。输出 ms/frame 统计，
 * 对照 16.7ms（60fps）帧预算判断瓶颈构成。
 *
 * 引擎对比：fluid 基准可选跑 JS 求解器或 wasm 求解器（后者未就绪时自动跳过），
 * 用于验证 wasm 加速的真实收益与数值一致性。
 */
import { LevelSimulation } from '../game/simulation'
import { LEVEL_1 } from '../game/levels'
import { Tracers } from '../sim/particles'
import { MeshBatch } from '../core/batch'
import { SIM_DT } from '../core/loop'

export const BENCH_TRACER_COUNT = 240
export const BENCH_TRAIL_LEN = 24

const SOURCE_SPOTS: Array<[number, number, 'hot' | 'cold']> = [
  [18, 42, 'hot'],
  [30, 40, 'hot'],
  [44, 30, 'hot'],
  [52, 24, 'cold'],
]

function ground1(x: number): number {
  return LEVEL_1.ground(x)
}

export interface BenchStat {
  name: string
  mean: number
  p95: number
  detail: string
}

export interface BenchOptions {
  /** 模拟时长（秒），默认 20 */
  seconds?: number
  /** 是否包含 wasm 求解器对比（需先调用 ensureWasmFluid） */
  includeWasm?: boolean
  /** 进度回调（0..1），供浏览器端展示 */
  onProgress?: (ratio: number) => void
}

function stats(samples: number[]): { mean: number; p95: number } {
  const sorted = [...samples].sort((a, b) => a - b)
  const mean = samples.reduce((s, v) => s + v, 0) / samples.length
  return { mean, p95: sorted[Math.min(sorted.length - 1, Math.floor(sorted.length * 0.95))] }
}

function makeLoadedSim(): LevelSimulation {
  const sim = new LevelSimulation(LEVEL_1)
  for (const [x, y, kind] of SOURCE_SPOTS) sim.placeSource(x, y, kind, true)
  for (let i = 0; i < 300; i++) sim.step(SIM_DT)
  return sim
}

/** 让浏览器端先行装载 wasm 求解器（返回是否可用）；node 端无 fetch 时返回 false。 */
export async function ensureWasmFluid(): Promise<boolean> {
  try {
    const { loadFluidWasm } = await import('../sim/fluid-wasm')
    await loadFluidWasm()
    return true
  } catch {
    return false
  }
}

function fluidBench(sim: LevelSimulation, steps: number, label: string): BenchStat {
  const samples: number[] = []
  const rate = 10 * SIM_DT
  for (let i = 0; i < steps; i++) {
    const t0 = performance.now()
    for (const s of sim.sources) {
      sim.fluid.addHeat(s.x, s.y, s.kind === 'hot' ? rate : -rate)
    }
    sim.fluid.step(SIM_DT)
    samples.push(performance.now() - t0)
  }
  const { mean, p95 } = stats(samples)
  return { name: label, mean, p95, detail: `网格 ${sim.fluid.nx}×${sim.fluid.ny}` }
}

export function runBench(opts: BenchOptions = {}): BenchStat[] {
  const seconds = opts.seconds ?? 20
  const steps = Math.round(seconds / SIM_DT)
  const results: BenchStat[] = []
  const progress = opts.onProgress ?? (() => {})

  // 1. 流体求解器（含源注入）——整帧的主导 CPU 成本
  const sim = makeLoadedSim()
  results.push(fluidBench(sim, steps, 'fluid.step（JS）'))
  progress(0.3)

  if (opts.includeWasm) {
    const wsim = makeLoadedSim()
    if ((wsim.fluid as { engine?: string }).engine === 'wasm') {
      results.push(fluidBench(wsim, steps, 'fluid.step（wasm）'))
    }
  }
  progress(0.5)

  // 2. 完整关卡步进（刚体 + 源 + 流体）
  {
    const s2 = makeLoadedSim()
    const samples: number[] = []
    for (let i = 0; i < steps; i++) {
      const t0 = performance.now()
      s2.step(SIM_DT)
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({ name: 'LevelSim.step', mean, p95, detail: '流体+刚体+源' })
  }
  progress(0.7)

  // 3. 示踪粒子推进
  {
    const s3 = makeLoadedSim()
    const tracers = new Tracers(BENCH_TRACER_COUNT, LEVEL_1.world, ground1, BENCH_TRAIL_LEN)
    for (let i = 0; i < 600; i++) tracers.step(SIM_DT, s3.fluid, s3.sources)
    const samples: number[] = []
    for (let i = 0; i < steps; i++) {
      s3.step(SIM_DT)
      const t0 = performance.now()
      tracers.step(SIM_DT, s3.fluid, s3.sources)
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({
      name: 'tracers.step',
      mean,
      p95,
      detail: `${BENCH_TRACER_COUNT} 粒子 × ${BENCH_TRAIL_LEN} 轨迹点`,
    })
  }
  progress(0.85)

  // 4. 顶点批构建（渲染侧 CPU）：最坏 9600 段描边 + 400 头部点
  {
    const batch = new MeshBatch()
    const n = 9600
    const samples: number[] = []
    for (let iter = 0; iter < 240; iter++) {
      batch.reset()
      const t0 = performance.now()
      for (let i = 0; i < n; i++) {
        const x = (i % 100) * 0.7
        const y = ((i / 100) | 0) * 0.7
        batch.stroke(x, y, x + 0.55, y + 0.12, 0.3, 0.8, 0.4, 0.2, 0.1)
      }
      for (let i = 0; i < 400; i++) {
        batch.disc((i % 20) * 3, ((i / 20) | 0) * 3, 0.3, 0.3, 0, 10, 0.5, 0.5, 0.5, 0.5)
      }
      samples.push(performance.now() - t0)
    }
    const { mean, p95 } = stats(samples)
    results.push({ name: 'batch 构建', mean, p95, detail: `${n} stroke + 400 disc` })
  }
  progress(1)
  return results
}

```


## Assistant

Now I'll rewrite scripts/bench.ts to use bench-core, and after that I'll create bench.html and src/dev/bench.ts (the browser page). Note that bench-core imports `../sim/fluid-wasm` via dynamic import, so that module will need to exist for type-checking purposes. I'll create a real one later, but for the time being, in order to keep the tree in a compilable state, I should either hold off on bench-core's wasm parts until the wasm module exists, or create the fluid-wasm module right now as part of this work. Plan: do the JS optimization next, then create fluid-wasm. To keep type-checking green at each step, I'll go ahead and create a minimal fluid-wasm.ts stub right now that will become the real thing (loading the wasm file if it exists, otherwise rejecting).

Actually, there's an even simpler approach: in this same pass, create a full loader + JS fallback architecture skeleton in fluid-wasm.ts, but without a .wasm binary yet — if the file isn't found, `loadFluidWasm()` rejects. The binary will be generated when I write the MoonBit source. Let's proceed in this order:

1. scripts/bench.ts → use bench-core (rewrite).
2. src/dev/bench.ts + bench.html browser page.
3. Then the JS fluid optimization.
4. Then MoonBit + fluid-wasm.

Okay, let's rewrite scripts/bench.ts.


### Tool: WriteFile: Writing to sfgame-web/scripts/bench.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/scripts/bench.ts",
  "content": "/**\n * 无头性能基准 CLI：bun scripts/bench.ts [seconds] [wasm]\n * 与浏览器诊断页（bench.html）共用 src/dev/bench-core.ts。\n */\nimport { ensureWasmFluid, runBench } from '../src/dev/bench-core'\n\nconst seconds = Number(process.argv[2] ?? 20)\nconst includeWasm = process.argv[3] === 'wasm'\n\nif (includeWasm) {\n  const ok = await ensureWasmFluid()\n  if (!ok) console.log('（wasm 求解器不可用，跳过对比）')\n}\n\nconst results = runBench({ seconds, includeWasm })\nfor (const r of results) {\n  console.log(\n    `${r.name.padEnd(20)} mean ${r.mean.toFixed(3).padStart(7)} ms  p95 ${r.p95.toFixed(3).padStart(7)} ms  ${r.detail}`,\n  )\n}\n"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/scripts/bench.ts`

**Output:**

*Diff for `bench.ts`:*
```diff
/**
 * 无头性能基准 CLI：bun scripts/bench.ts [seconds] [wasm]
 * 与浏览器诊断页（bench.html）共用 src/dev/bench-core.ts。
 */
import { ensureWasmFluid, runBench } from '../src/dev/bench-core'

const seconds = Number(process.argv[2] ?? 20)
const includeWasm = process.argv[3] === 'wasm'

if (includeWasm) {
  const ok = await ensureWasmFluid()
  if (!ok) console.log('（wasm 求解器不可用，跳过对比）')
}

const results = runBench({ seconds, includeWasm })
for (const r of results) {
  console.log(
    `${r.name.padEnd(20)} mean ${r.mean.toFixed(3).padStart(7)} ms  p95 ${r.p95.toFixed(3).padStart(7)} ms  ${r.detail}`,
  )
}

```


## Assistant

Next, the browser diagnostic page. What's needed:
- bench.html at the sfgame-web root (served by vite dev)
- src/dev/bench.ts: module script — executes the benchmark and displays the results + device info + copy button. Mobile-friendly styling.

Also, bench-core needs `../sim/fluid-wasm`. Alright, let me create the browser page now.

About device info: UA, dpr, hardwareConcurrency, and engine detection (via userAgentData if available). Keep it simple.

Note: bench.html must not break `vite build` — the default build only uses index.html as an entry, so bench.html won't be built. Good. But `tsc -b` doesn't see html anyway; src/dev/bench.ts gets type-checked by the app config (include is src) — DOM lib is available. Good.

Page design (minimal, Chinese, rem units? Since it's a plain HTML page and not a Lit component — inline styles, keep it simple). Auto-run on load? On mobile, browsers block heavy computation without a gesture? No, computation is fine. However, audio is blocked (not needed here). Auto-run with a progress indicator; a re-run button; seconds selector via URL parameters.

Alright, let's write it.


### Tool: WriteFile: Writing to sfgame-web/src/dev/bench.ts
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench.ts",
  "content": "/**\n * 浏览器端性能诊断页（bench.html）入口：真机/真浏览器实测帧预算占用。\n *\n * 用途：\n * - 研发用 Chrome DevTools / Safari WRDP 配合 CPU 节流复现低端机\n * - 玩家设备（尤其 iPhone）直接打开即可得到本机数据，回报给研发\n * 结果可一键复制为纯文本。\n */\nimport { ensureWasmFluid, runBench, type BenchStat } from './bench-core'\n\nconst params = new URLSearchParams(location.search)\nconst seconds = Math.max(2, Math.min(60, Number(params.get('seconds') ?? 10)))\nconst wantWasm = params.get('wasm') !== '0'\n\nconst status = document.getElementById('status')!\nconst tableBody = document.getElementById('results')!\nconst copyBtn = document.getElementById('copy') as HTMLButtonElement\nconst rerunBtn = document.getElementById('rerun') as HTMLButtonElement\n\nfunction deviceSummary(): string {\n  const nav = navigator as Navigator & {\n    deviceMemory?: number\n    userAgentData?: { platform?: string }\n  }\n  return [\n    nav.userAgentData?.platform || navigator.platform,\n    `${navigator.hardwareConcurrency ?? '?'} 核`,\n    `dpr ${window.devicePixelRatio}`,\n    nav.deviceMemory ? `${nav.deviceMemory} GB` : '',\n    `${window.screen.width}×${window.screen.height}`,\n  ]\n    .filter(Boolean)\n    .join(' / ')\n}\n\nfunction uaShort(): string {\n  const ua = navigator.userAgent\n  const ios = ua.match(/OS (\\d+[_.]\\d+)/)\n  if (ios) return `iOS ${ios[1].replace('_', '.')}`\n  const android = ua.match(/Android [\\d.]+/)\n  if (android) return android[0]\n  const safari = ua.match(/Version\\/([\\d.]+).*Safari/)\n  if (safari) return `Safari ${safari[1]}`\n  const chrome = ua.match(/Chrome\\/([\\d.]+)/)\n  if (chrome) return `Chrome ${chrome[1]}`\n  return ua.slice(0, 60)\n}\n\nfunction render(results: BenchStat[]) {\n  tableBody.textContent = ''\n  for (const r of results) {\n    const tr = document.createElement('tr')\n    const ratio = r.mean / 16.67\n    const level = ratio > 0.5 ? 'hot' : ratio > 0.2 ? 'warm' : 'ok'\n    tr.innerHTML = `<td>${r.name}</td><td class=\"num\">${r.mean.toFixed(3)}</td><td class=\"num\">${r.p95.toFixed(3)}</td><td class=\"num ${level}\">${(ratio * 100).toFixed(0)}%</td><td class=\"detail\">${r.detail}</td>`\n    tableBody.appendChild(tr)\n  }\n  copyBtn.hidden = false\n  rerunBtn.hidden = false\n}\n\nlet report = ''\n\ncopyBtn.addEventListener('click', async () => {\n  try {\n    await navigator.clipboard.writeText(report)\n    copyBtn.textContent = '已复制'\n    setTimeout(() => (copyBtn.textContent = '复制报告'), 1200)\n  } catch {\n    prompt('复制以下内容：', report)\n  }\n})\n\nrerunBtn.addEventListener('click', () => location.reload())\n\nasync function main() {\n  status.textContent = `设备：${deviceSummary()} · ${uaShort()}`\n  status.textContent += ` · 模拟 ${seconds}s`\n  if (wantWasm) {\n    status.textContent += (await ensureWasmFluid()) ? ' · wasm 已装载' : ' · wasm 不可用'\n  }\n  // 让状态先绘制再进入密集计算\n  await new Promise((r) => setTimeout(r, 60))\n  const t0 = performance.now()\n  const results = runBench({\n    seconds,\n    includeWasm: wantWasm,\n    onProgress: (p) => {\n      status.textContent = `运行中 ${(p * 100).toFixed(0)}%…（页面短暂卡顿属正常）`\n    },\n  })\n  const wall = ((performance.now() - t0) / 1000).toFixed(1)\n  const engine = results.some((r) => r.name.includes('wasm')) ? 'js+wasm' : 'js'\n  report = [\n    `造风 bench · ${new Date().toISOString()}`,\n    `UA: ${navigator.userAgent}`,\n    `设备: ${deviceSummary()} · ${uaShort()} · 引擎 ${engine}`,\n    ...results.map(\n      (r) => `${r.name}: mean ${r.mean.toFixed(3)}ms p95 ${r.p95.toFixed(3)}ms (${r.detail})`,\n    ),\n    `基准耗时 ${wall}s（${seconds}s 模拟）`,\n  ].join('\\n')\n  status.textContent = `完成（基准耗时 ${wall}s）。帧预算占比 = mean / 16.7ms。`\n  render(results)\n}\n\nvoid main()\n"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/src/dev/bench.ts`

**Output:**

*Diff for `bench.ts`:*
```diff
/**
 * 浏览器端性能诊断页（bench.html）入口：真机/真浏览器实测帧预算占用。
 *
 * 用途：
 * - 研发用 Chrome DevTools / Safari WRDP 配合 CPU 节流复现低端机
 * - 玩家设备（尤其 iPhone）直接打开即可得到本机数据，回报给研发
 * 结果可一键复制为纯文本。
 */
import { ensureWasmFluid, runBench, type BenchStat } from './bench-core'

const params = new URLSearchParams(location.search)
const seconds = Math.max(2, Math.min(60, Number(params.get('seconds') ?? 10)))
const wantWasm = params.get('wasm') !== '0'

const status = document.getElementById('status')!
const tableBody = document.getElementById('results')!
const copyBtn = document.getElementById('copy') as HTMLButtonElement
const rerunBtn = document.getElementById('rerun') as HTMLButtonElement

function deviceSummary(): string {
  const nav = navigator as Navigator & {
    deviceMemory?: number
    userAgentData?: { platform?: string }
  }
  return [
    nav.userAgentData?.platform || navigator.platform,
    `${navigator.hardwareConcurrency ?? '?'} 核`,
    `dpr ${window.devicePixelRatio}`,
    nav.deviceMemory ? `${nav.deviceMemory} GB` : '',
    `${window.screen.width}×${window.screen.height}`,
  ]
    .filter(Boolean)
    .join(' / ')
}

function uaShort(): string {
  const ua = navigator.userAgent
  const ios = ua.match(/OS (\d+[_.]\d+)/)
  if (ios) return `iOS ${ios[1].replace('_', '.')}`
  const android = ua.match(/Android [\d.]+/)
  if (android) return android[0]
  const safari = ua.match(/Version\/([\d.]+).*Safari/)
  if (safari) return `Safari ${safari[1]}`
  const chrome = ua.match(/Chrome\/([\d.]+)/)
  if (chrome) return `Chrome ${chrome[1]}`
  return ua.slice(0, 60)
}

function render(results: BenchStat[]) {
  tableBody.textContent = ''
  for (const r of results) {
    const tr = document.createElement('tr')
    const ratio = r.mean / 16.67
    const level = ratio > 0.5 ? 'hot' : ratio > 0.2 ? 'warm' : 'ok'
    tr.innerHTML = `<td>${r.name}</td><td class="num">${r.mean.toFixed(3)}</td><td class="num">${r.p95.toFixed(3)}</td><td class="num ${level}">${(ratio * 100).toFixed(0)}%</td><td class="detail">${r.detail}</td>`
    tableBody.appendChild(tr)
  }
  copyBtn.hidden = false
  rerunBtn.hidden = false
}

let report = ''

copyBtn.addEventListener('click', async () => {
  try {
    await navigator.clipboard.writeText(report)
    copyBtn.textContent = '已复制'
    setTimeout(() => (copyBtn.textContent = '复制报告'), 1200)
  } catch {
    prompt('复制以下内容：', report)
  }
})

rerunBtn.addEventListener('click', () => location.reload())

async function main() {
  status.textContent = `设备：${deviceSummary()} · ${uaShort()}`
  status.textContent += ` · 模拟 ${seconds}s`
  if (wantWasm) {
    status.textContent += (await ensureWasmFluid()) ? ' · wasm 已装载' : ' · wasm 不可用'
  }
  // 让状态先绘制再进入密集计算
  await new Promise((r) => setTimeout(r, 60))
  const t0 = performance.now()
  const results = runBench({
    seconds,
    includeWasm: wantWasm,
    onProgress: (p) => {
      status.textContent = `运行中 ${(p * 100).toFixed(0)}%…（页面短暂卡顿属正常）`
    },
  })
  const wall = ((performance.now() - t0) / 1000).toFixed(1)
  const engine = results.some((r) => r.name.includes('wasm')) ? 'js+wasm' : 'js'
  report = [
    `造风 bench · ${new Date().toISOString()}`,
    `UA: ${navigator.userAgent}`,
    `设备: ${deviceSummary()} · ${uaShort()} · 引擎 ${engine}`,
    ...results.map(
      (r) => `${r.name}: mean ${r.mean.toFixed(3)}ms p95 ${r.p95.toFixed(3)}ms (${r.detail})`,
    ),
    `基准耗时 ${wall}s（${seconds}s 模拟）`,
  ].join('\n')
  status.textContent = `完成（基准耗时 ${wall}s）。帧预算占比 = mean / 16.7ms。`
  render(results)
}

void main()

```


### Tool: WriteFile: Writing to sfgame-web/bench.html
**Status**: completed

**Input:**
```json
{
  "file_path": "/Users/sf/dev/scnucj2026/sfgame-web/bench.html",
  "content": "<!doctype html>\n<html lang=\"zh-CN\">\n  <head>\n    <meta charset=\"UTF-8\" />\n    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0, viewport-fit=cover\" />\n    <title>造风 · 性能诊断</title>\n    <style>\n      html {\n        font-size: 16px;\n      }\n      body {\n        margin: 0;\n        padding: 1rem 1rem calc(1rem + env(safe-area-inset-bottom));\n        font-family: -apple-system, 'PingFang SC', system-ui, sans-serif;\n        background: #fff8ea;\n        color: #3d3427;\n      }\n      h1 {\n        font-size: 1.15rem;\n        margin: 0.2rem 0 0.6rem;\n      }\n      #status {\n        font-size: 0.8rem;\n        line-height: 1.5;\n        color: #6b5b45;\n        margin-bottom: 0.8rem;\n        word-break: break-all;\n      }\n      table {\n        width: 100%;\n        border-collapse: collapse;\n        font-size: 0.82rem;\n        background: #fffdf7;\n        border-radius: 12px;\n        overflow: hidden;\n      }\n      th, td {\n        padding: 0.45rem 0.5rem;\n        text-align: left;\n        border-bottom: 1px solid #eee1c8;\n      }\n      th {\n        font-weight: 600;\n        color: #8a7a5f;\n      }\n      .num {\n        text-align: right;\n        font-variant-numeric: tabular-nums;\n        white-space: nowrap;\n      }\n      .detail {\n        color: #a09277;\n        font-size: 0.72rem;\n      }\n      td.ok {\n        color: #2fbf71;\n      }\n      td.warm {\n        color: #d99a2b;\n      }\n      td.hot {\n        color: #e05a3a;\n        font-weight: 700;\n      }\n      .btns {\n        margin-top: 0.9rem;\n        display: flex;\n        gap: 0.6rem;\n      }\n      button {\n        font-size: 0.9rem;\n        padding: 0.55rem 1.1rem;\n        border-radius: 999px;\n        border: 1px solid #d8c193;\n        background: #fff;\n        color: #3d3427;\n      }\n      button:active {\n        background: #f3e8cf;\n      }\n      .note {\n        margin-top: 0.9rem;\n        font-size: 0.72rem;\n        color: #a09277;\n        line-height: 1.6;\n      }\n    </style>\n  </head>\n  <body>\n    <h1>造风 · 性能诊断</h1>\n    <div id=\"status\">准备中…</div>\n    <table>\n      <thead>\n        <tr>\n          <th>组件</th>\n          <th class=\"num\">mean ms</th>\n          <th class=\"num\">p95 ms</th>\n          <th class=\"num\">帧预算</th>\n          <th>说明</th>\n        </tr>\n      </thead>\n      <tbody id=\"results\"></tbody>\n    </table>\n    <div class=\"btns\">\n      <button id=\"copy\" hidden>复制报告</button>\n      <button id=\"rerun\" hidden>重新运行</button>\n    </div>\n    <p class=\"note\">\n      测量与真实游戏相同的物理负载（第 1 关网格、4 源、240 示踪粒子），不含 GPU\n      渲染。帧预算占比超 50%（红色）即该组件是本机掉帧主因。 参数：?seconds=秒数（2–60）\n      · ?wasm=0 禁用 wasm 对比。\n    </p>\n    <script type=\"module\" src=\"/src/dev/bench.ts\"></script>\n  </body>\n</html>\n"
}
```

**Affected Files:**
- `/Users/sf/dev/scnucj2026/sfgame-web/bench.html`

**Output:**

*Diff for `bench.html`:*
```diff
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <title>造风 · 性能诊断</title>
    <style>
      html {
        font-size: 16px;
      }
      body {
        margin: 0;
        padding: 1rem 1rem calc(1rem + env(safe-area-inset-bottom));
        font-family: -apple-system, 'PingFang SC', system-ui, sans-serif;
        background: #fff8ea;
        color: #3d3427;
      }
      h1 {
        font-size: 1.15rem;
        margin: 0.2rem 0 0.6rem;
      }
      #status {
        font-size: 0.8rem;
        line-height: 1.5;
        color: #6b5b45;
        margin-bottom: 0.8rem;
        word-break: break-all;
      }
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 0.82rem;
        background: #fffdf7;
        border-radius: 12px;
        overflow: hidden;
      }
      th, td {
        padding: 0.45rem 0.5rem;
        text-align: left;
        border-bottom: 1px solid #eee1c8;
      }
      th {
        font-weight: 600;
        color: #8a7a5f;
      }
      .num {
        text-align: right;
        font-variant-numeric: tabular-nums;
        white-space: nowrap;
      }
      .detail {
        color: #a09277;
        font-size: 0.72rem;
      }
      td.ok {
        color: #2fbf71;
      }
      td.warm {
        color: #d99a2b;
      }
      td.hot {
        color: #e05a3a;
        font-weight: 700;
      }
      .btns {
        margin-top: 0.9rem;
        display: flex;
        gap: 0.6rem;
      }
      button {
        font-size: 0.9rem;
        padding: 0.55rem 1.1rem;
        border-radius: 999px;
        border: 1px solid #d8c193;
        background: #fff;
        color: #3d3427;
      }
      button:active {
        background: #f3e8cf;
      }
      .note {
        margin-top: 0.9rem;
        font-size: 0.72rem;
        color: #a09277;
        line-height: 1.6;
      }
    </style>
  </head>
  <body>
    <h1>造风 · 性能诊断</h1>
    <div id="status">准备中…</div>
    <table>
      <thead>
        <tr>
          <th>组件</th>
          <th class="num">mean ms</th>
          <th class="num">p95 ms</th>
          <th class="num">帧预算</th>
          <th>说明</th>
        </tr>
      </thead>
      <tbody id="results"></tbody>
    </table>
    <div class="btns">
      <button id="copy" hidden>复制报告</button>
      <button id="rerun" hidden>重新运行</button>
    </div>
    <p class="note">
      测量与真实游戏相同的物理负载（第 1 关网格、4 源、240 示踪粒子），不含 GPU
      渲染。帧预算占比超 50%（红色）即该组件是本机掉帧主因。 参数：?seconds=秒数（2–60）
      · ?wasm=0 禁用 wasm 对比。
    </p>
    <script type="module" src="/src/dev/bench.ts"></script>
  </body>
</html>

```


## User

Background agent "general-purpose: 调研 iOS wasm/MoonBit 现状" failed.

