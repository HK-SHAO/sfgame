{
  "name": "sfgame-web",
  "private": true,
  "version": "0.1.0",
  "description": "烧风：太阳精灵 · 用温度创造风",
  "author": {
    "name": "HK-SHAO",
    "email": "hk-shao@outlook.com"
  },
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "tsc -b && bun run build:wasm && vite build",
    "preview": "vite preview",
    "test": "bun run build:wasm && vitest run",
    "typecheck": "tsc -b",
    "build:wasm": "bun run scripts/build-wasm.ts",
    "check": "bun run typecheck && bun run test && vite build"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "assemblyscript": "^0.28.20",
    "typescript": "~7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
