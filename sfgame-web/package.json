{
  "name": "sfgame-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "check": "npm run typecheck && npm run test && npm run build",
    "build:native": "cd native && moon build --target wasm --release && cp _build/wasm/release/build/fluid.wasm ../src/sim/fluid.wasm"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "~7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "module": "index.ts"
}
