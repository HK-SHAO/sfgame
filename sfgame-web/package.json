{
  "name": "sfgame-web",
  "private": true,
  "version": "0.2.0",
  "description": "烧风：太阳精灵 · 用温度创造风",
  "author": {
    "name": "HK-SHAO",
    "email": "hk-shao@outlook.com"
  },
  "type": "module",
  "scripts": {
    "dev": "bun node_modules/vite/bin/vite.js",
    "build": "bun tsc -b && bun run build:wasm && bun node_modules/vite/bin/vite.js build",
    "preview": "bun node_modules/vite/bin/vite.js preview",
    "test": "bun run build:wasm && bun vitest run",
    "typecheck": "bun tsc -b",
    "build:wasm": "bun run scripts/build-wasm.ts",
    "check": "bun run typecheck && bun run test && bun vite build"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "~24",
    "assemblyscript": "^0.28.20",
    "typescript": "~7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
