{
  "name": "sfgame-web",
  "private": true,
  "version": "0.2.0",
  "description": "烧风：太阳精灵 · 用温度创造风",
  "author": {
    "name": "HK-SHAO",
    "email": "hk-shao@outlook.com"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node scripts/build-wasm.ts && vite build",
    "preview": "vite preview",
    "test": "node scripts/build-wasm.ts && vitest run",
    "typecheck": "tsc -b",
    "build:wasm": "node scripts/build-wasm.ts",
    "check": "tsc -b && node scripts/build-wasm.ts && vitest run && vite build"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "~24",
    "assemblyscript": "^0.28.20",
    "typescript": "~7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
