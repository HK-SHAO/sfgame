{
  "name": "sfgame-web",
  "private": true,
  "version": "0.0.0",
  "description": "烧风 · 一个用能量创造风的游戏",
  "author": { "name": "HK-SHAO", "email": "hk-shao@outlook.com" },
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "tsc -b && bun run build:wasm && vite build",
    "preview": "vite preview",
    "test": "bun run build:wasm && vitest run",
    "typecheck": "tsc -b",
    "build:wasm": "bun run scripts/build-wasm.ts",
    "check": "bun run typecheck && bun run test && vite build"
  },
  "dependencies": {
    "lit": "^3.3.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "assemblyscript": "^0.28.20",
    "typescript": "~7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
