---
name: level-design
description: 为「造风」（sfgame-web，Lit 3 + Canvas 2D 物理益智）设计新关卡：按协议 v1（YAML 序列化 + 表达式地形 + 六个原子字段）搭建关卡，用无头仿真验证可玩性，并注册参考答案。适用于新增关卡、DIY 关卡传播、教学关设计。
---

# 造风关卡搭建指南（协议 v1）

本 skill 指导用「积木式」协议搭建关卡。核心信念：**少量精选的原子能力，通过组合产生乘法式/涌现式的关卡**，而不是堆砌新机制。每个关卡是纯数据（YAML），可在无头引擎内跑完整模拟，方便调试、测试与接入算法/AI。

## 1. 一个关卡长什么样

关卡是 `sfgame-web/levels/level-N.yaml`（内置关卡）或任意 `.yaml` 文件（DIY 传播）。完整示例：

```yaml
# 第 3 关 · 信风 —— 站点序列：把路程拆成站点，逐站送达
schema: 1            # 协议版本，必填
id: 3                # 正整数
name: 信风
tagline: 站点序列
hint: 第一站在前方平原，第二站要翻过山脊。让飞机先抵达第一站，再在迎风坡放热源把它托上山脊。
win:
  title: 一站又一站
  text: 风把信从一站送到下一站。抵达不是终点，而是下一段路程的起点。
world: { w: 76, h: 56, cell: 0.75 }
ground:
  expr: 30 - 8*smoothstep((x-20)/16)
budget: { hot: 3, cold: 2 }
spawn: { x: -4, y: 16, vx: 40 }
goals:
  - { x: 15, r: 10 }
  - { x: 40, r: 9 }
ambient: { x: 2.4, y: 0 }
solutions:
  - name: 单热源翻山脊
    sources:
      - { x: 26, y: 28, kind: hot }
    winTime: 8.8   # 无头实测值，solutions.test.ts 守护 ±2s
```

序列化选型：**YAML**（易读、可写注释、锚点 `&a`/`*a` 复用重复值）；地形只用**表达式字符串**（精准、高密度、可移植到任意数学工具），刻意不用采样折线（不精准）也不引 path（与表达式重叠、求值复杂——堆积功能）。

## 2. 六个原子能力（积木）

| 原子 | 字段 | 作用 | 示例关卡 |
| --- | --- | --- | --- |
| 世界 | `world` | 画布尺寸与流体网格（w/h/cell） | 全部 |
| 地形 | `ground.expr` | y=f(x) 公式，定义空间与障碍 | 全部 |
| 预算 | `budget` | 热/冷源数量，资源约束 | 全部 |
| 起点 | `spawn` | 出生位置与初速度（可在画布外飞入） | 全部 |
| 站点序列 | `goals[]` | 按顺序飞行抵达的站点（≥1），顺序即访问顺序 | L3/L5 |
| 环境风 | `ambient` | 常风 `{x,y}` + 可选潮汐 `tide{period,phase,ampX,ampY}` | L1/L2 常风、L4 潮汐 |

参考解法（`solutions[]`）不是新原子，而是验证产物：全部源一次性放置，无头实测通关时刻写入 `winTime`。

### 组合语法（乘法式）

六个积木本身简单，组合起来涌现出玩法：

- 热源 + 地形 = 上升气流托升（L1）
- 冷源 + 地形 = 下沉气流压降（L2）
- 站点序列 + 地形 = 多段旅程（L3「信风」、L5「回归」）
- 常风 + 潮汐 = 风有涨落的节奏（L4「潮汐」）
- 热 + 冷 并排 = 对流传动的水平风（涌现，但实测强热柱轨迹混沌——见反模式）

设计任何关卡时先问：**我在组合哪些原子？有没有引入一次性特例？** 协议里出现新字段 = 新原子，必须能用在多个关卡上才值得加（第二关测试）。

## 3. 地形表达式函数表

`ground.expr` 是 y = f(x)，世界坐标 y 向下。支持：

- 变量/常量：`x`、`PI`、`E`
- 运算符：`+ - * / % ^`（幂，右结合）、一元正负、括号
- 函数：`abs min max clamp(v,lo,hi) step(x,edge) smoothstep(t) sin cos exp sqrt pow(a,b)`

常用配方：

```text
平原→高原平滑爬升：     30 - 8*smoothstep((x-20)/16)      # x∈[20,36] 由 30 升至 22
对称孤山：              44 - 24*smoothstep((x-34)/4)*smoothstep((50-x)/4)
两级山脊（L5）：        30 - 6*smoothstep((x-20)/10) - 6*smoothstep((x-44)/10)
```

`smoothstep(t)` 自动钳制输入到 [0,1]；`step(x,edge)` 在 x≥edge 时为 1。坡度过陡（>1:1）的段飞机无法滑爬，只能靠热源托升——这本身就是谜题设计工具。

## 4. 设计要义（行业经验沉淀）

- **起承转合**：任天堂 3D 关卡（Hayashida 访谈）把每关做成「引入 → 发展 → 转折 → 收束」。教学关尤其要一段只教一个概念。
- **假设 → 挫败 → 顿悟**（Mark Brown）：先让玩家带着旧经验尝试，失败后重估，获得新认识。L3 的「第一站自然抵达、第二站被山脊挡住」就是这个结构。
- **解法先行倒推**：先手工摆出可通关摆法，再倒推地形/预算；早期关卡保证只有一个明显思路（Portal Test Chamber 01 的教训：要强制顺序）。
- **零操作挂机不能通关**（本项目回归不变量）：但允许自然抵达前几站做「免费教学」。
- **克制**：不堆机制。一个关卡表达一个概念；新原子必须通过「第二关测试」——能想象出第二个不同用法才保留。
- **浪漫感**：#10 的叙事基调：信息论（信风逐站传递）、熵与时间之箭（潮汐）、庞加莱回归与宇宙演化（回归）。叙事嵌在 `win`/`tagline`/`hint` 里，机制先成立，文案后补。

## 5. 验证工作流（每个新关卡必须走完）

```bash
cd sfgame-web
# 1. 无操作 30s：必须不通关
bun run scripts/run-level.ts levels/level-N.yaml --sim 30
# 2. 参考解：必须可通关且快（教学关建议 ≤25s）
bun run scripts/run-level.ts levels/level-N.yaml --verify 26-28-h
# 3. 全量测试：solutions.test.ts 守护每个解的 winTime ±2s
bun run test
# 4. 浏览器一致性：无头结果与真实浏览器逐位一致（差 ≤1 模拟步）
bun run scripts/browser-consistency.ts
```

调参搜索经验（代价昂贵，先算复杂度）：

- **两段式搜索**：粗筛 `dt=1/30`、cap 25s、早退 → 精验 `dt=1/60`（与浏览器一致）。粗筛胜点可能是假阳性（dt 差异），必须精验。
- **扰动抽查**：把参考解每个源做 ±1 单位 8 方向扰动，统计仍通关的比例。≥6/8 视为「实际可玩」；全挂说明站点圆太小或布局是刀尖路径，回炉。
- 组合搜索前估耗时：N 候选点、k 源 ≈ C(N,k)×(单次模拟成本)。模拟成本 ≈ 网格格子数 × 步数 × ~0.3µs。超预算就缩小候选、加早退、分段打印进度。

## 6. 反模式（实测血泪）

- **强热柱接力**：轻质量飞机在强热柱中轨迹混沌，摆放差 1-2 单位结果剧变（#8 报告）。避开「必须精确命中」的布局。
- **站点圆太小**：站点边缘擦过 = 几乎必败。教学关站点 r 用 9-11，给容差。
- **采样地形**：折线采样不精准、信息密度低。用表达式，一行顶几十个点。
- **粗筛即定稿**：`dt=1/30` 的胜点在 `dt=1/60` 会翻车。所有参考解必须精验。
- **一关教太多**：一个关卡放三个新机制 = 没人能学会。把新概念拆到后续关卡。

## 7. 登记新关卡

1. 新建 `sfgame-web/levels/level-N.yaml`（含 `solutions` 与实测 `winTime`）
2. 在 `src/game/levels.ts` 的 LEVELS 数组登记文件名一行
3. 走完第 5 节验证工作流
4. 选关页、解法参考页、URL 直达（`?lv=N&src=…`）自动生效，无需额外 UI 改动
